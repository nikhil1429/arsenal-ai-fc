# NOTEBOOKLM_SETUP.md — the reference shelf (input surface, never a rep surface)

NotebookLM stays what canon says it is: study/input, not a rep surface.
The organism feeds it; it never feeds the organism directly.

## The one notebook: "Arsenal — The Season"
1. notebooklm.google.com → New notebook → **"Arsenal — The Season"**.
2. Sources to add (5 min, once):
   - Each locked capsule: open the gist raw URL per capsule → copy → add as
     pasted text (tokenization, embeddings, inference, context — and each new
     lock as it happens; the mirror keeps local copies but NotebookLM wants
     its own paste).
   - `learning-layer/OPPONENT_SCOUT.md` (the DOSSIER — probe grammar as audio revision).
   - `learning-layer/FINOPS_AI_CONCEPTS.md` + `FINOPS_MODULE3_PROCUREMENT_INTEL.md`.
3. Use it two ways only (keeps it an input, not a crutch):
   - **Audio Overviews** on commute/walks — passive re-exposure between reps.
   - **Quiz me from sources** — but log the outcomes as Gem reps if you want
     them counted (paste through the Drill Gem session flow).

## Weekly (30 sec)
After a new capsule locks: add it as a source. That's the whole maintenance.
