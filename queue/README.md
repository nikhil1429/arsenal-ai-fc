# ⚠ THIS IS NOT THE ESCALATION CHANNEL

**The channel is:**

```
C:\Users\nikhi\arsenal-audit-artifacts\queue\
```

— the `queue\` that sits **beside `ARCHITECT_HANDOFF.md`**, outside the repo. His ruling,
25 Aug 2026. That is where escalations, `RULING__*` files and rung records go, and it is
where the architect session actually reads.

## Why this file exists

This directory looks exactly like the channel, and it is the one an executor already has
open. On **28 Aug 2026 it took two victims in a single day**:

1. S8's `2026-08-28_s8-step0-blocked.md` was searched for here, not found, and reported as
   *"a claimed record that was never written"* — it existed in the channel the whole time.
2. The S9 incident record was **written here**, where the architect would never have seen
   it. It had to be copied across by hand.

Both were the same mistake, made twice in a few hours, by sessions that had read the order.
That is a class, not carelessness — so it gets a signpost rather than a reminder.

## The rule

- **Escalation, ruling, or rung record →** the artifacts `queue\` above. Always.
- **Anything already in this folder** stays (L9 — nothing is deleted or moved). It is
  history, not the channel.

If you are about to write a record here, you are almost certainly in the wrong directory.
