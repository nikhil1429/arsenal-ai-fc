# ESCALATION — S5 corpus half · the critic's REWARDED new kinds reach no consumer

**From:** S5 sweep session, 26 Aug 2026 ~06:20 IST · Opus 5 · effort HIGH · DEFAULT mode
**Status:** NOT blocking. B01 is complete 3/3 and B02 can proceed. This needs a ruling before
the *next* critic answer is verified, not before the next load.

## CONTEXT IN TWO LINES
B01_3 (the critic) returned and was captured through the byte-strict RAW path (copy-gate 15/15,
JSON parses). It obeyed the architect's shape-list ruling exactly — it proposed TWO kinds that were
not in the original five buckets and hunted for them: `unresolved_anomalies` and `manual_workarounds`.

## THE FORK
**`verify.mjs` has no home for an unlisted kind.** Its buckets are the six known ones
(decisions · rulings · promises · alternatives_rejected · missing_implementation · supersessions).
Measured on the live answer: both new keys are present as top-level arrays in `answers/B01_3.txt`,
and `verified.json` carries neither. The items are **not quote-checked, not kept, and not dropped** —
they are silently absent from the verified record, and they are absent from the KEPT 84 / DISCARDED 44
headline too, so the yield numbers understate what actually came back.

That is the ruling's own reward with no consumer — the shape §9 calls SHAPE 6, committed inside the
audit's own instrument (the third time this rung has found SHAPE 1/6 inside its own tooling:
refilter.mjs at S5, verify.mjs's day index at S5-R2, this).

**Why I did not fix it:** the fix is a code change to `verify.mjs` and **S5 BUILDS NOTHING**
(§10-D rule 10 + the rung's FORBIDDEN line). Nothing is lost — the items are on disk in
`answers/B01_3.txt` and re-verify for free once the instrument has a home for them.

## OPTIONS
- **A (my recommendation) — a micro-rung after the corpus half, before §9 intake.** `verify.mjs`
  gains a DECLARED-UNKNOWN bucket: any top-level array key that is not one of the six is
  quote-checked and coverage-checked by the SAME code path, and reported under its own returned
  name rather than being dropped on the floor. Declare-or-die, the same predicate shape S5-X used
  for the snapshot roster — a universal quantifier that excludes a subject without declaring it is
  the JUGAD RULE's own violation. Cost: small, one instrument, bite-proofs both ways.
  Runs alongside / after S5-Z4, never interleaved into a live chat.
- **B — leave it, and have S6 read the raw answers for unlisted kinds by hand.** Cheapest now,
  but it puts a human read back on the critical path for exactly the class the ruling called
  "worth MORE than another instance of a listed kind", and it will silently repeat on all 17
  remaining critic answers.
- **C — drop the outside-the-list task from the critic prompt.** Refused on its face: it reverses
  a standing architect ruling to make an instrument's gap go away, and it is a prompt edit
  (S5 builds nothing).

## WHAT I NEED BACK
One line: A, B, or C — and if A, whether it rides S5-Z4's session or takes its own row.

## SECOND ITEM, SAME SESSION — for the record, no decision needed
**The SENT-TURN read-back (standing outbound law, ratified ~04:15) cannot match raw, ever.**
Gemini's user-turn renderer wraps every non-empty line in one leading and one trailing U+0020.
Measured on B01_3: box 2151 chars, sent turn 2201, delta exactly 50 = 25 non-empty lines x 2.
Line count identical (31/31) and every line character-identical once the pad is stripped;
stripped, the sent-turn sha256 is byte-identical to the step file. **So the law's fourth hop must
strip one leading and one trailing space per line before hashing.** A future executor that hashes
the sent turn raw will read a mismatch on a perfectly-delivered prompt and halt for nothing.
This is a note for whoever writes the outbound law into code, not a fork.

## THIRD ITEM — a measurement the plan should carry
The critic pass yields far less than the extraction and inflates its receipt hard.
B01_2: KEPT 75 / DISCARDED 22 (~77% survival), 1 inflated day.
B01_3: KEPT 9 / DISCARDED 22 (~29% survival), **5 inflated days of 7** — including two days it
claimed 5 and 10 items for and delivered 0. Plus one ITEM-COUNT-MISMATCH ("declared 5, array holds
4 — the answer was cut mid-flight"). Every one of those was caught by a gate, which is the system
working; but if the ratio holds across B02-B18 the critic pass is buying ~1 kept item per 3.4
returned, and that is a cost/benefit line S6 should see before it budgets the remaining 17 critics.
