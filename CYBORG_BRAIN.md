# ⚪🔴 CYBORG_BRAIN.md — the next evolution, designed
### The organism → a real brain: a thalamus, seven parallel minds, a two-speed cortex, a five-layer memory, and a night-shift on a ₹1,500 Pi.
### ⚪🔴 · design pass 12 Jul 2026 · this is the BUILD SPEC for a fresh session · nothing here is built yet (except the foundations noted in §1)

> **(corrected 10 Aug 2026 — READ THIS BEFORE THE LINE ABOVE.)** "nothing here is built yet"
> was true for ~48 hours and has been FALSE since 14 Jul. It is the single most dangerous line
> in this file: a session that reads it and stops re-plans an organism that is already running.
> Live check, 10 Aug 2026: every organ named in §10's M0→M10 exists on disk and sits in a
> selftest suite — `node scripts/organism_test.mjs suites` runs them all by name, and
> `Get-ScheduledTask | ? { $_.TaskName -like "ArsenalFC*" }` shows the daemons on the real
> Windows schedule. The line is kept verbatim as the design-pass artefact it is; the BUILD
> STATUS block below is the live status. **Never plan from line 3. Plan from a live command.**

> **BUILD STATUS (14 Jul 2026): M0–M10 ARE BUILT, GREEN, AND SCHEDULED** — one labelled commit per unit (`organism M0…M9`, M10 inside M2's store functions + its nightly task), each selftested + real-run per the law. 39 suites green. New organs: `thalamus.mjs` (:4113) · `cortex.mjs` · `hippocampus.mjs` · `fuelboard.mjs` · `examiner.mjs` · `tone.mjs` · `dmn.mjs` · `presence.mjs` · `council.mjs` · `groundsman.mjs` · `awayday.mjs`; 38 scheduled tasks (`setup/INSTALL_CYBORG_TASKS.ps1`); the Dugout boots both daemons on every matchday start.
> **(corrected 10 Aug 2026 — the two NUMBERS in the line above rotted; the organ list held.)**
> All eleven organ files still exist (checked with `ls scripts/{thalamus,cortex,hippocampus,fuelboard,examiner,tone,dmn,presence,council,groundsman,awayday}.mjs` — 11 of 11 present), and `:4113` is still the thalamus port (`grep -n "const PORT" scripts/thalamus.mjs` → `4113 // one below the Dugout's 4114`). But:
> · **"39 suites green" is a 14 Jul snapshot.** The suites have roughly doubled since. Count them live, never from here: `node scripts/organism_test.mjs suites` (it prints every member and its verdict), or `npm test` for the authority run. On 10 Aug the two membership chains in `package.json` held **51 organism + 22 squad = 73** members. Do not copy that number in either — it moves on the next organ.
> · **"38 scheduled tasks (`setup/INSTALL_CYBORG_TASKS.ps1`)" mis-attributes and under-counts.** That installer creates **19** tasks (`grep -cE '^(Mk|MkHidden) ' setup/INSTALL_CYBORG_TASKS.ps1`); the rest come from `INSTALL_TASKS.ps1`, `INSTALL_CONDUCTOR.ps1` and `INSTALL_EVENING_CONDUCTOR.ps1`. The live schedule is the only truth: `Get-ScheduledTask | Where-Object { $_.TaskName -like "ArsenalFC*" }` — **50** on 10 Aug 2026. Read it, never quote it.
> **What stays hypothesis until it happens:** the first live-voice session with the whole arc (holding token + deep answer + whisper spoken through Charon mid-talk); the Pi's 48h `claude -p` OAuth smoke (§9 — physical step); wall_breaker ratification (the whisper stays behind the teeth until the shadow ledger earns it). **Wire-scars found while building (§12 was right):** live-socket `codeExecution` hangs the turn (the Chalkboard rides the REST sandbox as `run_python`); `googleSearch` has zero free quota in every shape (grounding honestly ABSENT); bare `gemini-3.1-flash` is dead on REST (`-latest` aliases survive churn); thinking models need generous output budgets in JSON mode.
> **(re-verified 10 Aug 2026 — this hypothesis line still HOLDS, which is why it is not being deleted.)** `node scripts/shadow.mjs status` → `wall_breaker: 0/10 scorable shadows · 0 hit(s) · training silently`, and `proactivity_ledger.json does not exist` — the whisper is still behind the teeth. The Pi's 48h OAuth smoke is still unrun (`dressing-room/state/bus_lease.json` does not exist on disk, so no node has ever taken the lease). Both wire-scars also still hold in code: `grep -n "codeExecution" scripts/dugout.mjs` shows the Chalkboard riding the REST sandbox as `run_python`, and the selftest asserts `search grounding honestly ABSENT (zero free quota — the wire said so)`.

> **Read this first, whole, in a new session.** It is self-contained: it + the repo is everything you need to build the evolution. It was distilled from ~475,000 tokens of parallel deep-thinking (6 specialist lenses) so the reasoning is done ONCE and never re-burned. Build in the order of §10. Every unit obeys the same law the whole repo lives by: **selftest green + real-run line + small labelled commit**, and **AI proposes · code validates · human approves.**
>
> Sibling docs: `THE_ORGANISM_A_TO_Z.md` (what exists today, A-Z) · `ORGANISM_ANATOMY.md` (the built body) · `ORGANISM_LEDGER.md` (BUILT/GATED/LEAP) · `CAPTAINS_HANDBOOK.md` (how the captain operates it).
> *(checked 10 Aug 2026: all four still exist at the repo root — `ls THE_ORGANISM_A_TO_Z.md ORGANISM_ANATOMY.md ORGANISM_LEDGER.md CAPTAINS_HANDBOOK.md`. They are PROSE, so they carry the same rot risk as this file: verify anything time-sensitive in them against code, never against each other.)*

---

## 0. THE ONE-PARAGRAPH VISION

Today the organism is a **batch machine**: it senses today, thinks overnight, acts tomorrow. This evolution makes it **living**: it senses *now*, thinks deep *only the moment it matters*, remembers *instantly*, dreams while the captain is away, and evolves continuously. The mechanism is a **two-speed brain** — a free, always-on **reflex layer** (Gemini Live across 7 accounts = 7 parallel cortical regions: eyes, ears, mouth) fused to a rare, profound **deep layer** (Claude Opus 4.8 with extended thinking) — joined by the one organ the body is missing: a **THALAMUS** that binds every sense into single "moments," scores their salience, and rations the deep brain so it wakes only for genuine surprise. The human stays the heart. The two brains serve him. The whole thing runs cloud-for-cognition, local-for-privacy, night-shift-on-a-Pi.

---

## 1. STATE AT HANDOFF (what is already true — do not rebuild)

- **Repo:** `nikhil1429/arsenal-ai-fc`, branch `main` (organism-final synced to it). ~103+ commits. 43 scripts, 39 green selftest suites (31 organism + 8 squad). Public repo; personal data all gitignored.
  **(corrected 10 Aug 2026 — every count in that sentence is a 12 Jul snapshot; the repo/branch and the gitignore claim held.)** `package.json` still names `git+https://github.com/nikhil1429/arsenal-ai-fc.git`. The numbers are all stale, so they are replaced by the commands that cannot rot:
  · commits → `git rev-list --count HEAD` (287 on 10 Aug, not ~103)
  · scripts → `git ls-files "scripts/*.mjs" | wc -l` (75 tracked, not 43)
  · suites → `node scripts/organism_test.mjs suites` (51 organism + 22 squad on 10 Aug, not 31 + 8)
  · "39 green" was a *result*, not a property — a suite is green only when you RUN it (`npm test`). Never read greenness out of a document.
- **THE TWO-SPEED SPLIT IS ALREADY THE LAW** (this session): fast reflex = Gemini Live; slow deep = **Claude Opus 4.8 extended thinking**, used only for the ~5% that needs real reasoning. The captain confirmed this is the target architecture.
- **7 GEMINI TANKS ARE LIVE** — verified `7 of 7 tanks LIVE`. 4-5 free Live-API developer keys (each a SEPARATE Google account = a separate free quota pool) + 2 Gemini AI Pro accounts (free via Jio: his second account + a family member's — addresses kept OUT of this public file; Deep Research, 1M context, NotebookLM, Veo). Keys are in gitignored `~/.gemini/.env` as `GEMINI_API_KEY`, `GEMINI_API_KEY_2…`. The Dugout's `loadKeys()` already reads the numbered pattern and rotates on quota.
  **(corrected 10 Aug 2026: the KEY MECHANISM held; the tank count and the word "LIVE" did not.)** `loadKeys()` is real and still reads the numbered pattern — `grep -n "GEMINI_API_KEY(_\\\\d+)?" scripts/dugout.mjs` shows the regex, and the selftest feeds it `GEMINI_API_KEY=k1 / _2 / _3` to prove rotation. But the registry is **EIGHT** tanks now, not seven (T8 Distiller joined with the working-memory build — see the §5 note), and one of them ships OFF: `dressing-room/state/fuelboard_config.json` carries `T3 Cochlea … "enabled": false`, and `node scripts/fuelboard.mjs` prints it `DEAD`. So "7 of 7 LIVE" is wrong twice over. Read the board, never this line: **`node scripts/fuelboard.mjs`** — it draws one bar per tank with its real state.
- **THE VOICE IS UNMUZZLED** (this session, "U5"): `dugout.mjs` — model chosen empirically = `gemini-3.1-flash-live-preview` (fastest first-audio ~0.58s, deepest, 65k output, only Live model that SEES). Constitution rewritten to **DEPTH IS OBEDIENCE**; `set_depth` voice lever (adaptive/brief/deep/lecture) → gitignored `dugout_prefs.json`, ships on `deep`. VAD hangover 1400ms (won't cut off a thinking pause). Vision sharpened (jpeg 0.82, up to 1280px, HD capture). 24 live tools.
  **(corrected 10 Aug 2026 — three of these five held exactly; two drifted.)** HELD: DEPTH IS OBEDIENCE is still in the constitution (`grep -n "DEPTH IS OBEDIENCE" scripts/dugout.mjs`, and a selftest asserts the old "Never lecture" muzzle is gone); `set_depth` still takes exactly `adaptive|brief|deep|lecture` and still persists to gitignored `dugout_prefs.json` (`git check-ignore dressing-room/state/dugout_prefs.json` → ignored); vision is still `jpeg_quality: 0.82, max_px: 1280`. DRIFTED: **the VAD hangover is 1500ms, not 1400** — `grep -n "silence_ms" scripts/dugout.mjs` shows `vad_server: { mode: "aligned", silence_ms: 1500 }` and the selftest pins it (`C1: server-VAD ships ALIGNED to his think-pauses (1500ms, probed)`). And **the tool count is not 24** — count it live rather than trusting any number here: `grep -n "functionDeclarations.length ===" scripts/dugout.mjs` (the selftest pins the real figure; it read 28 on 10 Aug, having passed through 26 at LADDER F1 and 27 at H6 — this is precisely the kind of number that rots every time a tool lands).
- **THE BRAIN'S P0 STARVATION BUG IS FIXED** (this session): `brain.mjs headroom()` now floors `cap0` at the conservative estimate; self-tune records `max(estimate, actual usage)` — a limit event can never again record `observed_window_ceiling=1` and switch off the overnight brain. **This exact starvation-guard MUST be ported to the new 7-tank ledger** (§6).
  **(corrected 10 Aug 2026 — the fix HELD, the imperative is DONE, and the cross-reference was always wrong.)** The brain-side guard is live: `grep -n "const cap0" scripts/brain.mjs` → `Math.max(est, (queueState && queueState.observed_window_ceiling) || est)`, and `grep -n "STARVATION GUARD" scripts/brain.mjs` shows the selftest driving `observed_window_ceiling: 1` and asserting the cap floors at the estimate. The port is no longer a TODO — it shipped with M3: `grep -n "STARVATION GUARD" scripts/fuelboard.mjs` → *"ported verbatim from the brain's P0 fix"*, `observed_ceiling = Math.max(quota_est, used_today)`. **And "(§6)" was a mis-pointer from the day it was written** — §6 is MEMORY; the tank ledger is described in §5 and §8. Read it as (§8).
- **The hard laws (inviolable — every organ below obeys them):** no metered API key ever (`claude -p` OAuth Max — `brain.mjs:430` REFUSES if `ANTHROPIC_API_KEY` set — **(corrected 10 Aug 2026: the LAW held, the LINE NUMBER rotted — there is no such guard at :430 any more. Never cite a line here; grep it: `grep -n "ANTHROPIC_API_KEY" scripts/brain.mjs` finds the live refusals, and the same guard now sits in `cortex.mjs` too (`grep -n "ANTHROPIC_API_KEY" scripts/cortex.mjs` → `cortex: ANTHROPIC_API_KEY set — REFUSING (Max plan only, never metered)`) and in `council.mjs`'s sonnet chair. The law spread; it did not weaken.)**; `gemini` OAuth via `~/.gemini/.env`); personal data (reps, biometrics, transcripts, frames) LOCAL + gitignored, public repo never holds it; no always-on ambient room mic (a two-human decision); humane clamps — no hype/shame/streaks/countdowns, win-only voicing, earned proactivity only; **medical clamp — biometrics never drive a verdict alone, prosody/emotion NEVER feeds any score; RED = doctor-referral, never self-interpreted.**

---

## 2. THE CLOUD QUESTION — answered: don't move to cloud, move DUTY to a Pi

The captain asked "should we move everything to cloud so my device stays light?" **The premise has a wrong word: heavy.** Nothing the laptop does is compute-heavy — all inference (Claude, Gemini, NotebookLM, Veo, Deep Research) is *already* remote. The laptop is a **thin client for cognition**; it only marshals tokens and relays audio.

The laptop is tethered by **DUTY, not LOAD**: stay awake overnight (the brain scheduler), watch the windows (ActivityWatch), hold the private bus. You don't fix a duty problem by shipping biometrics to a datacenter (breaks privacy law) or renting a VM with a paid key (breaks the $100 law).

**THE FIX — "THE KENNEL":** a ₹1,500 Raspberry Pi Zero 2 W (idle ~0.6-2.5 W vs a laptop's ~15-30 W) or a retired Android + Termux (₹0), **inside his own house** (crosses zero privacy lines — it's his hardware in his walls). Both CLIs run on ARM64 Linux via OAuth (no API key). It runs the night-shift; the main laptop becomes *optional to leave on*.

- **Cloud = inference (already true). Storage = his house** (until the post-offer Transfer Window reconsiders a real cloud store).
- **Ears + mouth NEVER move** — mic→VAD→PCM→Charon must sit milliseconds from the human; a second network hop kills turn-taking. Physics, not preference.
- **Reject Oracle/GCP free VMs** — "not his house" = a Transfer-Window-class privacy decision. The Pi is strictly better.
- Build detail in §7. **One uncertainty to smoke-test first:** whether `claude` Max CLI tolerates a long-lived headless OAuth session on the Pi (run `claude -p` under tmux 48h before trusting it with the window; fallback = Pi runs only the deterministic organs + git bus, which need no LLM auth).
  **(corrected 10 Aug 2026 — two things.** (1) **"§7" is a wrong cross-reference and always was:** the Kennel's build detail is **§9**, whose own title says so ("THE KENNEL — the night-shift body (§2's build detail)"). §7 is the emergent crown. (2) The smoke-test is **still unrun** and the Kennel **is not deployed** — `dressing-room/state/bus_lease.json` does not exist, so no node has ever taken the lease. The code to run it is ready (`scripts/groundsman.mjs`); the hardware step is his. Until then the premise of this whole section — "the main laptop becomes *optional* to leave on" — remains a plan, not a state. See §9's block.)

---

## 3. THE FOUNDATION — the two-speed brain (established; the frame for everything)

- **⚡ REFLEX BRAIN — Gemini Live · free · always-on · the senses.** Eyes (vision), ears (voice+transcription), mouth (Charon), across the 7 tanks. Catches everything in real time, sub-second, NEVER does deep judgment. Free → runs all day. Job: perceive, converse, and **capture the salient moment the instant it happens.**
> **(corrected 10 Aug 2026 — the version string "Opus 4.8" appears throughout this file (§0, §3, §5's T4) and appears NOWHERE in the code.** `cortex.mjs` spawns `claude -p --output-format json --model opus` — the **unpinned alias** — so the deep brain rides whatever `opus` resolves to on the Max plan on the day it runs, and a doc that names a version will be wrong the first time that alias moves. "Extended thinking" is genuinely wired, via the `MAX_THINKING_TOKENS` env var. Read the real invocation, never the version in this prose: `grep -n '"--model", "opus"' scripts/cortex.mjs`. The two-speed SPLIT — the thing this section is actually asserting — holds exactly.)

- **🧠 DEEP BRAIN — Claude Opus 4.8 + extended thinking · rare · profound · the judgment.** The hard reads: what's really happening with his learning, the coaching strategy, the genome mutation, the season's truth. Fired SPARINGLY — the ~5%. This is the ONLY place Claude tokens go.
- **🌉 THE BRIDGE.** Mid-conversation, the reflex Gaffer defers a hard question to Opus-extended (async), keeps talking, and the profound answer flows back into the live talk. Fast brain catches; deep brain thinks; no silence. (Mechanism: §4's async arc.)

---

## 4. THE KEYSTONE — `thalamus.mjs` (build this FIRST; nothing composes without it)

> **(corrected 10 Aug 2026 — "build this FIRST" was obeyed on 14 Jul; the keystone is IN.** The
> paragraph below ("Today the organism has two disconnected sensing regimes and no relay… There
> is no nucleus where every sense lands") describes the pre-M1 body and has been false for four
> weeks. The nucleus exists, is scheduled (`ArsenalFC-Thalamus`, daily 07:00), and answers on
> :4113. To see it rather than read about it: `node scripts/thalamus.mjs status` prints the
> current workspace and today's gate ledger with have/need counters. Everything in §4 is
> therefore a description of RUNNING code — except the specific numbers, which have all been
> tuned since (see §4.4's correction).)

Three independent lenses converged on this as THE missing organ. Today the organism has **two disconnected sensing regimes and no relay**: `heartbeat.mjs` is a batch cron sweep → `pulse.json`; `dugout.mjs` is real-time but its senses are **siloed and salience-blind** (each tool fires independently, nothing fuses a voice turn to the co-occurring screen frame, nothing scores whether a moment matters, the only path to Opus is a cron-latency human-armed trigger). There is no nucleus where every sense lands, no surprise computation, no tier decision, no cross-modal binding. **That missing nucleus is why it feels like a chatbot with tools instead of a cyborg.**

### 4.1 New organs (single-writer law honored)

> **(corrected 10 Aug 2026 — the table below is a BUILD-TIME ownership plan and it has drifted in
> three places. The organs are all real; the OWNERSHIP COLUMN is what went stale.)** Verify
> ownership from the organ's own header, never from this table — each script declares its files:
> `grep -n "sole writer\|single writer\|WRITES:" scripts/thalamus.mjs scripts/cortex.mjs scripts/gate_tune.mjs`.
> The three drifts are annotated inline in the rows.

| New code | Role | Sole writer of |
|---|---|---|
| `scripts/thalamus.mjs` | The relay nucleus + reticular gate. Persistent localhost daemon on **:4113** (one below Dugout's 4114). | `afferent.jsonl`, `workspace.json`, `salience_ledger.jsonl`, `wake.json` — **(corrected 10 Aug 2026: FOUR files is now an UNDER-count; the nucleus owns SEVEN.** Its own header declares five — `grep -n "single writer" scripts/thalamus.mjs` → *"this file alone writes afferent.jsonl · workspace.json · salience_ledger.jsonl · wake.json · wake_queue.jsonl"* (M14: wakes QUEUE, never clobber; `wake.json` is now the latest-wake mirror, not the handoff itself) — and two more landed after that header: `bg_queue.jsonl` (M22, `grep -n "appendBgQueue" scripts/thalamus.mjs`) and `dossier.json` (M8, `grep -n "writeDossier" scripts/thalamus.mjs`). All seven are gitignored — checked with `git check-ignore`.) |
| `scripts/cortex.mjs` | The deep-brain daemon. Watches `wake.json`, runs `claude -p --model opus` with extended thinking, POSTs the answer back to :4113 (writes *through* the thalamus, never directly — preserves single-writer). Reuses `brain.mjs` ledger + budget accounting. | (via thalamus) — **(corrected 10 Aug 2026: "(via thalamus)" is right about the THALAMUS's files and wrong that the cortex owns nothing.** It owns `cortex_runtime.json` and — via `cortex.mjs consolidate` — `concept_graph.json` (`grep -n "CONCEPT_GRAPH\|const RUNTIME" scripts/cortex.mjs`; `setpiece.mjs` is the graph's only reader). It also APPENDS to `brain_ledger.jsonl`, which is a deliberate shared append lane, not a breach — six organs append it and `brain.mjs` owns the schema only. Two other drifts: it serves `wake_queue.jsonl` now, not just `wake.json`, and it holds a singleton lock on **:4112** — one below the thalamus (`grep -n "4112" scripts/cortex.mjs`), a port this doc never named.) |
| `dressing-room/state/thalamus_config.json` | Canon: salience weights, tier thresholds (τ0≈0.25, τ1≈0.55, ε≈0.08), binding window, refractory, budget-coupling constant `k`. Approval-gated (curriculum-shaping). | — (config) — **(corrected 10 Aug 2026: "— (config)" meant "no owner script", and that was TRUE until 9 Aug 2026 and is FALSE now.** `gate_tune.mjs` is the DECLARED OWNER of this file's `tiers` block and really writes it: `grep -n "thalamus_config" scripts/gate_tune.mjs` → *"this file is the DECLARED OWNER of thalamus_config.json's `tiers` edits"*, `writeAtomic(CONFIG(), …)` on the apply and revert paths. It is still approval-gated — every apply rides a card the captain answered, the old tiers freeze into `_gate_tune_legacy["tiers@<date>"]`, and an out-of-band mutation AUTO-REVERTS. `gate_tune.mjs` is also sole writer of `gate_tune_ledger.jsonl`. **The threshold numbers in this cell are ALSO stale — see the τ note under §4.4.** The file itself is TRACKED, not gitignored.) |
| `dressing-room/state/workspace.json` | The **global workspace** — the current broadcast: one bound "moment," version-stamped so every region detects a new one. | thalamus |
| `dressing-room/state/afferent.jsonl` | Raw afferent event log — **gitignored** (carries transcripts + frame hashes). | thalamus |
| `dressing-room/state/salience_ledger.jsonl` | Every gate decision (tier, S, component breakdown, outcome) → nightly self-tuning + audit. | thalamus |
| `dressing-room/state/wake.json` | The TIER-2 handoff contract to `cortex.mjs`. Consumed-on-success like `brain_queue.triggers`. | thalamus |
| `dressing-room/state/wake_queue.jsonl` | **(added 10 Aug 2026 — this row was MISSING and the omission mattered.)** M14 THE OVERLAP: wakes now QUEUE instead of clobbering, so a second surprise during a 20s Opus read is no longer lost. `wake.json` demoted to the latest-wake mirror / legacy single-slot fallback. Evidence: `grep -n "M14" scripts/thalamus.mjs scripts/cortex.mjs`. Gitignored. | thalamus |
| `dressing-room/state/bg_queue.jsonl` | **(added 10 Aug 2026 — missing row.)** M22: suppress the WAKE, never the THOUGHT — a gated near-miss still lands here and can resurface later as a "second spotlight" hint in the live talk. Evidence: `grep -n "BGQUEUE\|bg-drained" scripts/thalamus.mjs`. Gitignored. | thalamus |

### 4.2 The afferent spine — every sense lands in ONE nucleus

> **(corrected 10 Aug 2026 — all three nerves are WIRED, and there are more than three now.** The
> nucleus really does expose `POST /afferent` (`grep -n '"/afferent"' scripts/thalamus.mjs`), but
> the browser never talks to it directly: the Dugout page posts to its OWN `/afferent-relay`
> endpoint and the Dugout server forwards — so `grep -n "afferent-relay" scripts/dugout.mjs` is
> where the voice and vision nerves actually live, and a search for `/afferent` in the page finds
> nothing. Nerve 3 is real too: `grep -n "watch(STATE_DIR" scripts/thalamus.mjs` shows a debounced
> `fs.watch` over the whole state dir. Beyond the three designed here, the spine has since grown
> a TALK-mode nerve (`grep -n "/afferent" scripts/talk.mjs` — posts to `ARSENAL_THALAMUS` or
> :4113), the Watcher tank's own observation nerve, and the Gemini-study harvest lane. **Also
> note the "voice" nerve is no longer what selects SELF — provenance is; see §4.3's correction.**)

Three nerves POST to `:4113/afferent` (append to `afferent.jsonl` + in-memory ring):
1. **Voice/language** — `dugout.mjs`/`talk.mjs` POST every user turn *before* answering: `{modality:"voice", text, concept_tokens[], confidence_word(knew|shaky|guessed — already in the rep ontology)}`.
2. **Vision** — the Dugout page already emits `realtimeInput.video` JPEG frames; it also POSTs each frame's **64-bit perceptual hash**. Salience of a frame = Hamming distance from the last one. **Static screen = distance ~0 = filtered at the door, free** (no region, no tokens). Only a *changed* surface carries visual salience. Raw pixels never persist — hash + caption request only.
3. **Bus deltas** — thalamus `fs.watch`es `dressing-room/state/*.json` (event-driven, near-zero cost). Each write emits a delta: a new rep, a **Governor state-change** (`readiness.json` GREEN→AMBER→RED), an FSRS card going due, a Twin market resolving. This is how a *machine* event enters the same relay as a spoken word.

### 4.3 The reticular gate — salience math (deterministic, microseconds, zero-LLM)
The gate guarding the expensive brains must itself be near-free:
```
S = clamp01( wpe·PE + wnov·NOV + wgov·GOV + werr·ERR + wself·SELF + wdead·DEAD − whab·HAB )
```
- **PE — prediction error from the Twin (the crux).** For any event the Twin has a market on, surprise = Shannon surprisal `-log2(p_model(observed))`, normalized. A predicted event is boring; an event the Twin bet AGAINST that just happened is maximal. No market → Laplace base-rate table in config.
- **NOV** — unseen concept / first-time `confused_with` pair (both in the capture contract). **GOV** — magnitude of a Governor transition (reads `readiness.json` only — biometrics *weight attention*, never *drive a verdict*; clamp intact). **ERR** — a `confidence:"knew"` rep that came back `correct:false` (the calibration break — most teachable instant). **SELF** — the captain names a doubt / "I don't get X" (matched vs `doubt_grammar.json`/`lexicon.json`). **(corrected 10 Aug 2026: the SIGNAL held, both named sources are wrong, and the GATE was rebuilt on 4 Aug.** SELF matches against `thalamus_config.json`'s own `self_markers` list — a bilingual phrase list including Devanagari — not against `doubt_grammar.json` or `lexicon.json` (`lexicon.json` is `doubtminer.mjs`'s file and the nucleus never reads it): `grep -n "self_markers" scripts/thalamus.mjs`. More importantly the gate is now **PROVENANCE-based, not modality-based** — `cfg.self_sources` = his mic, his Claude Code prompts, his MCP notes, his Gemini study turns — and it explicitly DENIES `claude-code-teaching`, because that stream is the machine's own answers, not him. The scar is worth reading in place: `grep -n "#1  SELF fired on" scripts/thalamus.mjs` — SELF fired on voice only, voice had been silent since 30 Jul, and 3,099 `code:` moments scored a maximum S of 0.000.) **DEAD** — a due card / staged scrimmage (time-pressure as salience — voicing still obeys the humane clamp). **HAB** — per `(modality, signal_key)` the same surprise decays exponentially under a refractory window — a flapping Governor or repeated frame **cannot re-fire the deep brain.** This is what stops a whip.
- **EXCLUDED from S by construction: prosody, tone, emotion, agitation.** Hard law — affect never feeds a score.

### 4.4 The escalation ladder (the token-rationing heart)

> **(corrected 10 Aug 2026 — THE LADDER'S SHAPE HELD EXACTLY; ITS NUMBERS ALL MOVED.)** The four
> rungs, the ε-band adjudication and the budget coupling are all in the running code — the daemon
> literally prints the formula on boot (`grep -n "relay nucleus LIVE" scripts/thalamus.mjs`). But
> **every τ/ε/k written into this doc is a 12 Jul design guess and none of them is the live value.**
> Measured 10 Aug 2026 from `dressing-room/state/thalamus_config.json`: `tau0 0.25` (design said
> ≈0.25 — held), **`tau1_base 0.4` (design said ≈0.55)**, **`epsilon 0.1` (design said ≈0.08)**,
> `budget_k 0.35`, `binding_ms 900`, `refractory_min 15`, `wake_cap_per_day 15`. These are TUNED
> values — `gate_tune.mjs` moves them on the captain's word (see the §4.1 row), so any number
> copied into prose is wrong by the next apply. **Read them live, always:**
> `node -e "console.log(JSON.stringify(require('./dressing-room/state/thalamus_config.json').tiers))"`
> — or just start the daemon and read its boot line.

```
S < τ0        → TIER-0  reflex handles instantly, free (Gemini Live). Still bound + broadcast.
τ0 ≤ S < τ1   → TIER-1  route to a parallel Gemini region for enrichment. Free.
S ≥ τ1  AND budget-ok AND not-refractory  → TIER-2  WAKE OPUS (cortex.mjs).
|S − τ1| < ε  → the ONLY sub-Opus paid thought: one Gemini Flash-Lite adjudication ("genuine reasoning-hard moment? y/n").
```
- **Budget coupling (rations Opus against the REAL ledger):** `τ1_effective = τ1_base + k·(1 − window_headroom_frac)`, where `window_headroom_frac` comes straight from `brain.mjs`'s budget accounting (the same `observed_window_ceiling` the P0 fix just guarded). When the Claude window nears empty, the wake threshold **rises automatically** — the last tokens spent only on the day's sharpest surprises. Plus a hard daily `wake_cap` in `salience_ledger.jsonl`. Rough magnitude: ~15 deep calls/day × ~40k tokens ≈ 600k ≈ inside one 800k window ≈ **15-25× collapse vs naive "everything on Opus."**
  **(corrected 10 Aug 2026: the two INPUTS to this arithmetic held; the WINDOW SIZE it divides into did not.)** `wake_cap_per_day: 15` is real (`thalamus_config.json`) and `est_tokens_per_wake: 40000` is real (`grep -n "est_tokens_per_wake" scripts/cortex.mjs`), so ~600k/day still stands. But **the window is not 800k** — `brain_config.json` carries `budget.window_capacity_est_tokens = 1600000`, and `node scripts/brain.mjs status` reports the *observed* ceiling (1,600,000 on 10 Aug, sourced `observed`, not estimate). The headroom is roughly double what this line assumed. Read the real one: `node scripts/brain.mjs status`. The savings multiplier is also measured now rather than guessed — `node scripts/fuelboard.mjs` prints the `naive-Opus shadow` line (tokens avoided vs free units actually spent).

### 4.5 Temporal binding — fusing voice+vision+bus into one moment
The nucleus holds a **binding window B ≈ 900ms**. Afferent events inside B that are referentially linked (co-temporal voice+frame, or sharing a `concept_token`) fuse into one `moment{moment_id, modalities[], spotlight, context[]}`. **Winner-take-all:** highest-S event is the spotlight; the rest attach as bound context. "He says *'wait, why does attention scale like this'* while a softmax diagram is on screen and an FSRS card on that exact concept just went due" = **one moment**, not three. **The broadcast IS the write:** thalamus writes the bound moment to `workspace.json` atomically, version-stamped; every region subscribes by watching the version bump. That shared file is the global-workspace substrate the organism currently lacks.

### 4.6 The async arc — the deep answer flows into the live talk
1. TIER-2 fires → thalamus writes `wake.json {moment_id, spotlight, bound_context, deadline_ms}`.
2. `cortex.mjs` (watching) spawns `claude -p --model opus` **extended thinking**, feeding the bound moment + relevant bus slice (twin, slip, learning_state, the capsule). POSTs answer to `:4113/deep-answer`; thalamus folds it into `workspace.json.deep`.
3. Meanwhile the reflex gave a **holding token** in the Gaffer's voice ("*ruko — isko theek se sochta hoon*"). At its next turn boundary the Dugout reads `workspace.json`, sees a `deep` answer for the live `moment_id`, and **injects it via `realtimeInput.text`** (the scar-table-verified runtime path) so Charon speaks Opus's reasoning *in the Gaffer's voice, mid-conversation.* (Note: `3.1-flash-live` is sequential-only on function calls — the async defer happens out-of-band through the thalamus + session resumption keeps the socket alive across the ~10-20s Opus round-trip.)

### 4.7 Selftest bar (unrun = hypothesis)

> **(corrected 10 Aug 2026 — this section is written as a BAR TO MEET; it has been MET since 14 Jul.)**
> `node scripts/thalamus.mjs selftest` runs green today and the six assertions below are all in it
> by name (`grep -n "AFFECT FIREWALL\|refractory\|moment_id\|adjudicat" scripts/thalamus.mjs`).
> The suite has grown far past these six — run it, don't count it from here. Read the phrasing
> below as the ORIGINAL acceptance bar, not as outstanding work.

`thalamus.mjs selftest` must assert deterministically with injected afferents: (1) a low-`p` Twin event outscores a predicted one; (2) a repeated event is refractory-suppressed (no double-wake); (3) `τ1_effective` rises as `window_headroom_frac`→0; (4) a voice+frame+bus triple inside B fuses to one `moment_id`; (5) prosody/emotion fields are ignored; (6) the ambiguous band calls the tiny model at most once. Then a live smoke: a genuine surprise reaching `wake.json`, the answer landing in `workspace.json.deep`.

---

## 5. THE SEVEN TANKS — seven parallel minds, each a DIFFERENT model

> **7 Aug 2026, on the live board:** the registry now carries an EIGHTH row — **T8 Distiller**
> (working-set, `gemini-flash-latest`, key 6), added by the working-memory build and finally
> wired to bill on 7 Aug (until then distiller's daily flash spend was invisible to the gauge).
> The SEVEN below remain this design's seven cyborg *sense* regions, unchanged; T8 is a
> metering lane beside them, so the fuel gauge (§ "fuelboard" below) draws **8 bars**, not 7.

**The technical jewel:** the marquee senses are **mutually exclusive on one model.** The seeing/teaching Gaffer (`3.1-flash-live`) *cannot* do affective/proactive audio or `NON_BLOCKING` async calls — those are `gemini-2.5-flash-native-audio` + `v1alpha` only. On one socket you get one or the other. **Rate limits are per-PROJECT** (not per-key), so 7 accounts = 7 independent quota pools, each running a *different model simultaneously.* Mutual exclusion dissolves into division of labor.

> **(corrected 10 Aug 2026 — the table below is the DESIGN cast list; the live registry is code.)**
> The eight tanks, their regions, their key slots and their models now live in exactly two places
> that agree with each other: `DEFAULT_TANKS` in `scripts/fuelboard.mjs` and the canon override
> `dressing-room/state/fuelboard_config.json`. **Read the cast from there, and the live state
> from `node scripts/fuelboard.mjs`** — never from this table. Per-row drifts are annotated below.

| Tank | Account | Region | Model | Job |
|---|---|---|---|---|
| **T1 Gaffer** | free key | language/mouth | `3.1-flash-live` (Charon) | voice + **code execution** + **search grounding** + thinking. The only mouth. **(corrected 10 Aug 2026: model held — `gemini-3.1-flash-live-preview` is still `DEFAULT_MODEL` in `dugout.mjs`, pinned by selftest. TWO of the three extras did not survive the wire: "code execution" is NOT `{codeExecution:{}}` on this socket (it hangs the turn) — the Chalkboard is a `run_python` TOOL on the REST sandbox; and "search grounding" is ABSENT, zero free quota, asserted absent by selftest. See §5.2.)** |
| **T2 Watcher** | free key | vision | `3.1-flash-live` vision-only | continuous screen/paper → fires `spinning`/`stuck-30s`/`wrong-answer-forming` at the thalamus. Never speaks. **(re-verified 10 Aug 2026 — BUILT and accurate. `grep -n "watcherConnect\|WATCHER_INSTRUCTION" scripts/dugout.mjs` shows a genuine second WebSocket on its own key slot, whose constitution names exactly SPINNING / STUCK / WRONG-ANSWER-FORMING, and whose audio is never played. Two details this row does not carry: it takes every SECOND frame (half cadence), and it ships LOW media-resolution by default with a scar-arm that retries full-res on an early 1007/1011.)** |
| **T3 Cochlea** | free key | affective ears | `2.5-native-audio` v1alpha, affective ON, modalities muted | hears tone/stress, continuous-transcribes → **affect firewall** (§5.1). **(corrected 10 Aug 2026: THIS TANK SHIPS DISABLED, BY LAW — the row reads as if it were running.** `fuelboard_config.json` carries `"enabled": false` for T3 and states why: *"a preview affective surface whose only legal output is a mouth-timing hint … flip enabled when the captain wants the empirical probe."* `node scripts/fuelboard.mjs` shows it `DEAD`, and the Dugout's own self-description calls it *"affective ears — DISABLED by law, the affect firewall"*. The firewall itself IS live and does not depend on this tank.) |
| **T4 Bridge/Deep** | (routes to `cortex.mjs`/Opus) | prefrontal | Opus 4.8 extended | the deep judgment, woken by the thalamus. **(corrected 10 Aug 2026: the ROUTE held; the version string "4.8" appears nowhere in the code and never did.** `cortex.mjs` spawns `claude -p --output-format json --model opus` — the unpinned **alias**, so the deep brain follows whatever `opus` resolves to on the Max plan, not a frozen version. "Extended thinking" is real and is set by env: `MAX_THINKING_TOKENS`, `max_thinking_tokens: 16000` by default and raised for the deep overnight reads. In the fuel registry this tank carries `model: "opus-via-cortex"`, `key_index: null` — it has no Gemini key; its budget IS the Max window.) |
| **T5 Scout** | **Pro account** | research | Gemini 3.x Pro, 1M ctx | Deep Research, whole-season/repo synthesis, NotebookLM, out-of-band. **(NOT FULLY VERIFIED 10 Aug 2026 — the repo says two different things and neither is provably wrong.** `fuelboard_config.json` gives T5 `key_index: 3` with `model: "gemini-3.1-pro-preview"` (i.e. an API lane), while the Dugout's own self-description calls T5 *"research — a HUMAN Pro surface, no API"*. Both can be true at once — a metering row for an API lane plus a human Deep-Research surface he drives by hand through `/fire` — but which one actually carries the research today is a claim this pass could not settle from code. Treat as a claim; the captain's word decides.) |
| **T6 Hippocampus** | free key | memory | `gemini-embedding-001` | embed/recall (already partly real, `dugout.mjs:279`). **(corrected 10 Aug 2026: model held, "partly real" is now fully real, LINE NUMBER rotted — never cite one here.** Grep it: `grep -n "gemini-embedding-001" scripts/dugout.mjs scripts/hippocampus.mjs` — both organs default to it, and both carry the same scar comment that `text-embedding-004` went 404 on v1beta. The recall lane is no longer partial: see §6, all five layers built.) |
| **T7 DMN / failover** | **Pro account** | default-mode | Gemini Pro / any | dreams when he's away (§6.3); absorbs any tank that trips so the conversation never dies. **(corrected 10 Aug 2026: the JOB held exactly; the account/model line did not.** In the live registry T7 is a **free key slot** (`key_index: 5`) on `gemini-flash-latest`, not a Pro account. The failover role is real and is code, not intent: `pickTank()` prefers the pinned tank, then T7, then any idle HOT, and **never** borrows T1/T2 mid-conversation. Also: **"(§6.3)" is a dangling cross-reference — §6 has no subsections. The DMN is §7a.**) |
| **T8 Distiller** | free key | working-set | `gemini-flash-latest` | **(added 10 Aug 2026 — the eighth row this table never grew, though the note above it announced T8 on 7 Aug.** Registry: `key_index: 6`, enabled. It is a metering lane for the working-memory distiller beside the seven sense regions, which is why the gauge draws eight bars. Evidence: `fuelboard_config.json`, and `grep -n "T8" scripts/fuelboard.mjs`.) |

**Concurrency is real:** T1 talks *while* T2 watches the same screen *while* T3 hears the sigh *while* T7 pre-drafts — four sockets on four accounts, gated by one thalamus. Needs a **per-tank credential/project router** (the key-list exists; the router does not).
  **(corrected 10 Aug 2026 — "the router does not [exist]" is FALSE; it shipped as M3.** The router is `pickTank()` + `recordUse()` in `fuelboard.mjs`, backed by `tanks.json`; the Dugout hands the Watcher its own `key_index` out of the same numbered pool and reports usage back through `/tank-use`. Evidence: `grep -n "function pickTank\|/tank-use" scripts/fuelboard.mjs scripts/dugout.mjs`. **One clause of this sentence is still hypothesis, though, and it is the interesting one:** the four-way *simultaneous* picture cannot be true as written today, because T3 ships disabled — the live concurrency is T1 + T2 + the background lanes, not T1+T2+T3+T7.)

### 5.1 EARS at ceiling — the Cochlea + the AFFECT FIREWALL
**(corrected 10 Aug 2026 — the FIREWALL IS BUILT; its function name is not `affectFirewall()`.)** Grep for the real thing: `grep -n "AFFECT_FIELDS\|function stripAffect\|sanitizeAfferent" scripts/thalamus.mjs`. It is stronger than this section describes — rather than converting affect into a hint and discarding it, `stripAffect()` **deletes the fields recursively at the door**, before an event even lands in `afferent.jsonl`, over a closed list (`prosody, emotion, tone, affect, stress, agitation, mood, sentiment`). The selftest proves it two ways: an event carrying `prosody/emotion/tone` scores byte-identically to a clean one, and the affect words never appear in the log at all — including nested inside `asr.analysis`, which a top-level-only delete loop used to let through. Note the honest consequence: with T3 shipped disabled (see the table), the "timing/softening hint for the mouth" half of this design has no live source.

Native-audio affective dialog hears tone/stress. **The law forces a firewall:** a new `affectFirewall()` in `thalamus.mjs` converts "sounds flat/strained" into *at most* a timing/softening hint for the mouth ("offer the floor-touch now, gently") and then **discards it.** It is structurally forbidden from writing any `.json` the Governor/Twin/genome read. Affect can change *when* and *how warmly* the Gaffer speaks — **never a number.** Declared-surface only: attaches to the *same* getUserMedia session he already opens — never a new ambient mic.

### 5.2 MOUTH at ceiling — three organs the Gaffer is missing
> **(corrected 10 Aug 2026 — all three bullets below SHIPPED as M4, and all three shipped DIFFERENTLY
> from the design. This is the section the wire rewrote; the BUILD STATUS block at the top of this
> file already flagged it, but the bullets themselves were never updated.)** Verify with one grep:
> `grep -n "M4 SCARS\|run_python\|googleSearch\|thinkingLevel" scripts/dugout.mjs`.

- **THE CHALKBOARD — code execution** (`{codeExecution:{}}` in the tools array, `dugout.mjs:564`): **(corrected 10 Aug 2026: WRONG MECHANISM and a rotted line number — do not build from this parenthesis.** `{codeExecution:{}}` on the LIVE socket **hangs the turn** (probed live 14 Jul 2026), so the Chalkboard is instead a `run_python` **function tool** answered out-of-band by a REST sandbox call. Two selftests hold the line: *"SCAR: NO codeExecution on the live socket (it hangs the turn — probed)"* and *"CHALKBOARD: run_python is a club tool"*. The capability the bullet promises is real; the wire shape is not.)** the Gaffer **runs Python live** to *prove* an answer or execute his own code mid-drill and read the real output. For an AI-PE candidate this is the single highest-signal capability — "don't trust me, watch it run." Wire into the scrimmage; reps flow to `scorer.mjs` like any market. Grades the **code, never the coder — win-only on the result, miss resolves silently into FSRS weight.
- **THE SCOUT'S EARPIECE — search grounding** (`{googleSearch:{}}`): current real-world facts mid-sentence with citations. **Firewalled to factual queries only — never his private state** (no personal string ever enters a search query). **(corrected 10 Aug 2026: THIS ONE DOES NOT EXIST AND WAS NOT BUILT.** `googleSearch` has **zero free quota in every shape** on this account — probed on the wire — so grounding is honestly ABSENT rather than half-wired. The selftest asserts the absence deliberately: *"search grounding honestly ABSENT (zero free quota — the wire said so)"*, checking that no tool config contains `googleSearch`. The privacy firewall described here therefore guards a door that is not open. Do not plan on this capability without a paid lane the $100 law would have to bless first.)
- **Thinking before speech** — set `thinkingLevel` on 3.1, gated by turn difficulty via the thalamus (not globally — latency). **(corrected 10 Aug 2026: BUILT, but the gating clause is wrong — it IS global.** The page sends an explicit `thinkingLevel` on **every** session from `CFG.thinking`, not per-turn from the thalamus: selftest *"page ALWAYS sends an explicit thinkingLevel, scar-armed (C4)"*. The "scar-arm" is the part worth knowing and is nowhere in this design: after 4 early socket closes the page **strips `thinkingLevel` entirely** and lets the server default ride (`grep -n "live scar bit: explicit thinkingLevel stripped" scripts/dugout.mjs`). So the live behaviour is global-with-a-fallback, not thalamus-gated.)

### 5.3 EYES at ceiling — the Watcher tank + the video-cap dodge
A second `3.1-flash-live` session doing **nothing but vision**, streaming his declared screen/paper continuously, forming its own running observations (never pausing when the talk pauses). **Why it's legal on quota:** true audio+video is capped at **2 minutes** (video ~258 tok/s). The Watcher uses **frame-mode** — discrete `realtimeInput.video{jpeg}` chunks on an *audio-context* session (the shape `dugout.mjs:644` already proved) at 1 frame/1-2s — billed as audio (~25 tok/s), lives to the 15-min cap, then resumes. **Frame-mode is the whole reason continuous eyes are affordable.**
  **(corrected 10 Aug 2026: BUILT, mechanism intact, two details wrong.** (1) The line number rotted — grep the shape instead: `grep -n "realtimeInput:{video:{data:" scripts/dugout.mjs`, with a selftest pinning it (*"vision sends realtimeInput.video (probed live; mediaChunks deprecated)"* — the legacy chunk-array field is dead on the wire). (2) **The cadence is not 1 frame/1-2s.** The base capture interval is `frame_ms: 2000`, it is multiplied by the tone knob (`frame_ms_mult` — ×2.0 on a conserve day, ×0.75 when open), and **the Watcher only receives every second frame** (`frameN++%2===0`), so its real rate is ~1 frame/4s on a nominal day and ~1/8s on a RED one. It also ships `MEDIA_RESOLUTION_LOW` by default — cheaper frames, eyes on the desk longer — with a strip-scar that retries full-res if setup fails early.)

### 5.4 The two Live-session must-builds
Both currently absent and hard-capping every session at ~10-15 min: **session resumption** (tokens valid ~2h) + **context-window compression** (sliding window, e.g. trigger 25k/keep 8k). Everything "while you work" and the async arc depend on these.
**(corrected 10 Aug 2026 — "Both currently absent" is FALSE; both shipped as M0, the very first unit.** Session resumption: `grep -n "sessionResumption" scripts/dugout.mjs` — the page sends `sessionResumption:{handle}`, stores each `sessionResumptionUpdate.newHandle`, and a handle is bound to ONE model (a mismatch returns null rather than replaying into the wrong socket). Compression: the setup carries `contextWindowCompression:{triggerTokens:…, slidingWindow:{targetTokens:…}}` from `CFG.compression`, with a selftest asserting the explicit trigger + window are sent. Read the live trigger/target from `CFG.compression`, not from the "e.g. 25k/8k" guess in this sentence — it was an example, never a measurement.)

---

## 6. MEMORY — the five-layer hippocampus (answers "will memory be contained?")

**Yes — because containment is not the session's job.** The live window is deliberately lossy (sliding-window "compression amnesia" — a preference stated 40 min in is blurred by minute 90). The durable brain lives in a new gitignored `dressing-room/hippocampus/` dir, written *outside* the session. The session forgets freely; the organ remembers.

> **(corrected 10 Aug 2026 — THE ENTIRE "Status" COLUMN BELOW IS DEAD. Read none of it.)** Every
> "not built" / "partial" / "exists shallow" in this table describes 12 Jul. All five layers
> shipped as M2 (+ M10 for the scaling) and live in ONE organ: `scripts/hippocampus.mjs`, whose
> header names them L0–L4 by these exact names (`grep -n "L0 REHYDRATOR\|L1 THE SCRIBE\|L2 LEDGER OF SELF\|L3 CONSOLIDATOR\|L4 RECALL REFLEX" scripts/hippocampus.mjs`).
> The files exist on disk: `ls dressing-room/hippocampus/` → `episodes.jsonl · identity_facts.json ·
> identity_facts.pending.jsonl · recall_bumps.jsonl · recall_hints.jsonl · scribe_log.jsonl ·
> who_he_is.json`. The dir is gitignored exactly as this section demands (`.gitignore` line
> `dressing-room/hippocampus/`). The nightly organs are on the real schedule too —
> `ArsenalFC-Consolidate` 02:10, `ArsenalFC-HippoStore` 02:20, `ArsenalFC-HippoIndex` hourly.
> **The "missing organ" column is now a NAME INDEX, not a work list.** Per-row notes below.

| Layer | What | Status | The missing organ |
|---|---|---|---|
| **L0 Working** | the live Live-API session | exists, lossy | **The Rehydrator** — on handle-rotation/reload, prepend a non-spoken cartridge: last-N durable episodes + all identity facts + today's `who_he_is`. Generalizes `loadDayCartridge()` to mid-session. |
| **L1 Instant episodic** | salient moments written the MOMENT they happen | partial (`recall_index.jsonl` but BATCH) | **The Scribe** — a live `mark_moment(kind, text)` async tool (`kind∈{doubt,win,preference,thread}`); handler appends to `hippocampus/episodes.jsonl` + embeds on the dedicated Hippocampus key. Async → no audio stall. |
| **L2 Durable facts** | "remember I…" → always injected | not built | **The Ledger of Self** — `hippocampus/identity_facts.json`; `remember(fact)`/`forget(id)` tools (captain-gated, surfaced not silent); `buildSystemInstruction()` unconditionally injects the (tiny) full ledger every session. Distinct from episodes: facts are *always present*, episodes are *searched*. **(corrected 10 Aug 2026: BUILT — `rememberFact()` / `forgetFact()` are real (`grep -n "function rememberFact\|function forgetFact" scripts/hippocampus.mjs`), the ledger has a hard `FACTS_CAP` so it stays small enough to always be present, and the cartridge now DATES every fact and prints its age (a 17-Jul assertion used to read identically to today's at every consumer). A confirm door was added 9 Aug: `promote --at <ts>` turns a staged row into canon. ⚠ **ONE REAL CONFLICT, FLAGGED NOT FIXED:** the sibling file `identity_facts.pending.jsonl` has **TWO live writers** — `hippocampus.mjs` rewrites the whole file while `mcp-memory.mjs` appends to it, i.e. a rewrite racing an append. That breaches the single-writer law. It is recorded here as a conflict and left alone; **this one needs the captain's ruling on which organ owns the staging lane** — do not "fix" it from a doc.)** |
| **L3 Consolidation** | nightly "who he is right now" | exists shallow (`day_cartridge` + `buildFingerprint` = a config collage) | **The Consolidator** — nightly on **Gemini 1M (Pro, free)** → `hippocampus/who_he_is.json` (~1-2KB: `fingerprint`, `open_threads`, `recent_wins/cracks`, `voice_tuning`, `do_not`). Loads at the TOP of every session so it opens KNOWING him. Opus-extended reserved for the rare night the 1M lane flags a contradiction. Prosody NEVER enters it. **(corrected 10 Aug 2026: BUILT, with two honest differences.** (1) The lane is not a single "Gemini 1M (Pro)" call — it is a **model ladder** that falls back: `[HIPPO_GEN_MODEL, "gemini-3.1-pro-preview", "gemini-flash-latest"]` (`grep -n "HIPPO_GEN_MODEL" scripts/hippocampus.mjs`), because bare `gemini-3.1-flash` is dead on REST and only `-latest` aliases survive churn. (2) The AI does not get the last word: a **STRICT schema validator + banned-phrase + narrator-voice check** accepts the proposal or the old file simply stands. Two scars are worth reading before trusting this layer: `who_he_is` **froze** once because its two material sources had gone dry (a third source, a learning-arc filter over `afferent.jsonl`, was added), and the cartridge used to assert "RIGHT NOW" over a file that could be days old — it now degrades by age, because age may only ever DEGRADE a claim, never lift one.) |
| **L4 Proactive recall** | memory surfaces when the topic does | not built (recall is pull-only) | **The Thalamic Recall Reflex** — per-turn: embed the turn → cosine vs `episodes.jsonl` + `who_he_is.open_threads` → if ≥0.55 inject an ephemeral non-spoken hint; the Gaffer weaves it only if it earns the turn (win-only, never "as you said Tuesday…" theatre). This IS the salience gate deciding when the Scribe fires. **(re-verified 10 Aug 2026: BUILT and the threshold is exactly right — `grep -n "RECALL_THRESHOLD" scripts/hippocampus.mjs` → `0.55`.** Two things the row does not carry: the reflex now **counts** its recalls through an append-only `recall_bumps.jsonl` folded in by the hourly `index` sweep (the FSRS stretch inside `memoryStrength` was dead wiring — nothing ever wrote anything but 0), and there is a second, **network-free, write-free** twin `recallReflexLexical()` cheap enough for a `UserPromptSubmit` hook, because the reflex used to be voice-only and voice had gone silent.) |

**The real ceiling to name — the vector store doesn't scale.** `recall_index.jsonl` is a flat file, linear-scanned per query — fine at a few thousand, slow at ~50-100k (a year of talk), and there's no forgetting so noise accumulates. **Fix (dependency-free, no external vector DB — that would break the local-only law):** nightly, (a) **shard episodes by month** + a curated "greatest-hits" hot shard; (b) **prune/merge** near-duplicates and drop low-salience old ones via an FSRS-style decay applied to memory itself — **biological forgetting, the feature not the bug**; (c) keep the hot working set in one small file. Recall stays O(recent).
**(corrected 10 Aug 2026: this "Fix" is written as future work and it SHIPPED as M10.** It is `hippocampus.mjs consolidate-store` — *"month-sharding + FSRS-style biological forgetting (prune to cold shards, never delete); recall stays O(recent)"* — and it runs nightly on the real schedule as `ArsenalFC-HippoStore` at 02:20. Note the one law the design did not state and the code chose: it **prunes to cold shards, never deletes**. Whether the ceiling this paragraph warns about has actually been reached is a separate, live question — measure it (`wc -l dressing-room/state/recall_index.jsonl dressing-room/hippocampus/episodes.jsonl`), don't assume it from the prose.)

**Continuity — the three survival paths:** page-reload → resumption restores the compressed window + Rehydrator re-states detail = zero felt loss. New day → `who_he_is` + `identity_facts` load at kickoff = opens knowing him. Long continuous talk → compression evicts freely because L1 already captured the salient bits; the thalamus re-fetches on demand. **Cost:** embeddings free (dedicated tank), consolidation on Gemini 1M (free), search = local cosine (zero network), storage = gitignored JSONL. Inside the ceiling by construction. **Layering, not replacement:** `recall_index.jsonl`/`indexRecall()` stay verbatim as the batch back-fill floor; the Scribe/Reflex are the live layer on top.

---

## 7. THE EMERGENT CROWN — capabilities impossible on the old substrate

Each is impossible on the current serial/single-brain/cron substrate and trivial once you have parallel regions + a thalamus + a DMN + the two-speed brain.

> **(corrected 10 Aug 2026 — ALL SIX CROWN CAPABILITIES ARE BUILT. The section is written in the
> future tense throughout; read it as present.)** One command shows the organs exist and pass:
> `node scripts/organism_test.mjs suites` lists `dmn`, `presence`, `council`, `examiner` by name.
> The files: `dmn.mjs` (a) · `presence.mjs` (e) · `council.mjs` (b) · `examiner.mjs` (c/d) ·
> `dossier.json` written by `thalamus.mjs` (f). Three of them are on the live Windows schedule —
> `ArsenalFC-DMN` hourly, `ArsenalFC-Presence` every minute, `ArsenalFC-Examiner` 21:55. What is
> NOT yet true is the last mile of (e): the whisper is still **behind the teeth** because
> `wall_breaker` has not been ratified (`node scripts/shadow.mjs status` → `0/10 scorable · need
> ≥0.7 · training silently`). Sensing is live; speaking is earned and not yet earned. Per-bullet
> corrections below.

- **(a) THE DEFAULT MODE NETWORK — `dmn.mjs` ("The Rest Room").** Fires **during the day, when the captain is AWAY** (ActivityWatch `away` signal), using idle free tanks as scratch cortex. Pulls the Twin's weak-point vector (worst FSRS retrievability × lowest-confidence markets) and runs a **Monte-Carlo interview simulation** — 20-40 rollouts fanned across parallel regions, each a different interviewer persona, probing exactly his soft concepts. **(corrected 10 Aug 2026: BUILT, and both specifics here are understated.** The weak-point vector is not Twin-only — the live source is *"calibration danger zone + stalling concepts + lowest-confidence Twin markets"* (`grep -n "weak-point vector" scripts/dmn.mjs`). And the rollout count is no longer 20-40: **M16 THE DREAM STADIUM** replaced the original 8-serial-rollouts-on-one-tank engine with one that borrows EVERY idle tank and drains them in parallel, **up to ~100 rollouts/night** — the away-gate IS the tank-borrow gate, because with the captain gone there is no conversation for `pickTank`'s mid-talk T1/T2 clamp to protect. The old engine is frozen verbatim beside it as `dreamLegacy` (layering, never replace). It also added a **verification phase**: every cluster is attacked by a hostile counter-rollout and a "broken" reframe is DROPPED — better no ammunition than wrong ammunition.)** Scores where the simulated candidate stalls, and for each predicted stall **pre-drafts the exact 15-second reframe + next drill into `dmn_precache.json`. Output is INERT — it only loads ammunition; it never fires. Dreams only about his REAL weak points from real reps (never fabricated). May only spend a tank's measured headroom (`ceiling − used − reserve`) — converts use-it-or-lose-it free quota into consolidation; blast radius $0.
- **(e) PREDICTIVE PRESENCE — the fusion (DMN × Thalamus × Twin).** **(corrected 10 Aug 2026: BUILT as `presence.mjs`, and the SENSOR IS NOT THE TWIN.** The stall signature is measured by `presence.mjs` from **ActivityWatch's window watcher** — window-switch rate over the last 10 minutes, i.e. tab-thrash — and it fires ONE afferent at the thalamus (`stall:leading-edge` + a hint of what he was in). Zero LLM. It is sole writer of `presence_log.jsonl` (gitignored) and it **never voices anything**: on a `conserve` day it senses but stays off the wire, and the whisper still has to clear the earned-voice gate — which, as noted above, `wall_breaker` has not yet cleared. It also self-calibrates weekly (`ArsenalFC-PresenceFit`, Sundays 03:30). One scar sits inside it worth knowing before trusting the precache join: ~49 DMN rollouts a night produced ZERO whispers across 16 days because raw window-title words were being matched against canonical concept names — the join now goes through `capture.mjs`'s own canon resolvers, the same ones the thalamus imports, rather than a third private dialect.)** THE most transformative capability. The Twin flags a **stall-signature forming** (telemetry matches the leading edge of a pattern that historically precedes abandonment — tab-thrash + long dwell, the touchline's stuck-spinning). The thalamus queries `dmn_precache.json` — and the Rest Room **already drafted the intervention hours ago.** So the whisper is **instant, zero model latency** — it lands in the 3-second window where it can still catch him, instead of 20 seconds later when he's gone. **ADHD-PI doesn't fail from not-knowing; it fails in the gap between "stuck" and "gone." This is the only mechanism that closes that gap to zero. Still passes the earned-voice gate + RED clamp + win-only framing ("you were about to crack this, here's the handhold" — never "about to fail again").
- **(b) THE COUNCIL — `council.mjs` ("The Back Room").** When the reflex hits the Bridge, instead of one Opus call, fan the question to **three cheap regions in parallel with adversarial framings** — Steelman (best case), Prosecutor (hardest attack), Captain's-Own-Voice (how HE'd defend it, seeded from his FORGE capsules) — then their three drafts become context for **one** Opus-extended *integration* call that adjudicates. Cheap parallel breadth, expensive serial judgment. If the three split hard, the *disagreement itself* is shown as signal. **(corrected 10 Aug 2026: BUILT, and there is a FOURTH chair this design never had.** The three seats are exactly as written and are now canon in `council_config.json` (the baked three are the fallback). **M15 THE FULL SQUAD** added a **Cross-Examiner on a different model family** — Claude sonnet via `claude -p`, Max plan, HEADROOM-GATED so it only sits when the real window can spare it, with its spend riding `brain_ledger.jsonl` so the budget sees every token; chair fails or window low → the byte-identical 3-chair council. And cross-family disagreement is not merely "shown as signal": when the two families read the same question disjointly, `council_flag.json` is written — this organ's ONE file — and the set-piece coach compiles it into a defend drill. Disagreement-as-curriculum. Evidence: `grep -n "M15 — THE FULL SQUAD\|council_flag" scripts/council.mjs`.)
- **(c/d) THE LIVE EXAMINER — `examiner.mjs` (extends `nemesis.mjs`).** Code execution (§5.2) in the scrimmage → runs his code live against hidden tests. Reflex executes (fast/free), deep judges architecture. The build-it-live-under-a-stranger's-gaze rehearsal = highest-transfer drill for an AI-PE interview. **(corrected 10 Aug 2026: BUILT, staged nightly at 21:55, with three facts this bullet does not carry.** (1) **Staging is pure code — no LLM.** It reads `learning_state.json` (stalling/learning first), falls back to FSRS hardest-due, then to the capsule floor. (2) Its output file `examiner_drill.json` is **TRACKED, not gitignored** — the captain's 5 Aug 2026 ruling; the organ's own header notes it said "gitignored" until the 7 Aug audit caught the drift, so do not re-assert privacy here. (3) It has THREE template banks — mechanism / phenomenon / judgment — chosen by what KIND of thing the concept is, after audit #36 caught it rotating the mechanism bank unconditionally and staging *"Implement a MINIMAL working hallucinations from scratch"*, which is not a thing that can be done. The "code execution" it runs is the `run_python` REST Chalkboard, not the live-socket shape §5.2 originally specified.)
- **(f) THE LIVING DOSSIER — `dossier.json`.** A running belief-state updated INTRA-DAY by the thalamus from every salience event (a stall, a nailed drill, a fast recovery) — a live Bayesian posterior over the Twin's priors. Every region reads it. Medical clamp absolute: the intra-day capacity nudge can only ever *lower* demand, never raise RED→green; prosody never enters; local + gitignored. **(re-verified 10 Aug 2026: BUILT, and every clamp in this sentence is enforced by a named selftest, not by intent.** `dossier.json` is written by `thalamus.mjs` (`grep -n "writeDossier" scripts/thalamus.mjs`) and is gitignored. The asserts: *"errs and wins tracked per concept, intra-day"* · *"3 stalls → capacity nudge, and it can ONLY say 'lower'"* · *"built from counts alone — no affect field can exist in it"* · *"a new day RESETS the posterior"*. One thing this bullet does not say and should: **THE DOSSIER TAKES ONLY CANON** — a stall's raw window-title words ("google", "chrome") are refused as concepts, and only ids that resolve through `capture.mjs`'s registry land; a stall with unregistered hints still counts as a stall but creates no concept. Also note the reader list is smaller than "every region": `presence.mjs` is the one other organ that touches this ground.)

**The 3 most transformative for an ADHD-PI interview candidate:** (1) Predictive Presence (closes the stuck→gone gap), (2) The Rest Room (manufactures the reps he can't self-initiate), (3) The Live Examiner (rehearses the exact build-live stressor).

---

## 8. THE ECONOMY + NEUROMODULATION (how it stays cheap, light, law-clean)

- **Two currencies, never mixed:** Gemini tokens (abundant, free, per-account-per-day rate-limited) vs Opus judgment (scarce — the Max 5h rolling window + weekly cap that `brain.mjs` already models). The thalamus is the exchange desk.
- **`scripts/fuelboard.mjs` → `dressing-room/state/tanks.json`** — the account-allocation ledger making "7 parallel regions" real. Per tank: `region` (pinned), `quota_est`/`quota_used_today` (measured, reset on the account's local-midnight), `observed_ceiling` (**inherits the exact starvation-guard: `Math.max(est, observed)`** — a 429 at low usage must not strand a region at ceiling=1), `state (HOT|WARM|COLD|DEAD)`. `pickTank(region, state)` prefers the pinned tank, borrows T7 then any idle HOT — **never borrows T1/T2 mid-conversation** (user-visible stall). The **fuel gauge** (7 bars in the Dugout) = the organism's interoception.
  **(corrected 10 Aug 2026 — BUILT, and this is the paragraph with the most small drifts in the file. The SEMANTICS all held; three surface details did not.)**
  · **Field name:** the runtime field is **`used_today`**, not `quota_used_today` (`node -e "console.log(Object.keys(require('./dressing-room/state/tanks.json').tanks.T1))"` → `day, used_today, faults_today, observed_ceiling, last_429, state`). `quota_est` is not in the runtime file at all — it lives in the registry (`fuelboard_config.json`), which is the right split.
  · **Signature:** it is **`pickTank(region, { inConversation, board }, deps)`**, not `pickTank(region, state)` — the second argument is an options object, and `inConversation` is what arms the T1/T2 clamp. Grep it: `grep -n "function pickTank" scripts/fuelboard.mjs`.
  · **"7 bars" is wrong and contradicts this file's own §5 note:** the gauge draws **one bar per registered tank**, which is 8 since T8 Distiller joined. Run it and count: `node scripts/fuelboard.mjs`. (`fuelboard.mjs`'s own MODES comment still says "the 7-bar fuel gauge" — that is a code comment carrying the same stale number, not evidence.)
  Everything else in the sentence verified true: the starvation guard is `Math.max(quota_est, used_today)` ported verbatim; the day rolls on **local** date; the borrow order is pinned → T7 → any idle HOT with T1/T2 excluded mid-talk, each asserted by name in the selftest.
- **Neuromodulation — `scripts/tone.mjs` → `tone.json`** — one scalar `arousal ∈ {conserve, nominal, open}` derived ONLY from the already-computed Governor verdict (never biometrics directly — clamp). Every organ reads it as a multiplier. RED (`conserve`): dampens the reflex (shorter turns, lower frame rate), **mutes the DMN** (no dreaming — a depleted captain rests), **raises the thalamus escalation threshold** (conserve the window). GREEN (`open`): the inverse. One knob, whole-brain effect — that's what makes it neuromodulation. Stale Governor → default `nominal`, not `open` (fail toward conserve).
  **(re-verified 10 Aug 2026 — BUILT exactly as designed; every clause above held on inspection, which is why nothing here is struck out.** It runs every 5 minutes as `ArsenalFC-Tone`. The live effect table is `conserve {tau1_bump +0.10, frame_ms_mult 2.0, dmn_allowed false}` · `nominal {0, 1.0, true}` · `open {−0.03, 0.75, true}` — read it live rather than from here: `cat dressing-room/state/tone.json`. Two scars the design did not anticipate and the code now carries: a **stale RED used to be PROMOTED to nominal** — un-muting the DMN on the captain's worst day — so the conserve verdicts are decided FIRST and age may only ever DEGRADE a verdict, never lift one; and an **unreadable timestamp used to count as fresh**, so an unparseable stamp is now UNKNOWN AGE and can never licence `open`. Evidence: `grep -n "fail toward rest\|only ever DEGRADE" scripts/tone.mjs`.)
- **Degradation guards:** a tank dies → failover to T7. All free tanks cold → drop vision → drop to text → **hard stop before touching Opus for reflex** (a quota outage must never silently drain the Max window). Opus unavailable → route the escalation to Gemini Pro 1M with `provenance:gemini-fallback` marked on the bus (never drop it silently). DMN runaway → bounded to $0 + drafts-only by construction.
- **Add a `naive_shadow` counter** to the ledger: token-count each turn to estimate "what this would have cost on Opus," so the fuel gauge shows the captain the real multiplier the thalamus saves (`count_tokens` is free). **(corrected 10 Aug 2026: this is written as a TODO and it is DONE.** `tanks.json` carries `naive_shadow_tokens` and `actual_units` at the top level, and `node scripts/fuelboard.mjs` prints the comparison as its last line (`naive-Opus shadow: ~<n>k tokens avoided (<m> free units spent instead)`). Callers report the naive figure through `recordUse(id, units, naive_tokens)`. Read the multiplier from the gauge; never quote it from a document — it moves every time a tank is used.)

---

## 9. THE KENNEL — the night-shift body (§2's build detail)

> **(corrected 10 Aug 2026 — READ THIS BEFORE PLANNING ANY OF §9. THE CODE IS BUILT; THE BODY IS NOT.)**
> This is the one section where "built" and "deployed" genuinely come apart, and conflating them
> would cause real wrong action. `groundsman.mjs` and `awayday.mjs` both exist, both selftest green,
> both sit in the organism suite. But **there is no Pi.** Evidence: `dressing-room/state/bus_lease.json`
> **does not exist on disk** — no node has ever taken the lease, so the two-node arbitration this
> section describes has never once run. What IS live is a laptop-only lane: the schedule carries
> `ArsenalFC-Groundsman-Push` daily at 03:45, which runs `groundsman.mjs push` — a commit+push of
> public-safe outputs from the laptop itself, not a night-shift on other hardware. The §2 fix
> ("the main laptop becomes optional to leave on") is therefore **still unrealised**, and the 48h
> OAuth smoke-test below is **still unrun**. Treat everything in §9 as ready-to-deploy, not deployed.

- **`scripts/groundsman.mjs`** on a Pi Zero 2 W (or dead Android + Termux): loop → `git pull` the bus → `brain.mjs tick` → `git push` **public-safe outputs only** → private state on an encrypted local volume never pushed.
- **`dressing-room/state/bus_lease.json`** — the single-writer law across two nodes. Heartbeat lease: laptop present → laptop holds it, Kennel idles; laptop dark > TTL → Kennel takes it and drains the overnight queue. **Load-bearing** — without it, laptop + Pi double-write the ledger and corrupt the self-tuning budget. Add a `host_id`/lease field to `brain_config.json`.
  **(corrected 10 Aug 2026: the lease is IMPLEMENTED, the file does NOT EXIST YET, and the final sentence's plan was superseded.** The shape shipped as `{holder, host_id, ts, ttl}` inside `bus_lease.json` itself — `host_id` did **not** go into `brain_config.json` (`grep -n "host_id" scripts/groundsman.mjs dressing-room/state/brain_config.json`). Modes are `heartbeat` (laptop, scheduled) / `night --host <id>` (the Kennel loop) / `status`. Today the file is **LOCAL and gitignored**, and the organ's own header spells out the three steps the day the Pi arrives: remove it from `.gitignore` so the lease travels the git bus, schedule `heartbeat` beside a push, and **raise the TTL to 90 min** — the 20-min default fits same-disk arbitration only and would be wrong across a git-carried transport. Priority order is code, not intent: the laptop always wins, the Kennel may take the lease only past TTL, and the laptop reclaims silently on wake.)
- **THE AWAY-DAY RUNNER** — a GitHub Actions cron on the PUBLIC repo for genuinely public-safe deterministic chores only (selftest CI, `repo_bundle.mjs`, wall regen). Guard with a **`public_safe:true` manifest flag per job** — the workflow REFUSES any job lacking it, so nothing touching biometrics/transcripts can ever leak into a cloud runner. A CI lane, NOT a home for the brain (Actions can't hold a long-lived Claude OAuth session). **(corrected 10 Aug 2026: BUILT, and the two things this bullet does not name are exactly the two you need to find it.** The runner is `scripts/awayday.mjs` (`run` · `list` · `selftest`), the manifest is **`ci_manifest.json` at the repo root** (not a file under `dressing-room/`), and the workflow is **`.github/workflows/awayday.yml`** — the only workflow in the repo. The refusal is real code, not a convention: `grep -n "public_safe" scripts/awayday.mjs` → any job without `public_safe === true` fails the whole run, loudly, with the offending job named. The selftest drives a "sneaky" manifest with an unflagged `biometric-sync` job to prove it. The design said "wall regen" among the chores; the shipped scope is narrower — *"selftest CI, bundle regen"*.)
- **Smoke-test first:** `claude -p` under tmux on the Pi for 48h — verify headless OAuth persistence before trusting the Kennel with the window. Fallback: Kennel runs only deterministic organs + git bus (no LLM auth needed).

---

## 10. THE BUILD ORDER (the mutation path — each unit: selftest + real-run + commit)

Dependency-driven. The thalamus is the spine; memory and tanks hang off it.

> **(corrected 10 Aug 2026 — M0 THROUGH M10 ARE ALL SHIPPED. This is a HISTORY, not a worklist.)**
> Do not pick a unit off this list and build it; it exists. One check settles the whole section:
> `ls scripts/{thalamus,cortex,hippocampus,fuelboard,tone,dmn,presence,council,examiner,groundsman,awayday}.mjs`
> returns 11 of 11, and `node scripts/organism_test.mjs suites` runs every one of them by name.
> Unit-by-unit, with where the proof lives: **M0** session resumption + compression in `dugout.mjs`
> (§5.4's correction) · **M1** `thalamus.mjs` + `cortex.mjs` + config + the state files, and the
> afferent POST and async arc are both wired (`grep -n "/afferent-relay\|DEEP THOUGHT arrived" scripts/dugout.mjs`)
> · **M2 + M10** all five memory layers plus `consolidate-store` in `hippocampus.mjs` (§6's correction)
> · **M3** `fuelboard.mjs` + `tanks.json` + `pickTank`, and the Watcher really is a second socket
> · **M4** the Chalkboard as `run_python`, grounding honestly absent, `thinkingLevel` global
> (§5.2's corrections — M4 is the unit that shipped LEAST like its spec) · **M5** `tone.mjs`
> · **M6** `dmn.mjs`, since extended by M16 · **M7** `presence.mjs` — sensing live, the whisper
> still unratified · **M8** `council.mjs`, since extended by M15, and the dossier · **M9** built
> but NOT deployed, see §9's block. **The organism has since grown units this list never
> anticipated (M14 wake-queue, M15 full squad, M16 dream stadium, M17, M22 bg-queue). This
> section stops at M10 and is not a map of what exists.**

- **M0 — Live-session must-builds** (unblocks everything "while you work"): session resumption + context-window compression in `dugout.mjs`. *(Small, isolated, do first.)*
- **M1 — THE THALAMUS** (`thalamus.mjs` + `cortex.mjs` + config + the 4 state files). The keystone. §4. Selftest bar in §4.7. Wire the afferent POST from `dugout.mjs`; wire the async arc back.
- **M2 — MEMORY the live layer** — the Scribe (`mark_moment`), the Ledger of Self (`remember`/`forget` + unconditional inject), the Rehydrator, the Thalamic Recall Reflex, the Consolidator (`who_he_is.json` on Gemini 1M). §6.
- **M3 — THE TANKS as parallel regions** — `fuelboard.mjs` + `tanks.json` (port the starvation-guard) + the per-tank project router. Split the Watcher (T2) and Cochlea (T3) into their own sockets with the affect firewall. §5, §8.
- **M4 — THE MOUTH ceiling** — code execution (the Chalkboard, into the scrimmage/`examiner.mjs`) + search grounding (firewalled) + `thinkingLevel`. §5.2.
- **M5 — NEUROMODULATION** — `tone.mjs` + `tone.json`, wired into reflex verbosity, the DMN gate, and the thalamus threshold. §8.
- **M6 — THE DEFAULT MODE NETWORK** — `dmn.mjs` (the Rest Room: away-signal → interview simulation → `dmn_precache.json`). §7a.
- **M7 — PREDICTIVE PRESENCE** — the wiring of Twin stall-signature × thalamus × precache, through the earned-voice gate. §7e. The crown.
- **M8 — THE COUNCIL** (`council.mjs`) + **the LIVING DOSSIER** (`dossier.json`). §7b, §7f.
- **M9 — THE KENNEL** — smoke-test the Pi OAuth first, then `groundsman.mjs` + `bus_lease.json` + the Away-Day Runner. §9. *(Can run in parallel with the above — it's the body, not the brain.)*
- **M10 — memory scaling** — month-sharding + prune/merge + the forgetting curve, once the store is real. §6.

---

## 11. THE LAWS THIS EVOLUTION OBEYS (inherited, none weakened)
No metered API key ever (`cortex.mjs` = `claude -p` Max; regions = free Gemini pool — the thalamus's job is literally to *protect* the ceiling). Personal data local + gitignored (afferent, frames-as-hashes, hippocampus, dossier — the public repo holds the *machinery*, never the moments). No ambient mic (the voice nerve carries only turns Dugout already captures). Medical clamp absolute (Governor read-only into GOV; biometrics weight attention never drive a verdict; prosody excluded from every score by construction; RED = doctor-referral). Humane clamp / earned proactivity (the gate deciding a moment is salient does NOT grant it a voice — outbound speech still passes `shadow.mjs`'s ratify gate + win-only voicing; the thalamus decides *what gets thought about*, never *what gets said*). AI proposes · code validates · human approves (the DMN/Council/Consolidator all emit drafts on the bus; nothing auto-acts). Layering never replace (every new organ sits on top of the proven code — `embedTexts`, `cosine`, `execRecall`, `loadDayCartridge`, `buildSystemInstruction`, the budget accounting — none rewritten).
**(re-verified 10 Aug 2026 — this whole section HELD; nothing in it needed correcting, which is worth recording.** All five named functions still exist under their original names in `dugout.mjs` (`grep -n "function embedTexts\|function execRecall\|function loadDayCartridge\|function buildSystemInstruction" scripts/dugout.mjs`), and `indexRecall`/`recall_index.jsonl` survive untouched as the batch back-fill floor exactly as §6 promised. The budget accounting is genuinely shared, not copied — `cortex.mjs` **imports** `headroom`, `loadConfig`, `bannedPhraseCheck` and `maxThinkingFor` straight from `brain.mjs` rather than reimplementing them. The layering law is also visible as frozen twins across the codebase: `dreamLegacy` in `dmn.mjs`, `_gate_tune_legacy["tiers@<date>"]` in the thalamus config, `recallReflexLexical` beside the embedding reflex. **One clarification the §11 sentence about single-writer deserves:** `brain_ledger.jsonl` is a deliberate SHARED APPEND LANE — the cortex, the council's sonnet chair and several other organs all append to it, with `brain.mjs` owning the schema alone. That is a documented exception, not a breach of "one writer per file".)

---

## 12. HONEST RISKS (unrun = hypothesis)
- **Preview churn** — `native-audio-preview-12-2025`, `3.1-flash-live-preview`, `v1alpha` affective/proactive are all preview surfaces; IDs/features shift (this session already logged `text-embedding-004` → 404). **Every tank's model behind a pref** (as `dugout_prefs.json` already does) so a killed preview swaps without a rewrite. **(corrected 10 Aug 2026: DONE, and in a better place than `dugout_prefs.json`.** Every tank's model now sits in canon at **`dressing-room/state/fuelboard_config.json`**, one line per tank, overriding the `DEFAULT_TANKS` fallback baked into `fuelboard.mjs` — so a killed preview is a one-line edit to a JSON file, no code change. The Dugout's own model stays swappable via prefs/env on top of that (`DEFAULT_MODEL`, selftest-pinned). The churn this risk predicted has already happened twice in the record: `text-embedding-004` → 404, and bare `gemini-3.1-flash` dead on REST — which is why the registry leans on `-latest` aliases wherever a preview id is not required.)
- **Free-tier per-project TPM/RPM is modest** — the 1000-concurrent/4M-TPM numbers are Vertex/Firebase, NOT the Developer-API free tier. One tank ≈ one conversation's budget, not infinite — which is *why* 7 projects matter; T7 is the shock absorber.
- **Pi Claude OAuth longevity unverified** — smoke-test 48h before trusting the night-shift (§9). **(re-verified 10 Aug 2026: STILL UNVERIFIED — this risk is fully live. `bus_lease.json` does not exist, so nothing has ever held the lease. See §9's block.)**
- **Flat-file memory won't scale to a year** — M10 (sharding + forgetting) before the store grows large. **(corrected 10 Aug 2026: the MITIGATION shipped — M10 is `hippocampus.mjs consolidate-store`, on the schedule nightly at 02:20. Whether the store has actually grown large is a live measurement, not a prediction: `wc -l dressing-room/hippocampus/episodes.jsonl dressing-room/state/recall_index.jsonl`.)**
- **The whole thing is a hypothesis until it runs** — build in §10 order, selftest + real-run each, exactly as the U-series and E2E pass did. **(corrected 10 Aug 2026: it RAN. That is what M0–M10's commits and the green suites mean, and it is why this document must now be read as a design record with live-command annotations rather than as a plan. The law itself does not retire — it just points somewhere new: the surviving hypotheses are the two named at the top of this file (the full-arc live-voice session and the Pi's 48h OAuth smoke) plus `wall_breaker` ratification. Everything else in here has a command that answers it.)**

---

*The organism already owns the neuromodulation (Governor), the prediction (Twin), the sleep-consolidation (digest), and the plasticity (genome). This evolution adds the three things it lacks — **parallel regions, a thalamus, and a default-mode network** — and wires the two-speed relay. That is the line between a batch pipeline and a mind. Build the keystone first. It proposes. You decide. COYG.* ⚪🔴
