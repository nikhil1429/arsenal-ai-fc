# ⚪🔴 CONDUCTOR_LOG — Arsenal AI FC · Build Ledger

**Append-only.** One block per agent, in build order (per `CONDUCTOR.md` §11 spec). A fresh Claude Code or chat session runs the §0 boot — reads `OPS_STATE.md` + `CONDUCTOR.md` + this file — then resumes from the last unfinished agent. **A green log is a claim; the committed `.mjs` is the fact** — always fetch/read the actual file (CONDUCTOR §9 anti-hallucination rule).

_Last updated: 2026-07-10 · Branch: `main`_

> **(corrected 10 Aug 2026 — READ THIS BEFORE ANY LINE BELOW.)** That stamp said `2026-07-10`
> while the newest block in this very file was the **9 Aug 2026** launch-audit annotation under
> M-2, and the file's own last commit is `32faedc` (9 Aug 2026, "Launch E: the docs stop lying").
> A month of edits the header denied. **So don't trust this line either — read the stamp live:**
> `git log -1 --format=%ad --date=short -- CONDUCTOR_LOG.md`. Branch likewise:
> `git rev-parse --abbrev-ref HEAD` (returned `main`, 10 Aug 2026).
>
> **THE STANDING WARNING FOR THIS WHOLE FILE.** This is an append-only BUILD ledger: nearly every
> line is a true statement *about the moment it was written*. Two classes of line rot anyway and
> are annotated inline below —
> **(a) selftest check-counts** (every suite has grown 2–5× since; always re-run, never read a
> number off this page), and
> **(b) anything about scheduling or gitignore** (the four signal tasks were folded into the
> conductor chain, and his D10 ruling of 5 Aug 2026 put two "gitignored" outputs into the public
> repo on purpose). Live counts in one line:
> `for s in capture fsrs calibration nemesis learning_state manager; do node scripts/$s.mjs selftest; done`

Build order (CONDUCTOR §3): **#0 Shared Capture Layer → #1 FSRS → #2 Calibration → #3 Nemesis → #4 learning-state → Manager (capstone, LAST).**

> **(checked 10 Aug 2026 — the two lines above HELD, both of them, word for word.)** `CONDUCTOR.md` exists at the repo root; its §11 really is the `CONDUCTOR_LOG.md` SPEC this file is written to (`Design capsule / Files written / Selftest / Output schema / Empty-state / Capture-hook / Secrets-PII / Deviations / Commit` — the field list every block above follows); its §0 really does name the same three boot files in the same order (`OPS_STATE.md` → `CONDUCTOR.md` → `CONDUCTOR_LOG.md`); its §3 really does state this build order verbatim; and §9 really does carry the anti-hallucination rule quoted above — *"never call an agent 'done' from the log's word alone — fetch the actual committed file and read it."* Verify: `grep -n "^## §" CONDUCTOR.md`. **That §9 rule is the reason this whole correction pass exists — apply it to every line below, including the corrections.**

---

## Ledger

## 0. Shared Capture Layer (`capture.mjs`) — green · 2026-07-10
- Design capsule: single writer of `reps_log.jsonl`; one JSON/line per study rep `{ts, surface:"gem"|"colab", concept, question, confidence(enum: knew|shaky|guessed), correct(bool), note?}`; two intakes — `paste` (Gems) + `pull` (Colab→Drive, Option B); deterministic, zero-LLM. Three consumers (FSRS/Calibration/Nemesis) READ it, never write it.
  - **(corrected 10 Aug 2026 — THREE intakes now, not two.** A third door `rep` (one rep as it happens, audit #107, 5 Aug 2026) was added and routes through the SAME `ingest()` — `grep -n 'mode === "paste" || mode === "rep"' scripts/capture.mjs`. It is not a second validator: the code comment at that site says so explicitly ("same lock … No second validator can drift from the first, because there isn't one"). Read the live door list: `grep -n 'mode === "' scripts/capture.mjs`. **The single-writer claim itself HELD on re-check** — `grep -rn "reps_log.jsonl" scripts/*.mjs` returns 26 files, and `capture.mjs` is the only one that writes; every other hit is a read. The consumer count also grew past three — `fsrs`/`calibration`/`nemesis` plus `learning_state`, `bootroom`, `brain`, `dugout`, `limits`, `nikhil_model`, `outwork_audit`, `physio` and more all read it. Count them live rather than trusting "three".)**
- Files written: `scripts/capture.mjs` · `.gitignore` (+`reps_log.jsonl`) · scheduled task `ArsenalFC-CapturePull` (DISABLED placeholder).
- Selftest (`node scripts/capture.mjs selftest`, verbatim; re-run clean, exit 0):
  ```
  ✓ empty-safe: missing log loads as 0 reps
  ✓ valid-append: 2 valid reps appended
  ✓ malformed-reject: 7 rejected, only 1 valid appended
  ✓ dedup: identical ts+question not re-appended
  ✓ empty-ingest: no file fabricated
  ✓ pull dormant: missing inbox → 0 pulled, wired=false

  ALL CHECKS PASSED
  ```
  **(corrected 10 Aug 2026: that verbatim block is a true transcript of 10 Jul and is KEPT as one — but it is 6 checks and the suite is far past it. Re-run on 10 Aug 2026: still `ALL CHECKS PASSED`, now **62** `✓` lines, including doors that did not exist in July (writer-lock, quarantine, the three-clocks arrival stamp, the heartbeat chain). Never read a count off this page: `node scripts/capture.mjs selftest`.)**
- Output schema: `reps_log.jsonl` `{ts, surface, concept, question, confidence, correct, note?}` — this is the RAW shared layer; the Manager reads the DERIVED consumer JSONs (FSRS/Calibration/Nemesis) per THE_MANAGER §4, not this raw log. Raw rep contract = the §4 study-rep event. matches: yes (raw layer feeds the §4/§10 consumers).
- Empty-state: missing/empty `reps_log.jsonl` = valid awaiting-data; consumers treat absent as empty; capture NEVER fabricates a rep. yes.
- Capture-hook: session-end structured report → `paste` (Drill-Gem JSON array) + `pull` (Colab→Drive inbox, Option B). Documented in the `capture.mjs` header + `MANUAL_WIRING.md`.
  - **(re-verified 10 Aug 2026 — the documentation pointer HELD.** `MANUAL_WIRING.md` still exists at the repo root and still carries the live rep contract, the gut-word law ("committed BEFORE the answer is revealed"), and the Colab `log_rep` cell that asserts the 3-set and emits `track:"skill"`; `capture.mjs`'s header still points at it (`grep -n "MANUAL_WIRING" scripts/capture.mjs`). **The `rep` door added 5 Aug is NOT described in this bullet** — see the three-intakes correction on the design-capsule line above.)**
- Secrets/PII: `reps_log.jsonl` gitignored + `git check-ignore` VERIFIED (personal study data kept off the PUBLIC repo). yes.
  - **(CORRECTED 10 Aug 2026 — THIS IS NO LONGER TRUE, AND THAT IS DELIBERATE. `reps_log.jsonl` is TRACKED and travels with the PUBLIC repo today.** Evidence: `git check-ignore -v dressing-room/state/reps_log.jsonl` returns nothing, and `git ls-files --error-unmatch dressing-room/state/reps_log.jsonl` names it. The ignore line was added at `ea10f85` (10 Jul, this block's own commit) and REMOVED at `2aff0a0` (5 Aug 2026, "audit #107: the learning-layer wiring repair"). This was HIS CALL, not drift — `.gitignore` records it in prose at the top of the state section: *"D10, 5 Aug 2026: reps_log.jsonl now travels with the repo (his ruling)"*, alongside the same ruling for `readiness.json` / `intake_log.json` ("He was shown both files by name, was told plainly that a public push is irreversible, and ruled twice that they go in the repo"). `capture_config.json` STAYS ignored — for a different reason, also written into `.gitignore`: it holds THIS machine's Drive path, so committing it "would break the next machine, not expose him" (verified ignored, `.gitignore:54`). **Do not re-add the ignore without his word.** Check the live state, never this line: `git check-ignore -v dressing-room/state/reps_log.jsonl`.)**
- Deviations / notes: **pull-path is now LIVE (Option B wired 10 Jul).** Google Drive for Desktop is installed + synced (My Drive mounts at `G:`, GoogleDriveFS running). Inbox = `G:/My Drive/arsenal/reps_inbox`, resolved from machine-local `capture_config.json` (gitignored) with env `ARSENAL_REPS_INBOX` override — **no hardcoded user path in the script** (the earlier `USERPROFILE\My Drive` guess was removed). **Round-trip PROVEN:** wrote a rep `.jsonl` into the inbox → `node scripts/capture.mjs pull` → `pulled 1 from 1 file(s)`, appended to `reps_log.jsonl` and moved the source to `/done`; test rep then removed so nothing persists. Task `ArsenalFC-CapturePull` **ENABLED — Status: Ready**, cadence **HOURLY 09:00–22:00 daily** (Daily trigger; repeat every 1h for a 13h duration → last run 22:00). Round-trip **re-confirmed LIVE** after the cadence change (`pulled 1 from 1 file(s)`, appended to `reps_log.jsonl` + moved to `/done`, test rep removed — nothing persists). (History: built DORMANT 10 Jul when Drive was absent → flipped LIVE once Drive installed → cadence changed from 12/15/18/21 to hourly 09:00–22:00.) Gems `paste` path unchanged (live from day one).
  - **(RE-VERIFIED LIVE 10 Aug 2026 — this whole bullet HELD, every clause.** `Get-ScheduledTask ArsenalFC-CapturePull` → `State=Ready`; trigger `MSFT_TaskDailyTrigger @2026-07-10T09:00:00` with `Repetition Interval=PT1H, Duration=PT13H` = hourly 09:00→22:00 daily, exactly as written; `Get-ScheduledTaskInfo` shows `LastRunTime 10-08-2026 18:00:02, LastTaskResult 0, NextRunTime 10-08-2026 19:00`. The no-hardcoded-path claim also held: resolution order is env `ARSENAL_REPS_INBOX` → `capture_config.json {inbox}` → dormant (`grep -n "ARSENAL_REPS_INBOX" scripts/capture.mjs`), and the live `capture_config.json` reads `"inbox": "G:/My Drive/arsenal/reps_inbox"` with that directory present on disk. **This is the ONE task in the 08:40–08:45 family still armed as its own row** — see the correction under the four consumers below. Verify live, not here: `Get-ScheduledTask -TaskName ArsenalFC-CapturePull`.)**
  - **(added 10 Aug 2026 — a `pull` now RECOMPUTES the consumers, which #1 FSRS's "left UNwired" note below still denies.** `capture.mjs pull` shells `heartbeat.mjs` by default when it ingested anything (`grep -n "if (r.pulled > 0 && !noChain)" scripts/capture.mjs`), and heartbeat's own order is capture → fsrs → capsule_bridge → calibration → nemesis → learning_state (read it live: `node -e "console.log(require('./dressing-room/state/heartbeat_config.json').order.map(o=>o.name).join(' → '))"`). `paste`/`rep` chain only on an explicit `--chain`; `--no-chain` opts out; `ARSENAL_CAPTURE_CHAINED=1` blocks recursion.)**
- Commit: #0 build `ea10f85` · NUL-fix `0177aa6` · Option-B-LIVE `604d914` · hourly-cadence `e933eaf`.
- Amendment (10 Jul): `confidence` **int 0-100 → enum `knew`|`shaky`|`guessed`** (one gut-word committed before the answer is revealed). Why: the 0–100 % scale was over-engineered false-precision; a 3-way gut-read is the honest, near-zero-friction signal and maps cleanly onto FSRS ratings downstream (guessed/shaky/knew → Hard/Good/Easy on a correct rep). `reps_log.jsonl` was empty (captain starts tomorrow) → **no migration needed**. Updated: `capture.mjs` validateRep + header, `MANUAL_WIRING.md` (Gem prompt asks the gut-word; Colab `log_rep` asserts the 3-set), selftest (added enum-reject + enum-accept). Selftest re-run **ALL CHECKS PASSED**. **(re-verified 10 Aug 2026: the enum is still exactly `knew`|`shaky`|`guessed` and the surface set is still exactly `gem`|`colab` — `grep -n "const SURFACES" scripts/capture.mjs` → `new Set(["gem", "colab"])`. Commit for this amendment, which the line left unnamed: `4eafa0b` "amend agent #0: confidence int(0-100) -> enum knew|shaky|guessed (gut-word before answer)", 10 Jul 2026.)**
- **Amendment v2 (10 Jul) — ontology at ground-zero** (`reps_log` was EMPTY → zero migration). Enriched the rep contract with: `track:"concept"|"skill"` · `axis:"a".."i"|null` (concept-only; skill MUST be null) · `latency_ms:int≥0|null` (optional) · `aided:bool|null` (skill-only) · `unregistered:bool` (concept not in registry → **SOFT: still logged, never dropped**). NEW canon file `dressing-room/state/concepts.json` (committed vocab: 9 axes + concept/skill IDs + aliases) — capture **READS only** (single-writer preserved), drives concept normalization (case-fold + alias) + the `unregistered` flag; missing registry ⇒ still logs. Strictly **additive (layering)** — every prior check retained; no `validateRepLegacy` needed (empty log, old checks are a subset). Selftest now **19 checks** (track/axis-accept/reject · skill-null-axis · skill+axis-reject · latency accept/reject · aided-only-on-skill · alias→canonical · unknown→unregistered · concepts-missing→still-logs · + all prior) → **ALL CHECKS PASSED**. `MANUAL_WIRING.md` emitters updated in lockstep (Gem emits `track:"concept"`+axis; Colab `log_rep` emits `track:"skill"`+`aided`) — REQUIRED, else new reps would reject. Commit: capture-v2 (see `git log`).
  - **(corrected 10 Aug 2026 — the unnamed hash is `d11e9df`** ("amend agent #0: capture v2 — track/axis/latency/aided/unregistered + concepts.json registry", 10 Jul 2026). CONDUCTOR §11's own spec line is `Commit: <hash once pushed>`, so "see `git log`" was a placeholder that never got filled. **The v2 content itself re-verified today:** `concepts.json` is present + TRACKED and still carries exactly **9 axes** (`a`–`i`) with a `core` flag on **every** concept and skill entry — count it live, never from here: `node -e "const c=require('./dressing-room/state/concepts.json'); console.log(Object.keys(c.axes).length+' axes, '+Object.entries(c.concepts).filter(([,v])=>'core' in v).length+'/'+Object.keys(c.concepts).length+' concepts cored, '+Object.entries(c.skills).filter(([,v])=>'core' in v).length+'/'+Object.keys(c.skills).length+' skills cored')"`. `MANUAL_WIRING.md` is still in lockstep — its rep contract line carries `track`, `axis`, `latency_ms`, `aided`, and the gut-word law; its Colab `log_rep` still asserts the 3-set and emits `track:"skill"`. The **19 checks** figure is long dead: 62 today.)**

## 1. FSRS (`fsrs.mjs`) — green · 2026-07-10
- Design capsule: CONSUMER of `reps_log.jsonl` (never writes it). CONCEPT-level cards (card id = normalized concept string); every rep on a concept = one review event, replayed in `ts` order through the real FSRS algorithm. Rating map: incorrect→Again · correct+guessed→Hard · correct+shaky→Good · correct+knew→Easy. Outputs `cards.json` (Manager summary) + `fsrs_store.json` (per-card). Single writer = `fsrs.mjs`.
  - **(corrected 10 Aug 2026 — FSRS HAS THREE INPUTS NOW, not one; planning off "consumer of reps_log" alone will miss two of them.** (i) **THE CAPSULE FLOOR** (15 Jul): his gist capsules under `dressing-room/state/capsules/` carry real dated review history — `lockedOn` plus each `reJirahDone` — and replay as review events with `surface:"capsule"`, so the decay guard covers locked ground from day zero (`grep -n "THE CAPSULE FLOOR" scripts/fsrs.mjs`; no dated lock ⇒ still no card, so it is not fabrication). (ii) **THE RE-JIRAH BRIDGE** (audit #108, 6 Aug): `rejirah_log.jsonl` is read so a `reJirahDone` date inherits the round's REAL result instead of an unconditional `Rating.Easy` — `grep -n "THE RE-JIRAH BRIDGE" scripts/fsrs.mjs` and `grep -n "readRejirahRounds" scripts/fsrs.mjs`. A round with any cracked axis now grades `correct:false`; rows with no matching close keep the old replay and are tagged `seed_basis:"legacy-gist"`. Also added since: `review_unit:"local_day"` with `collapse_rating:"worst"` (audit #24) — same-day reps collapse to one review at the WORST grade, and the pre-audit replay is frozen as `buildStoreLegacy` per the layering law. `grep -n "review_unit" scripts/fsrs.mjs`. **The rating map and the single-writer claim on the line above both re-verified unchanged in code** — `grep -n "function ratingOf" -A5 scripts/fsrs.mjs`, and one `writeAtomic` pair for `cards.json`/`fsrs_store.json` with no other script writing either.)**
- Files written: `scripts/fsrs.mjs` · `package.json` + `package-lock.json` (ts-fsrs dependency) · `.gitignore` (+`cards.json`, `fsrs_store.json`) · scheduled task `ArsenalFC-FSRS` (daily 08:40, before the Manager's 08:45).
  - **(corrected 10 Aug 2026 — `ArsenalFC-FSRS` STILL EXISTS BUT IS `Disabled`, AND THAT IS NOT A FAILURE. Read this before you "fix" a dead task.** Live check: `Get-ScheduledTask -TaskName ArsenalFC-FSRS` → `State=Disabled`, trigger still `08:40`, action still `node …/fsrs.mjs recompute`. The same is true of `ArsenalFC-Calibration` (08:42), `ArsenalFC-Nemesis` (08:43) and `ArsenalFC-LearningState` (08:44) — all four `Disabled`. They were folded into ONE ordered chain: `ArsenalFC-Morning-Conductor` (`State=Ready`, daily `09:15`, action `run_logged.cmd scripts\conductor.mjs morning`), whose step list keeps each agent's ORIGINAL time as its ordering key — `grep -n '{ id: "fsrs"' scripts/conductor.mjs` shows `at: "08:40", args: ["scripts/fsrs.mjs","recompute"], writes: "cards.json"`, and calibration/nemesis/learningstate follow at 08:42/08:43/08:44 exactly as this ledger recorded them. **The agents therefore still run daily** — proof, not inference: `cards.json`, `calibration.json`, `weaknesses.json` and `learning_state.json` all read `date: 2026-08-10` on the morning of this repair. Re-enabling the four standalone rows would double-run them. Verify live: `Get-ScheduledTask | ? TaskName -like 'ArsenalFC-*' | ft TaskName,State`.)**
- **FSRS impl: `ts-fsrs` 5.4.1 — FSRS-6.0, 21-weight default parameters**, `request_retention` 0.90 (configurable via env `FSRS_RETENTION`), `enable_fuzz=false` (deterministic → reproducible selftest). Real vetted algorithm, not hand-rolled math. `node_modules` gitignored; `package.json`/`package-lock.json` committed for reproducible install.
  - **(re-verified 10 Aug 2026 — EVERY CLAUSE OF THIS BULLET HELD.** Installed version 5.4.1 (`node -e "console.log(require('ts-fsrs/package.json').version)"`), declared `"ts-fsrs": "^5.4.1"` in `package.json`; **21 weights confirmed by running the library, not by trusting the prose** — `node -e "console.log(require('ts-fsrs').generatorParameters({}).w.length)"` → `21`; `request_retention` default `0.90` with the `FSRS_RETENTION` env override and `enable_fuzz=false` both still in `scripts/fsrs.mjs` (`grep -n "FSRS_RETENTION\|enable_fuzz" scripts/fsrs.mjs`); `node_modules/` ignored at `.gitignore:26`; both package files tracked (`git ls-files package.json package-lock.json`). The live `cards.json` stamps `engine: "fsrs-6 (ts-fsrs 5.4.1)"`, and note the script reads that version from `node_modules` at RUNTIME — so the engine string self-corrects on upgrade even though this ledger line would not.)**
- Selftest (`node scripts/fsrs.mjs selftest`, verbatim; exit 0):
  ```
  ✓ correct reviews lengthen interval (growth interval ≥ 7d)
  ✓ incorrect resets interval (lapse interval < 2d)
  ✓ sustained-correct interval > lapsed interval
  ✓ due/overdue counts (overdue=2, due_today=1)
  ✓ hardest_due ordered soonest-due first
  ✓ hardest_due tie-break by lowest-stability
  ✓ empty-safe: status awaiting_data, zero counts, no cards

  ALL CHECKS PASSED
  ```
  Plus a live end-to-end proof on real state: `capture paste` 4 reps → `fsrs recompute` → 2 cards, `overdue 1` (the lapsed "TDS 194C"), then cleaned back to `awaiting_data`.
  **(corrected 10 Aug 2026: 7 checks then, **38** now — re-run 10 Aug still `ALL CHECKS PASSED`. `node scripts/fsrs.mjs selftest`.)**
- Output schema: `cards.json` `{date, engine, request_retention, total_cards, due_today, overdue, hardest_due[], status, generated_at}` — matches THE_MANAGER §10 (`due_today`/`overdue`/`hardest_due`/`status:"awaiting_data"`). matches: yes.
  - **(corrected 10 Aug 2026 — the schema GREW, additively; every field above is still emitted.** Live keys today: `date, engine, request_retention, total_cards, due_today, overdue, hardest_due, status, gate, collapse, generated_at` — the two new ones are `gate` (the have/need counter every gate word now ships, audits #100/#106) and `collapse` (the #24 same-day review-unit report). Read them live rather than from here: `node -e "console.log(Object.keys(require('./dressing-room/state/cards.json')).join(','))"`. Live file on 10 Aug 2026 reads `date=2026-08-10, status=ok` — this agent is not dormant.)**
- Empty-state: no reps → `cards.json` `status:"awaiting_data"` + empty store; never fabricates a card. yes.
- Capture-hook: n/a — FSRS is a consumer; it reads Agent #0's `reps_log.jsonl`.
- Secrets/PII: `cards.json` + `fsrs_store.json` gitignored + `git check-ignore` VERIFIED (derived personal study data: concepts + schedule — kept off the PUBLIC repo). yes.
  - **(re-verified 10 Aug 2026 — STILL TRUE for both.** `git check-ignore -v` → `.gitignore:57` and `.gitignore:58`. These two survived the 5 Aug D10 ruling that un-ignored `reps_log.jsonl` and `learning_state.json`; do not assume the ruling swept the whole state dir. Check each file individually: `git check-ignore -v dressing-room/state/<file>`.)**
- Deviations / notes: introduces the repo's **first npm dependency** (`ts-fsrs`) — deliberate, per the captain's stated preference for a vetted impl; `package.json`/`package-lock.json` committed, `node_modules` gitignored (a fresh clone runs `npm install`). Recompute scheduled daily **08:40**; the optional "recompute after each capture pull" hook was left UNwired to keep `capture.mjs` single-purpose (can be added to the CapturePull task action later).
  - **(CORRECTED 10 Aug 2026 — THAT HOOK IS WIRED NOW, so do not "add it later"; it exists.** Organism audit #25 (4 Aug 2026) gave `capture.mjs` a heartbeat chain: a `pull` that ingested anything shells `heartbeat.mjs`, which runs capture → fsrs → capsule_bridge → calibration → nemesis → learning_state in that fixed order. Evidence: `grep -n "if (r.pulled > 0 && !noChain)" scripts/capture.mjs` and `grep -n "chainHeartbeat" scripts/capture.mjs`; the order is data, read it live with `node -e "console.log(require('./dressing-room/state/heartbeat_config.json').order.map(o=>o.name).join(' → '))"`. `capture.mjs` did NOT stop being single-purpose in the way this note feared — it shells a sibling and never computes a card itself, and recursion is blocked by `ARSENAL_CAPTURE_CHAINED=1`. The "scheduled daily 08:40" half is also now the conductor's ordering key, not a live task row — see the correction on the Files-written line above.)**
- Commit: FSRS build — STEP-2 commit `0361a6e`. **(verified 10 Aug 2026: `0361a6e` = "agent #1: FSRS scheduler (fsrs.mjs) — concept-level cards via ts-fsrs (FSRS-6)", 10 Jul 2026. Holds.)**
- **Fix (10 Jul, capture-v2 lockstep) — track-filter:** `validRep` now requires `track === "concept"`; `track:"skill"` (Python) reps are IGNORED by FSRS (canon: fluency is a #4 signal, not a spaced-recall card). Baked selftest mocks updated with `track:"concept"`; ADDED assertions `skill-track ⇒ ZERO cards` + `concept-track mocks still produce cards`. Rating map unchanged. Selftest **9 checks ALL CHECKS PASSED**; live proof: mixed concept+skill reps → only the concept card (`bpe`→`tokenization`). Commit: fsrs track-filter (see `git log`).
  - **(corrected 10 Aug 2026 — the unnamed hash is `dd67913`** ("fix agent #1: FSRS track-filter — only track:concept reps become cards (skill ignored)", 10 Jul 2026). **The track-filter itself re-verified in code, not from this line:** `grep -n 'r.track === "concept"' scripts/fsrs.mjs` → it is the last clause of `validRep()`, with the comment "CONCEPT-track only — skill (Python) reps are NOT cards (fluency = #4)". Rating map also re-read and unchanged: incorrect→Again · guessed→Hard · shaky→Good · knew→Easy (`grep -n "function ratingOf" -A5 scripts/fsrs.mjs`). "9 checks" is dead — 38 today.)**

## 2. Calibration (`calibration.mjs`) — green · 2026-07-10
- Design capsule: CONSUMER of `reps_log.jsonl` (BOTH tracks — self-knowledge is domain-general). `calibration_gap` = **ECE** vs target accuracies `{knew:0.95, shaky:0.65, guessed:0.30}`; `overconfidence_rate` = P(wrong|knew); per-topic `danger_zone` (knew-WRONG only: ≥3 knew reps AND knew-accuracy < 0.67) with concept-track axis-sharpen; rep-count trend windows; reliability floor (`awaiting_data`/`warming_up`/`ok`) **biased to SILENCE**. Deterministic, zero-LLM. Single writer `calibration.json`.
- Files written: `scripts/calibration.mjs` · `dressing-room/state/calibration_config.json` (canon config) · `.gitignore` (+`calibration.json`) · scheduled task `ArsenalFC-Calibration` (daily 08:42, after FSRS 08:40 / before Manager 08:45).
  - **(corrected 10 Aug 2026: `ArsenalFC-Calibration` is `Disabled` as a standalone row and now runs as the `calibration` step of `conductor.mjs morning`, still at `at: "08:42"` — the same fold described in full under the FSRS block above. `calibration_config.json` verified present + TRACKED. `calibration.json` still gitignored — `.gitignore:61`.)**
- Selftest (`node scripts/calibration.mjs selftest`, verbatim tail; exit 0):
  ```
  ✓ axis-sharpen: shared axis ⇒ axis f
  ✓ axis-sharpen: mixed axes ⇒ no axis
  ✓ domain-general: skill (Python) topic CAN enter danger, no axis
  ✓ trend narrowing (2 full windows, gap shrinks)
  ✓ trend holding steady (delta < trend_delta)
  ✓ trend establishing baseline (<40 reps)
  ✓ config missing ⇒ defaults
  ✓ concepts.json missing ⇒ topic = raw normalized id

  ALL CHECKS PASSED
  ```
  (19 checks total: empty-safe · warming_up-suppresses-danger · ECE=0.2125 · overconf+null · buckets+empty-null · danger low/mid · gates 2-knew/shaky-wrong · axis-sharpen · domain-general · trend ×3 · config/registry-missing.) Live proof: 5 reps → `warming_up` (gap 0.5, overconf 0.67), danger **suppressed** even though chunking qualifies; cleaned to `awaiting_data`.
  **(corrected 10 Aug 2026: 19 checks then, **31** now — re-run 10 Aug still `ALL CHECKS PASSED`. `node scripts/calibration.mjs selftest`. The design numbers this block states all re-verified in the live config: `targets {knew:0.95, shaky:0.65, guessed:0.30}`, `min_reps 20`, `window_size 20`, `trend_delta 0.02`, `danger {min_knew_reps:3, accuracy_low:0.5, accuracy_mid:0.67}` — so "≥3 knew reps AND knew-accuracy < 0.67" is exactly gates (a)+(b) in `scripts/calibration.mjs` (`grep -n "gate (a)" -A6 scripts/calibration.mjs`). Read them live, not here: `cat dressing-room/state/calibration_config.json`. **The real state on 10 Aug 2026 is `status: "warming_up"`** — the danger zone is still SUPPRESSED by design, so an empty `danger_zone[]` today is the gate working, not the agent failing.)**
- Output schema: `calibration.json` `{date, calibration_gap(ECE), trend, overconfidence_rate, buckets{knew,shaky,guessed:{n,accuracy}}, danger_zone[{topic,confidence,accuracy,axis?,note}], total_reps, status, low_confidence, generated_at}` — §10 required (date/gap/trend/danger_zone) present + additive extras. matches: yes.
  - **(re-verified 10 Aug 2026 — every field above is still emitted, plus one more added since: `gate` (the have/need counter). Live keys: `node -e "console.log(Object.keys(require('./dressing-room/state/calibration.json')).join(','))"`.)**
- Empty-state: 0 reps → `status:"awaiting_data"`, gap null, danger []; never fabricates. yes.
- Capture-hook: n/a — consumer of Agent #0's `reps_log.jsonl`.
- Secrets/PII: `calibration.json` gitignored + `git check-ignore` VERIFIED (derived study data: gap + confident-wrong topics). `calibration_config.json` = canon, trackable. yes.
- Deviations / notes: reads BOTH tracks per capsule (a knew-wrong on Python is a blind spot too). `danger_zone` SUPPRESSED below `min_reps` (Fork-4 bias-to-silence — an early false alarm teaches the captain to distrust the agent). ECE = Fork-1 scalar; full per-bucket breakdown kept (Fork-2 intent, nothing discarded). No new npm deps.
- Commit: Calibration build — this commit (see `git log`). **(corrected 10 Aug 2026 — "this commit" is unresolvable to a later reader, which is the exact failure CONDUCTOR §11's `Commit: <hash once pushed>` field exists to prevent. The hash is `e1d9ae7`** — "agent #2: Calibration (calibration.mjs) — ECE gap + confident-wrong danger-zone", 10 Jul 2026 22:56 IST.)**

## 3. Nemesis (`nemesis.mjs`) — green · BUILT (Fork A resolved = A3) · 2026-07-10
BUILT + selftest green (Fork A resolved = A3). The locked design below drove the build and is retained for provenance; build details + build-capsule fixes are appended at the end of this block.
- REFRAME (locked, god-tier): Nemesis is NOT a ranked shame-list (harmful for ADHD-PI + redundant — recurrence=FSRS's job, confident-wrong=Calibration's). Its UNIQUE signal (no per-card view can make it) = CROSS-CONCEPT AXIS PATTERN: "misses on tokenization+chunking+retrieval all cluster on axis-e (failure-modes)" → your nemesis is a KIND OF THINKING, not a topic. This is the payoff for capturing `axis` from day 1. Frame = self-SCOUT (OPPONENT_SCOUT/DOSSIER idiom), never shame. No 10x/hype in output (compounding frame).
- Principles (locked): (1) signal = RELAPSE (broke AFTER learned) or confident-wrong — NOT mere wrong≥3 (else it repeats FSRS). (2) ONE headline nemesis ("today's #1 opponent"), full list collapsed below (ADHD single-focus). (3) receipts/evidence per entry (§10 evidence:[...]) — scouting report, not taunt. (4) healed/closed visible (beaten opponent = trophy; list never endless). (5) cold-start honest — cross-concept pattern needs volume, near-silent day-1 BY DESIGN; FLOOR = per-concept relapse (simple), CEILING = cross-concept axis meta-pattern (emerges over weeks). bias-to-silence.
- Forks LOCKED: B (miss-signal) = relapse OR confident-wrong OR shaky-wrong, grouped per topic+axis (this is where the shaky-wrong deliberately kept OUT of Calibration flows in). C (ranking) = headline one primary nemesis + the cross-concept axis pattern; recency-weighted recurrence, decay so stale/healed sink. D (healed) = last 3 clean on a topic + no knew-wrong ⇒ status:"closed" (kept in history, off active rank; no alarm-fatigue).
- **[CORRECTED 10 Aug 2026 — FORK A IS CLOSED. DO NOT "RESOLVE IT FIRST NEXT THREAD"; IT WAS RESOLVED ON 11 JUL 2026 AND THE CODE SHIPPED.]** The bullet below is the *pre-decision* design record and is kept verbatim for provenance, exactly as this block's own header intends ("The locked design below drove the build and is retained for provenance"). But it opens with "resolve FIRST next thread", which is an INSTRUCTION, and a session that obeys it re-opens settled work — this file has caused that class of mis-planning before. The answer is **A3**, three lines further down under `--- BUILD ---`, and in code: `grep -n "Single writer of weaknesses.json" scripts/nemesis.mjs`; `nemesis.mjs` is the sole writer and `manager.mjs` only reads it (`grep -n "weaknesses" scripts/manager.mjs` — every hit outside the selftest fixtures is a read). Canon was reconciled the same day at commit `249d494` ("canon: THE_MANAGER §4/§5 — Nemesis writes weaknesses.json (Fork A3), Manager reads it + writes the team-sheet line"), and `THE_MANAGER__Master_Prompt.md` §4 now reads *"Nemesis is the writer of the FILE (Fork A3); the Manager consumes it and writes the team-sheet LINE"*. **Read the bullet below as history, never as a task.**
- Fork A — OPEN (resolve FIRST next thread): who writes weaknesses.json? THE_MANAGER §4 says "Manager is the writer"; CONDUCTOR §13 Fork-2 leaves it open. A1 = Nemesis writes ranked weaknesses.json directly (conflicts §4 wording). A2 = Nemesis writes raw nemesis_events.json, Manager derives ranked file at capstone. A3 = Nemesis is sole writer of weaknesses.json incl. rank, Manager only consumes (reconcile §4 as "Manager writes the team-sheet LINE, Nemesis writes the FILE" — consistent with FSRS→cards.json, Calibration→calibration.json). Chat-Claude lean = A3. Captain must pick, THEN the full build capsule is written.
- Schema target: THE_MANAGER §4/§10 weaknesses.json { weaknesses:[{id,topic,recurrence,last_seen,status,evidence[]}] } + additive axis-pattern field. Empty-safe (status:"awaiting_data"), single-writer, deterministic zero-LLM, output gitignored (derived PII), reads reps_log (both tracks) + calibration.json.

--- BUILD (2026-07-10) ---
- Files: `scripts/nemesis.mjs` · `dressing-room/state/nemesis_config.json` (canon, trackable) · `.gitignore` (+`weaknesses.json`) · scheduled task `ArsenalFC-Nemesis` (daily 08:43, after Calibration 08:42 / before Manager 08:45).
  - **(corrected 10 Aug 2026: `ArsenalFC-Nemesis` is `Disabled` as a standalone row and now runs as the `nemesis` step of `conductor.mjs morning`, still at `at: "08:43"` — same fold as FSRS/Calibration/LearningState, explained in full under the FSRS block. `nemesis_config.json` verified present + TRACKED; `weaknesses.json` still gitignored at `.gitignore:64`. Live file reads `date=2026-08-10, status=ok` — the agent is running.)**
- **Fork A RESOLVED = A3:** Nemesis is the SOLE writer of `weaknesses.json` (incl. rank); the Manager consumes it + writes the team-sheet LINE (FSRS→cards.json / Calibration→calibration.json precedent). Post-green THE_MANAGER §4 one-line canon clarification pending — human-gated, not mid-build.
  - **(CORRECTED 10 Aug 2026 — that clarification is NOT pending; it landed the same day, 11 Jul 2026, at commit `249d494`** "canon: THE_MANAGER §4/§5 — Nemesis writes weaknesses.json (Fork A3), Manager reads it + writes the team-sheet line". §4 now carries the sentence verbatim, and §5 adds *"Written by NEMESIS per Fork A3 (see §4); listed here for schema reference only — the Manager READS it, does not write it."* Verify without opening a doc's word: `grep -n "Fork A3" THE_MANAGER__Master_Prompt.md`. A "pending, human-gated" line left standing after the gate has been passed is how a session ends up asking him to rule on something he already ruled on.)**
- **Build-capsule fixes baked:** [A] `id` = stable topic slug (never positional — history/evidence survive recompute) · [B] `recurrence` = RAW int, `score` = separate recency-weighted float · [C] **`calibration.json` NOT read** — clean 2-input consumer (`reps_log` + `concepts.json`); confident-wrong is re-derived here, no coupling. *(Supersedes the design line above that said "reads … + calibration.json".)* · [D] `axis_pattern.strength` = distinct-concept cluster size; single volume gate reused (`warming_up_min_reps`, no extra knob).
- Selftest (`node scripts/nemesis.mjs selftest`, verbatim tail; exit 0):
  ```
  ✓ healed ⇒ status closed, retained, not headline
  ✓ single-focus: exactly one headline object
  ✓ receipts: every real entry has non-empty evidence[]
  ✓ schema: {id,topic,recurrence,last_seen,status,evidence[]} present
  ✓ concepts.json absent ⇒ axis null + no axis_pattern

  ALL CHECKS PASSED
  ```
  (17 checks: empty-safe · relapse-vs-never-learned · confident-wrong · shaky-wrong · guessed-wrong-excluded · recency-weighting · id-stability · recurrence-int/score-float · axis-cluster+mixed-null · volume-gate · skill-relapse-no-axis · healed-closed · single-headline · receipts · schema · registry-missing.) Live: 3 reps → `warming_up` headline (chunking relapse axis f + async skill), axis_pattern suppressed; cleaned to `awaiting_data`.
  **(corrected 10 Aug 2026: 17 checks then, **33** now — re-run 10 Aug still `ALL CHECKS PASSED`. `node scripts/nemesis.mjs selftest`.)**
- Output schema: `weaknesses.json` canonical `{id,topic,recurrence,last_seen,status,evidence[]}` + additive `{date,generated_at,total_reps,status,low_confidence,headline,axis_pattern{axis,concepts,strength,note}, per-entry axis,score}` — §4/§5/§10 present + extras. matches: yes.
  - **(re-verified 10 Aug 2026 — all present, plus two added since: `gate` (have/need counter) and `status_line`, both from audits #100/#106. Live keys: `node -e "console.log(Object.keys(require('./dressing-room/state/weaknesses.json')).join(','))"`. Fix [C] above also re-verified in code — nemesis STILL does not read `calibration.json`: `grep -n "calibration" scripts/nemesis.mjs` returns only comments naming the precedent, never a read. It remains the clean 2-input consumer (`reps_log.jsonl` + `concepts.json`) the build-capsule fix promised.)**
- Empty-state: 0 reps → `status:"awaiting_data"`, weaknesses [], headline/axis_pattern null; never fabricates. yes.
- Secrets/PII: `weaknesses.json` gitignored + `git check-ignore` VERIFIED (weak topics + receipts). `nemesis_config.json` = canon, trackable. yes.
- Commit: Nemesis build — this commit (see `git log`). **(corrected 10 Aug 2026 — the hash is `f55556e`** "agent #3: Nemesis (nemesis.mjs) — cross-concept axis-pattern self-scout". **Note the DATE:** this block is headed `2026-07-10`, but `f55556e` landed 11 Jul 2026 at 01:15 IST — the same overnight session, past midnight. The header day is the session's, not the commit's; both are honest, they just disagree by an hour. Trust `git log`, not the header.)**

## 4. learning-state (`learning_state.mjs`) — green · 2026-07-10
- Design capsule: the fluency / positional map (the **Maidan**). CONSUMER of `reps_log` (both tracks) + `fsrs_store.json` (join `due`→`rejirah_due`) + `concepts.json` (axis/bucket/**core**) + `learning_state_config.json` (thresholds + Maidan). Emits per concept/skill fluency 🔴🟡🟢 + velocity/trajectory + per-axis rollup + rejirah_due + edge-map + confusion-pairs + Maidan stages/handoffs + core_vs_light. Deterministic, zero-LLM. Single writer `learning_state.json`.
  - **(corrected 10 Aug 2026 — SIX inputs now, not four; the two extra ones are the forge's.** The four named above are all still read, at `scripts/learning_state.mjs:56–59`. Added since: `forge_session.json` and `forge_sessions.jsonl` (`:62–63`). Read the live list rather than this sentence: `grep -n "join(STATE_DIR" scripts/learning_state.mjs`. **The single-writer claim HELD** — one `writeAtomic(OUT, out)` in the whole file, and the only other scripts that write a `learning_state.json` are `manager.mjs`'s selftest fixtures under `tmpdir()`, never the real bus.)**
- **PART A `capture-v3`** (prior commit `6fe8df2`): `reps_log` gained `confused_with` (canonicalized like concept) + `edge` (verbatim) — the permanent raw inputs feeding edge-map + confusion-pairs. All prior capture checks + the 3 consumers stayed green.
- Files (PART B): `scripts/learning_state.mjs` · `learning_state_config.json` (canon) · `concepts.json` (+`core` flag on every entry) · `.gitignore` (+`learning_state.json`) · scheduled `ArsenalFC-LearningState` (daily 08:44, after FSRS/Cal/Nemesis, before Manager 08:45).
  - **(corrected 10 Aug 2026 — TWO of the five clauses have moved.** (1) **`.gitignore` no longer carries `learning_state.json`; the file is TRACKED and public.** It was added at this block's own commit `fd54b2c` (11 Jul) and REMOVED at `2aff0a0` (5 Aug 2026, audit #107) under the captain's **D10 ruling** — the same ruling recorded in `.gitignore` prose that put `reps_log.jsonl`, `readiness.json` and `intake_log.json` into the repo on purpose. Verify: `git check-ignore -v dressing-room/state/learning_state.json` (silent) and `git ls-files --error-unmatch dressing-room/state/learning_state.json` (names it). Do not re-ignore without his word. (2) `ArsenalFC-LearningState` is `Disabled` as a standalone row and now runs as the `learningstate` step of `conductor.mjs morning`, still at `at: "08:44"` — the fold is explained in full under the FSRS block. The other three clauses HELD: `learning_state_config.json` and `concepts.json` are both present + TRACKED, and the `core` flag is on **every** entry — counted live today, 26/26 concepts and 12/12 skills.)**
- LOCKED decisions honored: Maidan = **v0 SEED** (stage/handoff structure real domain architecture; latency/runnable thresholds are v0 hypotheses, R1-calibrated, lossless re-run) · **aided-gating IN** (skill 🟢 requires `aided:false`) · velocity IN · edge-map + confusion-pairs fed by real capture-v3 data · rejirah_due **joins** FSRS decay + axis (no decay re-impl — single-responsibility) · **DEFERRED to R1 (NOT built):** threshold calibration, drill-mechanics, pedagogical prereq-graph · Nidhi boundary: outcomes/state only.
- Selftest (`node scripts/learning_state.mjs selftest`, verbatim tail; exit 0):
  ```
  ✓ Maidan: weak_connection surfaces at ok volume
  ✓ warming_up: <min_reps ⇒ low_confidence + weak_connection null (suppressed)
  ✓ Manager surface fields present
  ✓ registry missing ⇒ graceful (raw id, bare axis letter, no crash)

  ALL CHECKS PASSED
  ```
  (20 checks: empty-safe · fluency-ladder · held≠fluent · regression · aided-gating true⇒not🟢/false⇒🟢 · latency-absent proxy · velocity improving/stalling · per-axis rollup · rejirah_due join + fsrs-missing⇒[] · edge-map-latest · confusion-pairs+attach · Maidan runnable+weak_connection · warming_up-suppress · Manager-surface-fields · registry-graceful.) **Ripple:** all 5 agents' selftests green after `concepts.json` `core` add. Live: 4 reps → chunking 🟢, pydantic 🔴, edge + core_vs_light populated; cleaned to awaiting_data.
  **(corrected 10 Aug 2026: 20 checks then, **48** now — re-run 10 Aug still `ALL CHECKS PASSED`. `node scripts/learning_state.mjs selftest`. The "all 5 agents green" ripple was re-run whole on 10 Aug and still holds, now across six suites: capture 62 · fsrs 38 · calibration 31 · nemesis 33 · learning_state 48 · manager 91, every one `ALL CHECKS PASSED`. Those six numbers will also rot — the command is the truth, not the tally.)**
- Output schema: `learning_state.json` — Manager §10 surface (`maidan_stage_focus, weak_connection, python_fluency, rejirah_due, core_vs_light`) + additive (`concepts[], axes[], maidan{}, edge_map, confusion_pairs`, envelope). matches: yes.
- Empty-state: 0 reps → `awaiting_data`, rep-derived lists `[]`, surface null/`{}`, `maidan.stages` skeleton from config (`runnable_frac:0`, `awaiting_data`); never fabricates a rep-derived value. yes.
- Capture-hook: n/a — consumer (capture-v3 added its two raw fields at the #0 layer).
- Secrets/PII: `learning_state.json` gitignored + `git check-ignore` VERIFIED. `learning_state_config.json` + `concepts.json` = canon, trackable. yes.
  - **(CORRECTED 10 Aug 2026 — the first half is NO LONGER TRUE and was reversed on purpose: `learning_state.json` is TRACKED and travels with the PUBLIC repo, per his D10 ruling of 5 Aug 2026 (`.gitignore` removal at commit `2aff0a0`). Full evidence and the same warning are on the Files-written line above. The second half HELD — both config files are present and tracked.)**
- Deviations / notes: registry-missing ⇒ **bare axis letter** (no label) + raw id — graceful, not null (learning-state keeps the rep's axis; concepts.json only supplies the label). Single volume gate (`warming_up_min_reps`) suppresses the `weak_connection`/`maidan_stage_focus` headline. No npm dep.
- Commit: learning-state build — this commit (see `git log`). **(corrected 10 Aug 2026 — the hash is `fd54b2c`** "agent #4: learning-state (learning_state.mjs) — the Maidan fluency/positional map", landed 11 Jul 2026 04:29 IST; PART A's `6fe8df2` landed 04:20 IST the same night. As with Nemesis, the `2026-07-10` header is the session's day, not the commit's.)**
- **(added 10 Aug 2026 — output schema re-verified.** Every field this block claims is still emitted, plus two added since: `gate` (have/need counter) and `position`. Live keys: `node -e "console.log(Object.keys(require('./dressing-room/state/learning_state.json')).join(','))"`. Live file reads `date=2026-08-10, status=ok` — this agent runs daily.)**

---
**SIGNAL-SOURCE AGENTS COMPLETE.** #0 Capture · #1 FSRS · #2 Calibration · #3 Nemesis · #4 learning-state — all built + selftest-green + scheduled (08:40–08:44) + outputs gitignored. Next & LAST = **the Manager capstone** (M-1 wrapper reference → M-2 soul → M-3 `claude -p` + billing guards → M-4 sandbox §11 → M-5 tasks), integrated + re-tested on the REAL agent JSONs now present.

> **(corrected 10 Aug 2026 — the headline verdict HOLDS; two of its four supporting clauses no longer describe today.)**
> - "all built + selftest-green" — **STILL TRUE**, re-run 10 Aug: all six suites `ALL CHECKS PASSED`. Don't take the word: `for s in capture fsrs calibration nemesis learning_state manager; do node scripts/$s.mjs selftest; done`.
> - "scheduled (08:40–08:44)" — **the TIMES survive, the four TASKS do not.** `ArsenalFC-FSRS`/`-Calibration`/`-Nemesis`/`-LearningState` are all `Disabled`; those same four now run in order as steps of `conductor.mjs morning` (`ArsenalFC-Morning-Conductor`, `Ready`, daily 09:15), each keeping its original `at:` as the ordering key. `ArsenalFC-CapturePull` is the one row still armed on its own (hourly 09:00–22:00). Full evidence under #1 FSRS.
> - "outputs gitignored" — **TRUE for `cards.json`, `fsrs_store.json`, `calibration.json`, `weaknesses.json`; FALSE for `reps_log.jsonl` and `learning_state.json`,** which his D10 ruling (5 Aug 2026) deliberately put into the public repo. Never assume the whole family shares one answer — check each: `git check-ignore -v dressing-room/state/<file>`.
> - "Next & LAST = the Manager capstone" — **that work is DONE and RUNNING, not next.** M-3's `claude -p` runs live in `brain.mjs` (`grep -n "manager_m3" scripts/brain.mjs dressing-room/state/brain_config.json`); the `formation_read` job is `enabled: true`, `model: "opus"`, `at: "08:45"`, `max_per_day: 1`. See the corrections in the two Manager blocks below before planning any capstone work.

## Manager (capstone) — M-1 `manager.mjs` PLACED + green · 2026-07-11
Capstone build step 1 of 5 (§9 THE SPLIT, Part 1 — the deterministic wrapper, NO LLM). This is NOT the full Manager: M-2→M-5 remain.
- **[CORRECTED 10 Aug 2026 — "M-2→M-5 remain" was true on 11 Jul and is FALSE today. The capstone is LIVE.]** M-2's `system.md` is a COMPLETE 586-line draft (`wc -l dressing-room/manager/system.md`) with all eleven sections present through THE EXEMPLARS; M-3's `claude -p` runs for real inside `brain.mjs` (`grep -n "manager_m3" scripts/brain.mjs`) reading that file into `runManager({llm})`; M-5's schedule + ntfy exist in a different shape than planned (details on the "Remaining" line at the end of this block). What is genuinely still open is the captain's **line-by-line review** of `system.md` and any §11-sandbox hardening he wants. **A session that reads this line alone will re-plan a capstone that has been shipping sheets for weeks** — the exact rot `CLAUDE.md` carries scars for. Verify before planning: `grep -n "manager_m3" scripts/brain.mjs dressing-room/state/brain_config.json`.
- **Placed VERBATIM** from web-Claude's proven prototype: `scripts/manager.mjs`, sha256 `f35c9f38…ca9c1`. Committed **`1d4e158`** (with `.gitignore`).
  - **(corrected 10 Aug 2026 — that sha256 was TRUE at placement and is a dead fingerprint now.** Verified honest: `git show 1d4e158:scripts/manager.mjs | sha256sum` → `f35c9f38e1f662cd…7ebca9c1`, matching the abbreviation exactly. But the working file today hashes `3ecbc586131b414d…6132f4cc0`, because **seven commits have touched `manager.mjs` since** (`git log --oneline 1d4e158..HEAD -- scripts/manager.mjs | wc -l` → 7; eight in total including the placement itself) — 393b32a, 34aeba6, 4d67b9a, 8a56f1f, 4f94805, 92af5db, 8b8eb34. **"Placed VERBATIM" is a fact about 11 Jul, not a description of the file on disk.** Never diff today's file against this hash and conclude it was tampered with. Compare live: `git show 1d4e158:scripts/manager.mjs | sha256sum` vs `sha256sum scripts/manager.mjs`.)**
- Selftest: `node scripts/manager.mjs selftest` → **ALL CHECKS PASSED (35 passed, 0 failed)**. Baked fixtures = verbatim real agent outputs (rich mid-season + cold-start `awaiting_data`).
  - **(corrected 10 Aug 2026 — `35 passed / 0 failed` is THE PLACEMENT FIGURE OF 11 JUL AND HAS NOT BEEN TRUE FOR WEEKS.** Re-run 10 Aug 2026: **`ALL CHECKS PASSED (91 passed, 0 failed)`**. This exact number is the one `CLAUDE.md` records itself being burned by — a repair pass copied `35 passed / 0 failed` out of *this file* into canon as if it were live. Keeping it here is correct (it is what happened at placement); **quoting it forward is not.** Read the count live, always: `node scripts/manager.mjs selftest`.)**
- **Re-tested against REAL agent JSONs (not §10 dummy)** — found + FIXED two real-data schema mismatches: (1) Goalkeeper `readiness.json` uses `day` / `workType[]` / `timing{}` (not `date` / `work_type_overlay` / timing-string) → wrapper reads the real fields, renders the timing object to a one-line SHAPE, puts workType in ENERGY, no more `[object Object]`; (2) UTC `toISOString` vs the agents' LOCAL `date` → wrapper now uses a **LOCAL date basis** so today-dated agents read `fresh`. Also: `STATE_DIR` is **script-anchored** (`join(__dirname, "..", "dressing-room", "state")`) like fsrs/calibration — **CWD-independent (VERIFIED in the placed file)**; readiness staleness is **Oura-lag tolerant** (≤2d via `day`).
  - **(re-verified 10 Aug 2026 — EVERY claim on this line still holds in the live file, which is worth saying out loud because the file has changed eight times since.** The real-field read: the live `readiness.json` keys are `ok, engine, day, mode, nights, verdict, ceiling, workType, timing, signals, tiers, medication, periodization, safety, guardrail` — so `day` / `workType[]` / `timing{}` are exactly as this line describes, and `timing` is genuinely an object (the `[object Object]` bug was real). Script-anchoring: `grep -n "DEFAULT_STATE" scripts/manager.mjs` → `join(__dirname, "..", "dressing-room", "state")`, with `stateDir || process.env.ARSENAL_STATE_DIR || DEFAULT_STATE` as the resolution order — CWD-independent, env override optional. Oura tolerance: `grep -n "Oura sync lag" scripts/manager.mjs` → `lag >= 0 && lag <= 2` ⇒ `fresh`. **One thing this line could not have known:** staleness is now ENFORCED, not merely logged (E2E audit, 25 Jul 2026) — a stale source degrades down the same null path a missing one does. `grep -n "STALENESS IS NOW ENFORCED" scripts/manager.mjs`.)**
- Design honored: **bias-to-silence propagates up** (a null / `awaiting_data` agent field ⇒ NO line); consumes Nemesis `headline` / `axis_pattern` **verbatim** (Fork A3 — not re-derived); **LLM = plugged stub** (M-1 sets NO API/key ⇒ always falls back — the sheet appears **unconditionally**); **zero-hallucination validator** (every number in the sheet ∈ FEATURES, else reject → fallback); atomic temp→rename writes.
  - **(corrected 10 Aug 2026 — "LLM = plugged stub" describes M-1 ONLY; the socket has had a real plug in it since M-3 went live.** `runManager({ llm })` still defaults to `llm = async () => null` with `reason = "no-llm (M-1)"` (`grep -n "no-llm (M-1)" scripts/manager.mjs`) — that is the FALLBACK, unchanged and still guaranteeing a sheet. But `brain.mjs`'s `manager_m3` job now passes a real `llm` that shells `claude -p` with `system.md` prepended (`grep -n 'job.kind === "manager_m3"' -A8 scripts/brain.mjs`). **So "always falls back" is no longer the live behaviour** — `manager_notes.json` records the day's real `source`, and a fallback morning today means something degraded, not that the design is dormant. The other four clauses re-verified and unchanged: the validator, the verbatim Nemesis consumption, bias-to-silence, and `writeAtomic` temp→rename. `manager.mjs` is also confirmed the SOLE writer of `team_sheet.md` — `grep -rn "team_sheet.md" scripts/*.mjs | grep -i writeAtomic` returns `manager.mjs` and nothing else, which is exactly what `brain_config.json`'s surface note claims.)**
- Real run (2026-07-11 live state): sheet rendered · `[source=fallback · no-llm (M-1)]` · staleness = readiness/cards/calibration/weaknesses/learning_state **fresh**, timeaudit `stale(2026-07-09)` (honest — not run since). SHAPE = real peak windows, ENERGY = ENCODE directive, no `[object Object]`.
- `.gitignore`: `team_sheet.md` · `manager_notes.json` · `season.json` · `captain_note.md` · `post_match/` — added + `git check-ignore -v` verified.
  - **(re-verified 10 Aug 2026 — ALL FIVE STILL IGNORED,** at `.gitignore:69`, `:70`, `:71`, `:72`, `:73` respectively. This family was NOT touched by the 5 Aug D10 ruling that released `reps_log.jsonl` and `learning_state.json`. Re-check per file rather than per block: `git check-ignore -v dressing-room/state/team_sheet.md`.)**
- **Record-correction:** the `1d4e158` commit message note "state dir CWD-relative → M-5 needs `Start In` = repo root" described the FIRST (superseded) placement `072baa8`. The corrected `f35c9f38` is script-anchored, so **M-5's scheduled task is CWD-independent — no `Start In` / `ARSENAL_STATE_DIR` required** (same as every other ArsenalFC task).
  - **(NOT VERIFIED 10 Aug 2026 — `072baa8` DOES NOT EXIST IN THIS REPOSITORY. `git cat-file -t 072baa8` → "Not a valid object name"; it appears in no branch, no tag and no reflog, and `git log -- scripts/manager.mjs` lists `1d4e158` as the FIRST commit ever to touch that path. Most likely it was a local commit amended or rebased away before the push. Treat the hash as unresolvable — do not go looking for it, and do not delete the record either: the CONCLUSION it supports was independently confirmed today (`DEFAULT_STATE` is `__dirname`-anchored, so no `Start In` is required). Only the citation is dead, not the claim.)**
- Remaining: **M-2** `system.md` soul (THE_MANAGER + THE_GAFFER — captain approves line-by-line) → **M-3** `claude -p` wiring + billing guards (`ANTHROPIC_API_KEY` never set + Extra-Usage OFF = hard $100 ceiling; swap the stub) → **M-4** sandbox the §11 scenarios → **M-5** scheduled task 08:45 + ntfy.
  - **[CORRECTED 10 Aug 2026 — THIS "REMAINING" LIST IS THE SINGLE MOST MIS-PLANNING-PRONE LINE IN THE FILE. Live status of each, read off code, not docs:]**
    - **M-2 — BUILT (complete draft), REVIEW STILL OWED.** `dressing-room/manager/system.md` is 586 lines with all eleven sections of the STRUCTURE list present — **but count them carefully, because the heading levels differ:** `grep -n "^#" dressing-room/manager/system.md` returns ONE `#` (line 18, "THE GAFFER — Manager (Agent #1) · Opus system prompt", which IS section #1 IDENTITY — it has no `## IDENTITY` heading of its own) plus TEN `##` sections (THE INVIOLABLE LAW · HOW YOU THINK EACH MORNING · THE RECONCILIATION · THE FORMATION-READ · PRECEDENCE · THE SEASON ARC · THE SOUL/VOICE · VERIFIED CANON+HARD-FLAGS · THE OUTPUT CONTRACT · THE EXEMPLARS) = 1 + 10 = the eleven. A bare `grep -c "^## "` returns **10** and will look like a missing section if you forget IDENTITY is the H1. Sections #6–#11 were drafted at commit `949b08e`, 12 Jul 2026. **The captain's line-by-line review is the part genuinely outstanding**, and the file's own header says so.
    - **M-3 — LIVE, RUNNING DAILY.** `brain.mjs` handles `job.kind === "manager_m3"`: it reads `SYSTEM_MD` off disk, prepends it to the wrapper's FEATURES block, shells the model, slices the sheet, and pushes ntfy. `brain_config.json`'s `formation_read` job: `engine: "claude", model: "opus", at: "08:45", enabled: true, max_per_day: 1, window: "any"`. The **billing guard is real** — the design's `ANTHROPIC_API_KEY`-never-set rule is enforced in code, not merely intended. Verify: `grep -n "manager_m3" scripts/brain.mjs dressing-room/state/brain_config.json`.
    - **M-4 (sandbox the §11 scenarios) — PARTLY EVIDENCED, NOT CLEANLY CLOSED. (NOT VERIFIED 10 Aug 2026: I could not find a step that says "M-4 done".)** What IS in code: `manager.mjs` names §11 Example A and drives a cold-start Matchday-1 fixture through the real code path (`grep -n "§11" scripts/manager.mjs`), and the 91-check selftest exercises rich, cold, stale, warming-up and validator-reject paths. Whether that satisfies what M-4 meant by "sandbox the §11 scenarios" is **the captain's call, not mine.** Treat as a claim.
    - **M-5 — DONE, BUT IN A DIFFERENT SHAPE THAN PLANNED; do not go create an `ArsenalFC-Manager` task.** There is no such row — 50 `ArsenalFC-*` tasks existed on 10 Aug 2026 and none is named Manager, but **count and search live rather than trusting that 50**: `Get-ScheduledTask | ? TaskName -like 'ArsenalFC-*' | ft TaskName,State`. The 08:45 slot is now the `sheet` step of `conductor.mjs morning` (`grep -n '{ id: "sheet"' scripts/conductor.mjs` → `at: "08:45", args: ["scripts/brain.mjs","tick"], needs: ["goalkeeper","learningstate"]`), plus `ArsenalFC-BrainTick` (`Ready`, every 30 min) as the second path in. The ntfy half is real and in `brain.mjs`'s `manager_m3` branch (`grep -n "SHEET_PUSH_TITLE" scripts/brain.mjs`), with an absence bell on the other side. One nuance worth knowing before touching the schedule: `formation_read`'s `window` was widened from `"morning"` to `"any"` at audit #108 because this laptop sleeps through most of 07:30–12:00 and the sheet had frozen at 2026-08-01 — `at: "08:45"` is now the EARLIEST time, not a fixed one. That reasoning is written into `brain_config.json`'s own `_window_note`; read it before narrowing the window again.

## Manager (capstone) — M-2 system.md IN PROGRESS (elevated build) · 2026-07-11
Capstone step 2 of 5 (§9 SPLIT Part 2 — the Gaffer soul / Opus system-prompt). Built section-by-section, captain-approved line-by-line, at an ELEVATED "ceiling" bar (build god-tier, output honest — CONDUCTOR §1). system.md = THE_MANAGER v2.1 + THE_GAFFER v2.1 DISTILLED into a runtime Opus system-prompt — NOT a verbatim concat (cut: v2-changelogs, provenance, §10 dummy-state, audit-markers).
- STRUCTURE (11 sections; re-ordered for the LLM's cognition + 2 NEW procedural sections the docs don't have): 1 IDENTITY · 2 THE INVIOLABLE LAW · 3 HOW YOU THINK EACH MORNING (NEW cognition scaffold) · 4 THE RECONCILIATION (NEW two-brain clash procedure) · 5 THE FORMATION-READ · 6 PRECEDENCE · 7 THE SEASON ARC · 8 THE SOUL/VOICE (+governor-lens) · 9 VERIFIED CANON+HARD-FLAGS · 10 THE OUTPUT CONTRACT · 11 THE EXEMPLARS.
- LOCKED + committed: #1 IDENTITY (two-brain anchor-model + backdrop + #14 bond) · #2 THE INVIOLABLE LAW (6 laws — no-invented-number[+wrapper-validated] · Governor-absolute[RED cancels · convergence-not-hours · akathisia hard-block · no med-interpretation] · 3 honesty-overrides · canon-fidelity/no-fabricated-quotes · no-calendar-pressure · scope) · #3 HOW YOU THINK EACH MORNING (8-step morning cognition scaffold: body-gate → rest-of-bus → formation-read → two-brain grade+reconcile → precedence → one-thing+bench+floor → intensity-dial → re-check+write; threads #4/#5/#6 by pointer, no duplication; free-man surfaced in #5 and selected in step 6; closing 'structure sets him free' = §6.5 EF-offload; verbatim captain-approved, elevated ceiling bar) · #4 THE RECONCILIATION (detailed two-brain clash procedure — the 'how' behind #3's step-4 summary: human-override-first [any shame/threat/agitation/shutdown → human anchor takes framing+intensity on the spot; mood/agitation → doctor, never a self-interpreted verdict] → find-the-root [game-root vs human-root; anchor = who LEADS, not who is abandoned — both brains stay in the room, e.g. Pep rests the best player to win the season] → Governor breaks any remaining tie on the body, without appeal; constant manner of EVERY resolution = HOLD-THE-WHAT-SOFTEN-THE-HOW [standard never drops, only delivery/framing/pace bends — honesty-override (i) made procedure] → out = ONE voice, seams hidden, no committee indecision; requisite-variety rationale in-voice, no jargon; forks resolved: 'hold WHAT / soften HOW' locked + #4 stays general, worked instances reserved for #11; captain-approved verbatim at the ceiling) · #5 THE FORMATION-READ (the daily positional read — three questions answered before any choice: weak handoff [which connection is fraying · "nodes break after the pass does" · = candidate for the one thing] · out of position [a signal firing in the wrong zone · = candidate for the bench] · free man [Pep's circulate-until-a-gap · never-zero floor located before kickoff · = the floor]; the three are produced raw material the morning consumes, not notes — ranking deferred to #6, clash-resolution to #4, reading ≠ deciding; ONE approved Maidan line ties the same three reads to the learning formation [weak handoff = drill the CONNECTION not the concept, tokenization→embeddings]; #3's anti-averaging one-liner expanded into a live self-check [a stack of bullets each off one signal = you averaged = Good-Regulator fail; name the one connection the day turns on]; captain-approved verbatim at the elevated ceiling).
- DESIGN CALLS locked: distill-not-concat · 2nd-person "You are the Gaffer" · Joe Hart verbatim quote reserved for #9 (pointer only in #1) · bias-to-silence baked in law #1 · trophy-line-vs-no-pressure reconciled in law #5 (days-to-ship = cabinet-light, not countdown) · med signal-confidence detail (RHR/HRV/temp = LOW) deferred to #8.
- OPEN at #8: meds by-name vs generic "your stack" (system.md is PUBLIC-committed) — captain decides.
  - **(CORRECTED 10 Aug 2026 — RESOLVED, and resolved the safe way. Do not re-open it with him.** The decision landed at commit `949b08e` (12 Jul 2026), whose message states it outright: *"#8 SOUL+VOICE (meds-PII resolved: generic 'his stack')"*, and `system.md`'s own header repeats it — *"meds-PII resolved: generic 'his stack', never a medication name — public repo"*. Verify: `grep -n "his stack" dressing-room/manager/system.md`. **A no-medication-names rule in a PUBLIC-committed prompt is a privacy boundary, not a style preference — treat it as settled and do not soften it.**)**
- Scaffolding (top-of-file comment + section numbers) STRIPPED at M-2 completion, before M-3; #1 light-tighten then.
  - **(CORRECTED 10 Aug 2026 — THIS DID NOT HAPPEN, AND M-3 SHIPPED ANYWAY. The scaffolding is still there.** `sed -n '1,16p' dressing-room/manager/system.md` shows the full `<!-- BUILD STATUS: M-2 COMPLETE-DRAFT … -->` comment, including the line *"this scaffolding comment + the section numbering are STRIPPED after the captain's review pass"* — which reveals the plan actually MOVED: the trigger became **his review pass**, not M-2 completion, so the strip is correctly still pending on a gate he has not walked through. It is not a missed step. **Practical consequence, and the reason this is worth flagging:** `brain.mjs` feeds this file to Opus WHOLE and verbatim, so those sixteen scaffolding lines are in the model's system prompt every single morning — including a line that tells it the file is an unfinished draft. Harmless-looking prose, live in production. **His call whether that matters; do not strip it unilaterally — the header names his review as the gate.**)**
- RESUME: continue M-2 from #6 PRECEDENCE.
- [9 Aug 2026, launch audit] That RESUME line rotted: system.md is COMPLETE at 586 lines
  (every section through THE EXEMPLARS), M-3's claude -p runs LIVE in brain.mjs (manager_m3,
  billing guard, formation_read daily 08:45 Opus). What actually remains his: the line-by-line
  captain review of system.md, and any §11-sandbox hardening he still wants.
- Files: dressing-room/manager/system.md (#1+#2+#3+#4+#5 locked · pending markers #6–#11 in header comment). Commit #3: 50c7b29 · Commit #4: 2dfa6bd · Commit #5: <hash on push>.
- **(corrected 10 Aug 2026 — two repairs on this line.)**
  **(1) `<hash on push>` was never filled in. Commit #5 is `89496c8`** — "M-2 #5: THE FORMATION-READ locked (system.md) + CONDUCTOR_LOG ledger fill (#4 hash 2dfa6bd, RESUME → #6)", 11 Jul 2026. Commits #3 `50c7b29` and #4 `2dfa6bd` both verified live and correct. The full section chain, for anyone tracing it: `1ca48e2` (#1+#2) → `50c7b29` (#3) → `2dfa6bd` (#4) → `89496c8` (#5) → `949b08e` (#6–#11, COMPLETE-DRAFT) → `34aeba6` (E2E audit repair, 26 Jul). Read it live: `git log --oneline -- dressing-room/manager/system.md`.
  **(2) "pending markers #6–#11" is stale — #6–#11 were written at `949b08e`, 12 Jul 2026,** the day after this line. All eleven sections exist today; the header's word for their state is DRAFTED, not LOCKED, because his line-by-line review is still owed. Count the sections live, never from here — and use `grep -n "^#"`, not `grep -c "^## "`, for the reason spelled out in the M-1 block above (section #1 IDENTITY is the H1, so the `##` count is 10, not 11).
- **(added 10 Aug 2026 — closing note on this block's RESUME line, which the 9 Aug annotation above already caught.)** The 9 Aug correction stands and was re-verified today from code: `system.md` = 586 lines, `manager_m3` live in `brain.mjs`, `formation_read` enabled at 08:45 on Opus. Nothing here needed reversing. **The lesson this block should carry forward:** "RESUME: continue M-2 from #6 PRECEDENCE" was a true instruction for exactly ONE day, and then spent roughly four weeks telling every fresh session to rebuild sections that already existed. **A resume-pointer written into an append-only ledger is a landmine.** If you leave one, date it and say what would make it false.
