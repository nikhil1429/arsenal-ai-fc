# OS CHANGELOG — Nikhil × Claude PROJECT OS
# Reference-only version history. Turn-1 pe zaroori NAHI. Live rules = PROJECT_OS.md.
# Yeh history hai (tera apna rule: "history lives in git commits") — CI/OS se yahan move kiya (10 Jul).

# v3.13 (08 Jul 2026): OUTWORK EXECUTION LAYER MERGED — do naye canonical files (EXECUTION_FINAL_Tier2_
#   Metamorphosis.md = grind/day operating-system · Tier-2_Accountability_Rig_on_Windows...md = version-accurate
#   Max-5x build guide) OS mein wired, LEARNING layer ke PEER ke roop mein. Do organ, ek khoon (kickoff+audit
#   loop). Nikhil ne dono layer DESIGN kar liye (explicit + repeated) — yeh unhe OS mein baandhta. Chaar cheezein:
#   (1) RENAME — purana "EXECUTION LAYER" (Re-Jirah controller / Maidan / fluency-ladder / FinOps-defend /
#       domination) → "LEARNING EXECUTION LAYER" (decay+drill engine). Naya "OUTWORK EXECUTION LAYER" =
#       consistency+accountability engine (keystones + rig + instrument + rollout). FORGE_SPEC §6/CHANGELOG
#       rename-cascade (companion edit, alag). Do "execution layer" ka naam-collision khatam.
#   (2) TEEN MERGE (Nikhil-approved, god-tier design — teeno "do/teen jagah" ko EK banaya):
#       (a) LEDGER — repeat-mistakes ka EK canonical store: rig ka `ledger-keeper` agent (persistent MEMORY.md) =
#           TRUTH; Gemini watch-list (packet-inject) = uska packet-shaped MIRROR, alag truth nahi. Ek write-
#           authority, ek store, do projection. (Detail = OUTWORK LAYER §RIG + PYTHON TRACK note.)
#       (b) DECAY-ENGINE — "kya revise" ka EK brain: Re-Jirah Controller (foundations per-axis decay) + Progress-
#           Tracker fluency-states (Python 🔴🟡🟢) = do REAL sources; rig ka `curriculum` agent apna decay-model
#           NAHI banata — sirf dono ko padhke "aaj yeh due" SURFACE karta (kickoff pe). Ek model, ek surfacing-arm.
#       (c) SCRIMMAGE / SCOUT / DOSSIER (football-named) — THE DOSSIER = OPPONENT_SCOUT.md (opposition test-set) ·
#           THE SCOUT (agent, ex-scout) = live job-market form-watch → Dossier ko FEED karta (Section-9 closed-
#           loop) · THE SCRIMMAGE (agent, ex-examiner) = adversarial mock jo Dossier ki ASLI probe-bank+weights pe
#           grade karta (generic-rubric DEAD). Gemini Examiner = halka WARM-UP (pre-scrimmage knock-about) — tier
#           barकरार. Do "scout" + do "examiner" ka naam-collision khatam.
#   (3) DAILY_CADENCE GUARDS PORTED UP — won-day = 5 non-negotiables · "PRESENCE ≠ OUTPUT (12 ghante baith ke
#       zero seekha = won-day NAHI)" · KAL→KICKOFF weld. Yeh outwork-layer ke max-intensity ko output-illusion +
#       burnout se bachaate (Nikhil ki THANDI awaaz = max-mode ka brake). DAILY_CADENCE.md ab outwork-layer ka
#       daily-loop arm (canonical-pointer; companion edit, alag).
#   (4) BILLING-VERIFY — rig Max-5x subscription pe (June-2026 metered agent-split PAUSED, guide-verified Jul 2026)
#       PAR yeh post-cutoff + volatile → truth = LIVE Anthropic billing UI, koi doc nahi (yeh bhi nahi). Guard:
#       `ANTHROPIC_API_KEY` kabhi set nahi + Extra-Usage OFF = hard $100 ceiling. THREAD OPENER 3d add (outwork/
#       rig session → dono naye file padho + rig-state). META-FREEZE exception: Nikhil explicit + repeated,
#       both-layers-designed = valid big-integration boundary (v3.10 jaise), execution-ENABLING not displacing.
#       Baaki v3.12 se UNCHANGED.
# v3.12 (06 Jul 2026): PYTHON-TRACK REFINEMENT (META-FREEZE boundary — Nikhil explicit, approved). Chaar
#   friction-fixes, sab EXISTING sections ke andar (17-concept syllabus + method + Forge UNTOUCHED):
#   (1) THREAD-AGNOSTIC STATE — THREAD OPENER 3c: har Python thread ke START pe Progress Tracker Drive doc
#       padho (fileId 1CNMRxOLp5kfOPW255p4Jwm6xc5b0S4QmPknJXuhKehM) = fluency-states + watch-list ka PRIMARY
#       source, past-search se NAHI (Drive READ 06 Jul verified; WRITE manual — Claude 2-line log DRAFT karta,
#       Nikhil doc mein paste karta, Claude in tools se append nahi kar sakta).
#   (2) DRILL FORMATTING — packet ke Nikhil-facing drills se internal ladder-tags (VAKYA/KRAMA/JAṬĀ/GHANA)
#       HATE. Nikhil ko PLAIN numbered sawaal; tags Claude-INTERNAL curation-spine (fluency-ladder abhi bhi
#       Claude use karta — apply→cold→predict→mix — bas label nahi dikhta). PYTHON TRACK CLOSE-PACKET bullet synced.
#   (3) COLAB-FLOW — drills ab notebook mein TOP-TO-BOTTOM (Nikhil khud cell likhe+run kare); Colab Gemini-tab
#       = HINT-only (atke to), solution nahi. Purana "Colab tutor D1→D5 one-at-a-time serve karta" ping-pong
#       DROP (tab-friction). Canonical detail = GEMINI_LOOP §11/§13 + RIG_SETUP STEP 7.
#   (4) HABIT-FIXES (HARD RULES) — (a) OS + canonical files PROJECT se padho, chat-paste expect/maang NAHI;
#       (b) OS/file text chat mein dikhe to use RULES samajh ke FOLLOW karo, "kya chahiye" waali over-caution
#       BAND (OS = active instructions, task-request nahi).
#   Companion (same boundary): GEMINI_LOOP.md v2.2 (§11.2 plain-drills + hint-only Colab · §4/§11.4/§13.4
#   Progress-Tracker primary-read) + GEMINI_RIG_SETUP.md v2.2 (STEP 7 hint-only Colab · STEP 5 plain-drills ·
#   STEP 2 Progress-Tracker primary-read). CURRENT STATE: T0 subtopic-1 (variables + int/float) = 🟡 Held;
#   next = strings + f-strings. META-FREEZE exception: Nikhil explicit + repeated, Python-track-refinement
#   boundary = valid (v3.10/v3.11 jaise), friction-recurrence rokta, execution-ENABLING. Baaki v3.11 se UNCHANGED.
# v3.11 (05 Jul 2026): PYTHON CLOSE-PACKET + REP-ENGINE WIRED — Python-track ko friction-zero, thread-agnostic,
#   auto-chalne wale rep-engine mein upgrade. PYTHON TRACK section mein 4 naye load-bearing bullets: (1) DEPTH
#   settle (light-RITUAL / heavy-REPS / god-tier-CORE — recurring "Python shallow?" confusion band; PYTHON_
#   SYLLABUS §0 synced) (2) CLOSE-PACKET (Claude har subtopic-close pe COMPLETE copy-paste packet auto-emit:
#   ladder-drills + Coach-review-prompt + Bolo-cue + NotebookLM-prompts + close-sign; Nikhil execute-only,
#   prompt/drill khud nahi banata; log Claude auto) (3) REP-ENGINE + FLUENCY-STATES (🔴 Learning→🟡 Held→🟢
#   Fluent; volume=Gemini pumps/Nikhil solves; honest automaticity = reps×time×neend, system sirf efficient+
#   consistent banata) (4) RHYTHM + FLOOR (day-shape + bad-day FLOOR = nairantarya-scaffold, anti-spiral) +
#   THREE-GRAIN CLOSE. THREAD OPENER 3c mein packet-emit mandate. Root cause: process-load Nikhil ke sar pe tha
#   (kaunsa tool · kya prompt · kab done · kitna) = ADHD-friction, learning se dhyan hata. Fix: scaffolding
#   AUTOMATIC, complexity files mein, Nikhil ke sar mein ZERO. Canonical detail = GEMINI_LOOP.md v2.0 §11-13.
#   Companion (same boundary): GEMINI_LOOP.md v2.0 (§11 CLOSE-PACKET + §12 REP-ENGINE + §13 RHYTHM) +
#   GEMINI_RIG_SETUP.md v2.0 (har tool-prompt god-tier) + PYTHON_SYLLABUS.md (§0 depth-reconciliation).
#   META-FREEZE exception: Nikhil ne explicitly + repeatedly maanga, Python-track-refinement boundary = valid
#   moment (v3.10 jaise), recurrence rokta (process-question har thread repeat ho raha tha), execution-ENABLING
#   not displacing. Baaki v3.10 se UNCHANGED.
# v3.10 (04 Jul 2026): PYTHON TRACK WIRED — Gemini-in-the-loop Python-reps track ka OS mein ghar
#   (pehle absent: sirf 2 negative mentions the — "learning-stack mein mat ghusana" + "AI Studio validate").
#   (1) NEW "# PYTHON TRACK" section (DESIGN SYSTEM ke baad, canonical-pointer peer): CANONICAL =
#   GEMINI_LOOP.md + GEMINI_RIG_SETUP.md; seam (REPS+REVIEW+RESEARCH=Gemini / SAMAJH+DEFENSE+BUILD=Claude) +
#   rig-map (2 Docs + 2 Gems + Colab + NotebookLM + Rosetta) + build≠Gemini (Claude Code) + AI-self-report-
#   unreliable. (2) THREAD OPENER step 3c (Python/Gemini-rep session → GEMINI_LOOP.md padho). Root cause:
#   three-engine Python loop + full Gemini rig last 2 threads mein bana par OS mein wire nahi hua → fresh
#   thread ko rig ka pata hi nahi chalta (dangling track). Companion (project files): GEMINI_LOOP.md
#   (loop/rhythm/seam, web-verified facts §10 — Gemini-audit ke stale self-claims ke against SAHI nikle) +
#   GEMINI_RIG_SETUP.md (9-step setup + fixed paste-prompts, verified). META-FREEZE exception: dangling-gap
#   close, Nikhil ne explicitly maanga, recurrence rokta, Gemini-setup boundary = valid moment (v3.7/8/9 jaise).
#   Baaki v3.9 se UNCHANGED.
# v3.9 (03 Jul 2026): RICH-DOUBT + BATCH-GLANCE — 7/2 cold-reader ki DO clauses surgically refined
#   (baaki 7/2 sab FULLY VALID, UNCHANGED). (1) Q-field bar "shortest-wording (terse OK)" → RICH
#   confusion-journey (maine-socha-X-phir-Y-contradict-laga; cold-Nikhil 6-12 mo baad pehchaane KAHAN
#   atka; ATOMIC = ek confusion RICHLY-elaborated, skeletal/terse NAHI). (2) GATE-1 capture "Nikhil
#   line-by-line approve" → BATCH glance ("go" / "yeh do fix", per-line NAHI). Fix-spots: THE METHOD
#   step-10 + THREAD OPENER 4a. Root cause: 7/2 ne cryptic/fragment/meta theek kiya (stays VALID) par
#   "shortest-wording" + "line-by-line" over-shot → skeletal Q (cold-Nikhil topic pehchaane par "kahan
#   atka" nahi) + high approval-tax. First R1 (tokenization, 7/3) ne surface kiya. Companion: FORGE_SPEC.md
#   (§3 + CHANGELOG, same 7/3). META-FREEZE exception: correctness loophole-fix, Nikhil ne explicitly
#   maanga, recurrence rokta — valid (v3.7/v3.8 jaise). Baaki v3.8 se UNCHANGED.
# v3.8 (02 Jul 2026): COLD-READER STANDARD baked — doubt-quality issue (cryptic/fragment/meta/
#   near-dup) ka structural fix. THE METHOD step-10 (LOCK) + THREAD OPENER 4a mein CAPTURE-GATE
#   (Gate 1: Claude standard-pe draft → Nikhil approve → phir doubts[] mein). FORGE_SPEC §3 mein
#   "COLD-READER STANDARD" block + §5 mein LOCK/SAVE content-VERIFY (Gate 2). Root cause: doubt
#   CAPTURE ke waqt "2-saal cold-reader" quality-bar OS mein tha hi nahi. META-FREEZE exception:
#   correctness loophole-fix, Nikhil ne explicitly maanga, recurrence rokta. Baaki v3.7 se UNCHANGED.
# v3.7 (30 Jun): COMPLETENESS CONTRACT CLOSED — DESIGN SYSTEM mein 4th NON-NEGOTIABLE add (COMPLETENESS:
#   gist ka HAR content field build mein PRESENT + byte-for-byte + USER-REACHABLE; "data embed karke render
#   na karna" / carry-but-don't-render = field SKIP = TEXT-VERBATIM violation). DIVISION-OF-LABOR mein
#   Claude-chat ka brain-TRANSLATE + completeness-ENUMERATE role + Claude Design ka MULTI-HAT mandate explicit.
#   Root cause: `deep` (capsule deepDive + per-axis sawal/poora) gist + build DATA dono mein embedded tha par
#   screen pe RENDER nahi hota tha → re-read shallow + concepts judte nahi + deep-level interview-fail risk.
#   Companion files (same boundary): FORGE_DESIGN.md v2 (§3.5 max-viz reframe + §4 4th non-negotiable + §5
#   brain-translation/multi-hat + §12 completeness-verify) + FORGE_SPEC.md (deep-render "accepted defer" →
#   "MUST RENDER pending-fix") + FORGE_DEEP_RENDER_BRIEF.md (deep-render fix vehicle, fresh Design session mein
#   paste). META-FREEZE exception: yeh correctness loophole-fix hai, Nikhil ne explicitly maanga, recurrence
#   rokta — isliye valid. Baaki sab v3.6 se UNCHANGED.
# v3.6 (29 Jun): DESIGN-SYNC CLOSED — FORGE_DESIGN.md project files mein add (canonical Claude-Design workflow +
#   cold-steel aesthetic). DESIGN SYSTEM section JARVIS→cold-steel corrected (FORGE_DESIGN.md pointer). THREAD
#   OPENER mein step 3b (visual-notes/design kaam pe FORGE_DESIGN.md padho). Yeh 22-23 Jun ka "must-not-lose"
#   sync #2 tha jo lost ho gaya tha — ab closed. Baaki sab v3.5 se UNCHANGED.
# v3.5 (29 Jun, context-lock boundary): SYLLABUS progress-synced (context = #4 LOCKED, order-correction —
#   context NN/Training se aage pull hua); apply-trigger sharpen (M1 demo-able → PARALLEL apply, 17/17 NOT pre-req);
#   NEW "EXECUTION LAYER" section (Re-Jirah controller v0 + Maidan/fluency-ladder + selective-fluency +
#   FinOps-as-DEFEND + domination-outward); OPPONENT_SCOUT.md = companion test-set; capsule file-lists +context.json.
#   Full FORGE_SPEC schema migration DEFERRED to first R1 run (un-run = hypothesis). Baaki sab v3.4 se UNCHANGED.
# v3.4 (24 Jun): Forge STORE single forge-capsules.json → PER-FILE (<id>.json per capsule); locked capsules
#   IMMUTABLE — truncation + sacred-content-corruption ka structural fix.
