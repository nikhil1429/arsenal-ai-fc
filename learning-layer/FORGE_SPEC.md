# FORGE_SPEC.md — Schema Lock (canonical)
> Yeh file schema FREEZE karti hai. Drift rokne ke liye. Koi bhi capsule isi shape mein banega.
> Engine (THE-FORGE.html) isi schema ko gist se padhta hai. Mismatch = blank render. Toh yahi master. *(corrected 10 Aug 2026: "mismatch = blank render" ab SACH NAHI. 9-Aug ke rebuild ke baad engine kabhi blank nahi hota — generator do cheezein carry karta: (a) ek baked SNAP jo hamesha pehle render hota, aur (b) ek GENERIC WALKER — jo field designed-sections ki list mein nahi hai woh bhi RAW render hota, skip nahi. Evidence: `grep -n "PLACED" setup/build_forge_html.mjs` (designed-field list) + `grep -n "Fields with no designed section yet" setup/build_forge_html.mjs` (walker ka section) + `grep -n "completeness:" setup/build_forge_html.mjs` (per-capsule footer jo rendered-vs-present ginta). Schema-mismatch ab blank nahi, **VISIBLE** hota. Gist phir bhi master — woh unchanged.)*
> Last locked: **context (concept #4, 2026-06-28).** 4 concepts locked: tok #01, emb #02, inf #03, context #04. *(corrected 10 Aug 2026: yeh aaj bhi SACH hai — verified live — par lock-count prose mein rakhna hi rot hai; agla lock ise chup-chaap jhoot bana dega. **Ginti hamesha LIVE karo, yahan se KABHI nahi:** `ls dressing-room/state/capsules/` (mirror.mjs ka read-only gist-copy) ya `node -e "const m=require('./dressing-room/state/mirror_manifest.json');console.log(m.counter,m.status,m.enumeration.ids)"`. 10 Aug 2026 ka read: `4/4 capsules mirrored · ok · [context, embeddings, inference, tokenization]`. Aur dhyaan: sach ka source gist ka apna LISTING hai, config nahi — `grep -n "enumerateGist" scripts/mirror.mjs`, jo `mirror_config.json` ki `ids[]` ko FLOOR maanta, truth nahi.)*
> **Schema update (2026-06-22, post-emb-lock boundary): 3-LAYER notes model added — `weld` + `deep` + `viz` (viz reserved). See §2.5 + §3.** Hinglish throughout.
> **Store update (2026-06-24, inference-lock boundary): single `forge-capsules.json` flat-array → PER-FILE (`<id>.json`, one capsule each). Schema UNCHANGED — sirf STORE + WRITE-PATH badla. See §1 + §2 + CHANGELOG.**
> **Progress-sync (2026-06-29, context-lock boundary): lock-count 2→4 (context #04 locked 6/28). Controller v0 fields RESERVED in §3 (per-axis + capsule-level; shape sketched, viz/heroViz jaise) — see §6. FINAL shape + constants + capsule-POPULATION at first R1. Locked capsules UNTOUCHED (immutable; R1 mein populate).**
> **Render-completeness (2026-06-30, mid-Re-Jirah, AUTHORIZED correctness-fix): `deep` (per-axis + capsule) RENDER = COMPLETENESS REQUIREMENT, defer NAHI. §4 adapter "NOT rendered yet" rows for `deep` → "MUST RENDER (pending fix)". Root cause: `deep` gist + build DATA mein embedded tha par screen pe render nahi hota → re-read shallow + interview-fail risk. Governing rule = FORGE_DESIGN.md §4 #4 (completeness contract). Schema DATA-shape UNCHANGED. See §4 NOTE + CHANGELOG.**
> **Cold-Reader Standard (2026-07-02, AUTHORIZED correctness-fix): §3 mein naya "COLD-READER STANDARD" block + `doubts`/`bridges` field-rules upgrade (ATOMIC · SUBJECT-named · answer-HIDDEN · no-near-dup · TAXONOMY). §5 mein "GATE 2 — CONTENT-VERIFY at LOCK/SAVE" rule. Root cause: doubt CAPTURE ke waqt "2-saal cold-reader" quality-bar spec mein tha hi nahi (sirf "har stuck-point capture karo") → ~1/3 doubts cryptic/fragment/meta (context ~73%). Companion: PROJECT OS v3.8 (THE METHOD step-10 + opener-4a = Gate 1, capture-time). Research locked: Wozniak 20-rules (R4/R11/R12) + Zettelkasten + Tulving + curse-of-knowledge — converge. Schema DATA-shape UNCHANGED (fields same; sirf QUALITY-BAR + verify-gate add). META-FREEZE exception: correctness loophole-fix, Nikhil ne explicitly maanga, recurrence rokta — valid (v3.7/v3.8 jaise). See §3 block + §5 GATE 2 + CHANGELOG.**
> **Rich-doubt + batch-glance (2026-07-03, AUTHORIZED correctness-fix — refines 7/2 over-tighten): §3 ki DO clauses surgically corrected. (1) Q-field bar `shortest-wording (terse OK)` → RICH confusion-journey (maine-socha-X-phir-Y; cold-Nikhil 6-12 mo baad pehchaane KAHAN atka; ATOMIC = ek confusion RICHLY-elaborated, skeletal/terse NAHI). (2) GATE-1 capture `Nikhil line-by-line approve` → BATCH glance ('go'/'yeh do fix', per-line NAHI). Baaki 7/2 cold-reader (cryptic/fragment/meta failure-patterns, TAXONOMY, subject-anchor, answer-HIDDEN, no-near-dup, Gate-2 verify) FULLY VALID, UNCHANGED. DATA-schema UNCHANGED. Root cause: 7/2 ne 'shortest-wording' + 'line-by-line' over-shot (skeletal Q + high approval-tax). First R1 (tokenization, 7/3) ne surface kiya. Companion: PROJECT OS v3.9. META-FREEZE exception (correctness loophole-fix, explicitly requested). See §3 + CHANGELOG.**
> **Rename cascade (2026-07-08, OS v3.13 outwork-merge companion): OS "EXECUTION LAYER" section → "LEARNING EXECUTION LAYER" (naya OUTWORK EXECUTION LAYER ka PEER banne pe do "execution layer" naam-collision khatam). Is file mein sirf woh 4 spots badle jahan OS us section ko NAAM se reference karta (§3 schema-comment + §6 header/body + 6/29 changelog line). Controller v0 DESIGN + SCHEMA (5 knobs + 4 mechanics + reserved per-axis/capsule fields) UNCHANGED — pure rename of the referenced OS section, koi schema-touch nahi. DATA-shape UNCHANGED. Companion: PROJECT OS v3.13. See §6 + CHANGELOG + §5 META-FREEZE ledger.**

---

## 1. STORE
- **Master** = GitHub Gist — **ONE JSON file per capsule** (`<id>.json`: `tokenization.json`, `embeddings.json`, `inference.json`, `context.json`, ...). Each file = a SINGLE capsule object (array wrapper NAHI). Raw URL pattern: `.../raw/<id>.json`. (Purana single `forge-capsules.json` flat-array **DEAD** — migrated per-file 2026-06-24.)
- Raw URL pattern: `https://gist.githubusercontent.com/nikhil1429/ce50c28d585c2fcd915a9dbf61871a56/raw/<id>.json` (e.g. `.../raw/tokenization.json`, `.../raw/embeddings.json`, `.../raw/inference.json`, `.../raw/context.json`).
- **Engine** = `THE-FORGE.html` — **ab REPO ROOT mein** (9 Aug 2026, uska word: "as of now
  forge.html mein hi rakho". Purani "laptop + git, NEVER project files" copy KHO GAYI — out-of-git
  ka wahi rot jo THE_GAFFER.md ke Drive-tag ne sikhaya tha; ab git hi engine ka ghar hai).
  - *Shipped (2026-08-09):* **intended shape** — live gist fetch per capsule + baked SNAP fallback
    (never-empty), HAR field render hota hai `deep`+`viz` samet, completeness footer ke saath
    (FORGE_DESIGN §4/§12). Rebuild: `node setup/build_forge_html.mjs`.
  - *(verified 10 Aug 2026 — is bullet ke har dawe ka apna grep, kyunki is file ne pehle bhi engine ke
    baare mein purana sach carry kiya hai: live-fetch → `grep -n "no-store" setup/build_forge_html.mjs`
    aur `grep -n "LIVE gist" setup/build_forge_html.mjs` · per-axis `deep` render → `grep -n "deep — poora khol" setup/build_forge_html.mjs`
    · capsule-level `deep` → `grep -n "god-tier re-learn layer" setup/build_forge_html.mjs` · `viz` →
    `grep -n 'sec("Viz"' setup/build_forge_html.mjs` · completeness footer → `grep -n "0 skipped" setup/build_forge_html.mjs`.
    Sab present. **Engine ka rebuild ek command hai — file ko haath se kabhi mat likhna.**)*
- **Never-empty**: engine has a baked `SNAP` (copy of the gist) so it renders even offline / if fetch blocked.
  *(precision added 10 Aug 2026: SNAP gist se SEEDHA bake nahi hota — generator `dressing-room/state/capsules/`
  se padhta hai, jo **mirror.mjs** ka read-only gist-copy hai (`grep -n "CAPS_DIR" setup/build_forge_html.mjs`).
  Toh chain = **gist (master) → mirror.mjs → capsules/ → SNAP → engine**, aur runtime pe engine phir bhi gist
  ko seedha fetch karta hai. Matlab: purane mirror pe rebuild = purana SNAP. Rebuild se pehle `node scripts/mirror.mjs`.
  Mirror us pull ka ek dated backup bhi rakhta hai — `grep -n "capsule_backups" scripts/mirror.mjs`.)*

## 2. WRITE PATH (Option A — manual, at concept LOCK) — PER-FILE
1. Concept Jirah-survive karta → Claude sirf us **naye** capsule ka file (`<id>.json`, single object). Locked files KABHI re-generate nahi (immutable).
2. Nikhil **'Add file'** → naam `<id>.json` → paste → Save.
2b. Existing capsule edit (`reJirahDone`/doubt back-write) → Claude us **EK** file ka updated version deta → Nikhil us file ko **replace**, baaki files untouched.
3. Engine boot pe gist `fetch()` karta → live render. (file:// pe fetch block ho to baked SNAP dikhta — Claude tab naya THE-FORGE.html de jisme SNAP refreshed ho.) *(~~Aspirational — current engine baked-only, §1.~~ **corrected 10 Aug 2026: yeh step ab ASPIRATIONAL NAHI, LIVE hai.** 9-Aug ke rebuild ka boot-order bilkul yahi hai: pehle SNAP se render (never-empty floor), phir har capsule ka gist raw URL parallel fetch, aur SAB safal hone pe hi CAPS swap + `srcmode` "LIVE gist" ho jaata — evidence `grep -n "SNAP is the never-empty floor" setup/build_forge_html.mjs` aur `grep -n "fresh.every(Boolean)" setup/build_forge_html.mjs`. Ek bhi fetch fail = SNAP hi rehta, aadha-live kabhi nahi. Aur SNAP-refresh ke liye Claude ab naya HTML "deta" nahi — `node scripts/mirror.mjs && node setup/build_forge_html.mjs`.)*
4. Nothing auto-saves. Nikhil decides.
   *(unchanged rule, precision added 10 Aug 2026 — ab is rule ke aas-paas do machine-lanes hain, aur dono ise TODTE NAHI: (a) `/gist-patch` skill Chrome mein gist ka edit-page khol ke `reJirahDone` ki EXACT patch-line pre-fill karti hai par Save **kabhi** nahi dabati — "HIS CLICK is the write" (`grep -n "DO NOT click" .claude/skills/gist-patch/SKILL.md`); (b) `mirror.mjs` gist se NEECHE khinchta hai, kabhi WAPAS nahi likhta — "the mirror never writes back — a one-way afferent nerve" (`grep -n "one-way afferent nerve" scripts/mirror.mjs`). Master pe likhne wala aaj bhi sirf uska haath hai.)*

## 2.5 LOCK-TIME CONTENT CAPTURE — 3 LAYERS *(added 2026-06-22, post-emb-lock boundary)*
Har concept LOCK pe teen content-layers capture hote — **layer, never replace.** Teeno zinda rehte:

| Layer | Density | Kya | Kab padha jaata |
|---|---|---|---|
| **1 · WELD** *(default, pehle se)* | explain + defend | Per axis `strike` (interviewer Q) + `weld` (defended answer). Quick recall-trigger + interview-defense. | Recall / Re-Jirah ke waqt |
| **2 · DEEP** *(new — gist mein 6/22)* | scratch-se-re-learn | Per axis `deep` + capsule-level `deep` (analogy + worked example + why-chain + stuck-story). | ~2 mahine baad jab concept dobara re-learn karna ho |
| **3 · VIZ** *(RESERVED — aa raha)* | visual | Per axis `viz` + capsule-level `heroViz` (mechanism + exact values + suggested form). | Animation se seekhne / refresh ke waqt |

**DEEP capture rule (non-negotiable):** `deep` content **sirf** Nikhil ke Bolo / teaching-threads se aata, **verbatim-faithful**. Claude **invent kabhi nahi** karta — na axis weld, na deep, na koi example. Lock pe Claude threads/Bolo se recover karta → **Nikhil line-by-line verify** karta uske gist-canonical hone se pehle. *(Kyun: yeh woh content hai jo Nikhil interview mein khud defend karega — reword/invent = woh apni-samajh nahi rata.)*

**VIZ capture (Part-2 mein finalize):** mechanism + exact values = **Claude-locked**; visual/UX/flow = **Design-free** (coverage-not-density). Exact `viz`/`heroViz` schema **abhi over-define NAHI** — Design ka build firm hone pe (premature lock se bachna).

> **RENDER-COMPLETENESS NOTE (2026-06-30):** Layer capture (gist mein content daalna) aur layer RENDER (build mein screen pe dikhana) DO alag cheezein hain. `deep` capture ho chuka (gist + build data dono mein), par RENDER nahi hota tha — yahi gap tha. **Capture ≠ render.** Build mein har captured content field RENDER hona ZAROORI (completeness — FORGE_DESIGN.md §4 #4). Carry-but-don't-render = field skip.
>
> **CLOSED (verified 10 Aug 2026):** yeh gap ab khula nahi hai. Naya generator dono `deep` render karta — per-axis ek click-reveal `<details>` mein, capsule-level apne section mein (`grep -n "deep — poora khol" setup/build_forge_html.mjs`, `grep -n "god-tier re-learn layer" setup/build_forge_html.mjs`). **Aur fix sirf `deep` tak seemit nahi raha — usse behtar hua:** ab koi bhi ANJAAN field chup-chaap skip ho hi nahi sakta, kyunki jo key designed-list mein nahi hai woh raw render hoti hai aur footer har capsule pe "designed + raw — 0 skipped" ginta hai (`grep -n "Fields with no designed section yet" setup/build_forge_html.mjs`). Yani 6/30 wala scar ab CODE mein band hai, prose ke bharose nahi. **Rule khud unchanged rehti — capture ≠ render, aur render lazmi hai.** (Neeche §4 ke adapter rows aur uska 6/22 NOTE 9 Aug tak yeh gap khula bataate rahe; wahan bhi theek kar diya.)

## 3. CAPSULE SCHEMA (exact — har capsule object)
```json
{
  "id": "tokenization",            // long id. (corrected 10 Aug 2026: "engine IDMAP se short id (tok) pe map hota" ab GALAT — engine mein IDMAP hai hi nahi. `grep -rn "IDMAP" --include=*.mjs --include=*.html .` = ZERO hits. Naya engine `id` ko seedha use karta: nav-label, aur gist raw URL `<GIST>/<id>.json`. Short ids sirf test-fixtures mein bache hain. Field-rules + §4 mein bhi theek kiya.)
  "num": "01",
  "lockedOn": "2026-06-15",        // ISO date concept LOCK hua. Re-Jirah schedule isi se compute
  "reJirahDone": [],               // completed Re-Jirah due-dates (ISO array). Re-Jirah ke baad Claude add karta
  "stream": "foundations",         // foundations | courses | finops
  "title": "Tokenization",
  "status": "tempered",            // JIRAH ka result (self-rating se NAHI). number optional ("tempered-90")
  "dot": "magenta",                // cyan=primary · red=crack · magenta=tempered
  "source": "...",
  "why": "one line — build + interview relevance",
  "hook": "business cliffhanger (definition nahi)",
  "mechanism": "Nikhil ke locked words mein, crisp",
  "deep": "CAPSULE-LEVEL re-learn (markdown string). Sections: ### HOOK + ### MECHANISM + ### THE ONE PICTURE. hook/mechanism se FULLER. Verbatim-faithful from threads/Bolo. (Added 6/22.)",

  "faultLines": [                  // 9 axes, axis a..i. status JIRAH se.
    {
      "axis": "a", "title": "...",
      "strike": "interviewer Q",
      "weld": "defended answer (quick recall-trigger)",
      "status": "held",
      "deep": "PER-AXIS re-learn (markdown string). Sections: **SAWAL:** + **POORA JAWAB:** (analogy + worked example + why-chain + stuck-story). Verbatim-faithful. Layers ON TOP of weld — replace NAHI. (Added 6/22.)"
      // "viz": { ... }            // RESERVED — per-axis visualization spec (mechanism + exact values + form). Finalize Part-2 (post-emb-viz ship). ABHI gist mein nahi.
      // ---- CONTROLLER v0 per-axis fields (RESERVED 6/29; FINALIZE + POPULATE at first R1; constants in OS LEARNING EXECUTION LAYER, v0) ----
      // "axisType":       "recall" | "reconstruct" | "defend",          // kis tarah test ho (classification — R1 pe assign)
      // "nextDue":        "ISO date",                                   // per-axis next Re-Jirah (global +3d/+2wk/+6wk replace; SM-2-lite). RUN-OUTPUT.
      // "lastResult":     "held-clean" | "held-struggle" | "cracked",   // RUN-OUTPUT
      // "calibrationGap": "predicted-vs-actual (control-signal: confident+cracked → tighter interval + mode-bump)",  // RUN-OUTPUT
      // "fluencyState":   "tempered" | "fluent"                         // dṛḍhabhūmi; SIRF load-bearing core pe. RUN-OUTPUT (high-rep ke baad).
    }
    // ... b..i. axis i = 3-awaaz drill.
  ],

  "threeWays": { "ceo": "...", "junior": "...", "skeptic": "..." },   // axis-i ka 3-nazariye

  "traps":  [ { "bait": "seductive wrong (quoted)", "wrong": "kyun galat", "truth": "correction" } ],
  "bridges":[ { "to": "embeddings", "conn": "x -> y", "q": "cross Q", "a": "connection" } ],
  "doubts": [ { "q": "Nikhil ka exact stuck-point (uske shabd)", "a": "crisp resolution = interview answer" } ],
  "calibration": "predicted-vs-actual gap + freshness note",
  "buildHook": "FinOps mein exact spot + defend-able decision",
  "interviewLines": [ "tight English line, cold bol-ne layak" ],
  "bolo": "Nikhil ke apne words — whole concept. Claude invent NAHI karta.",
  "bolo_by": "— Nikhil, Jirah-tested ...",

  "viz": "bpe",                    // LEGACY (capsule-level string) — tok baked-animation pointer. NEW viz layer = per-axis "viz" + capsule "heroViz" (neeche). Naam-collision note §3 field-rules.
  // "heroViz": { ... }            // RESERVED — capsule-level quick-glance animation spec. Finalize Part-2. ABHI gist mein nahi.
  // ---- CONTROLLER v0 capsule-level (RESERVED 6/29; FINALIZE at first R1) ----
  // "edgeMap":        "honest knowledge-boundary string ('yeh defend kar sakta, yeh nahi aur zaroorat bhi nahi'). Edge pe bluff NAHI = senior signal.",
  // "confusionPairs": [ { "a": "...", "b": "...", "differentiator": "..." } ]   // X-vs-Y discrimination drill, actual error-log se
}
```

### Field rules
- `faultLines`: **9 axes (a..i)**, status from Jirah only. NOT 3 escalating levels (purana 5-faultline schema DEAD). *(verified live 10 Aug 2026 — chaaron locked capsules mein theek `abcdefghi`, ek bhi missing/extra nahi. Khud dekho: `node -e "const fs=require('fs');for(const f of fs.readdirSync('dressing-room/state/capsules'))console.log(f, JSON.parse(fs.readFileSync('dressing-room/state/capsules/'+f)).faultLines.map(a=>a.axis).join(''))"`. Axes ke apne naam bhi ek doosri jagah data ban chuke hain — `dressing-room/state/concepts.json` ki `axes` map, aur `rejirah.mjs` unhi definitions se har axis ka test-type nikaalta.)*
- `doubts`: EVERY genuine knowledge stuck-point, Nikhil ke shabd. Mandatory — dual-purpose interview bank. Apni entry, weld mein dafan NAHI. **COLD-READER STANDARD binding (full block neeche):** `q` = ATOMIC (ek confusion) + SUBJECT explicitly named (koi dangling `ye`/`woh`/`Map`/`second-enemy`/`(pehle-guess)` nahi) + ANSWER-HIDDEN + RICH confusion-journey (maine-socha-X-phir-Y — cold-Nikhil pehchaane KAHAN atka; ATOMIC = ek confusion RICHLY-elaborated, skeletal/terse NAHI) + NO near-duplicate; `a` = complete standalone (mechanism + why). **TAXONOMY:** SIRF genuine knowledge stuck-points — curriculum/planning/status/deferral/generic-vocab kabhi NAHI. Capture = **Gate 1** (OS step-10 + opener-4a: Claude standard-pe draft → Nikhil BATCH-glance ('go'/'yeh do fix') → phir save; raw stuck-point seedha kabhi nahi). Verify = **Gate 2** (§5).
- `traps` >= 2, `bridges` >= 2 (dependency neighbours). **`bridges[].q` bhi COLD-READER STANDARD pe** (SUBJECT-anchored + answer-HIDDEN + atomic — `doubts[].q` jaisa; `to`/`conn`/`a` cold-legible). *(verified live 10 Aug 2026 — chaaron capsules dono floors se kaafi upar hain; ginti live lo, yahan se nahi: `node -e "const fs=require('fs');for(const f of fs.readdirSync('dressing-room/state/capsules')){const c=JSON.parse(fs.readFileSync('dressing-room/state/capsules/'+f));console.log(c.id,'traps',c.traps.length,'bridges',c.bridges.length,'doubts',c.doubts.length)}"`. **Dhyaan:** `bridges[].q` par Gate 2 ka machine-scan abhi NAHI chalta — `doubtminer.mjs` sirf `doubts[]` walk karta hai, isliye bridges par cold-reader ki jaanch aaj bhi poori tarah aankh ka kaam hai.)*
- `bolo`: Nikhil bolta/likhta; Claude sirf tighten karta. Invent kabhi nahi.
- `id`: long form (`tokenization`, `embeddings`, `inference`, `context`, `hallucinations`, `tooluse`, ...). ~~Engine IDMAP reconcile karta.~~ **(corrected 10 Aug 2026: IDMAP MAR CHUKA hai — poore repo mein zero hits, `grep -rn "IDMAP" --include=*.mjs --include=*.html .`. Long id hi aakhri sach hai: gist filename, engine ka fetch URL, `mirror_config.json` ki `ids[]`, `capsules/<id>.json`, aur `deep.mjs`/`rejirah.mjs`/`capsule_bridge.mjs` ka concept-argument — sab yahi long id. Reconcile karne ko kuch bacha hi nahi.)** **Filename = `<id>.json` (per-file store, §1).** *(naming ka ek live mismatch, 10 Aug 2026 — is line ka `tooluse` aur registry ka naam alag hain: `dressing-room/state/concepts.json` mein woh concept `tool_use` likha hai (`grep -n "tool_use" dressing-room/state/concepts.json`). Woh concept abhi tak locked nahi hai isliye aaj kuch toota nahi; lock ke waqt ek hi spelling chunni padegi warna mirror/registry do alag cheezein dhoondhenge. **NOT VERIFIED 10 Aug 2026 — kaunsi spelling jeetegi, yeh code se tay nahi hota; captain ki call hai.)*
- **`deep`** (per-axis + capsule-level) — *added 6/22*: scratch-se-re-learn layer (markdown string). **Verbatim-faithful ONLY** — threads/Bolo se recovered, Claude invent NAHI karta, Nikhil verify karta. Layers ON TOP of `weld`, replace NAHI. `weld` = quick recall-trigger; `deep` = full re-learn. **Dono zinda. Dono RENDER hone chahiye (completeness, §4 NOTE / FORGE_DESIGN.md §4 #4).** *(verified 10 Aug 2026: **ab dono render hote bhi hain** — yeh "chahiye" 6/22 se 9 Aug tak ek adhoora waada tha. Per-axis `deep` click-reveal mein, capsule-level apne section mein — §4 table ke corrected rows dekho. Requirement khud unchanged.)*
- **`viz` / `heroViz`** — *RESERVED, aa raha*: per-axis `viz` (spec object) + capsule-level `heroViz` (spec object). Mechanism + exact values = **Claude-locked**; visual/UX = **Design-free** (coverage-not-density). Exact schema **Part-2 mein finalize** (Design build se shape pakka), tab tak over-define nahi.
- **`viz` naming note:** capsule-level `viz: "bpe"` (string) = **LEGACY** tok baked-animation pointer. Going-forward viz layer = **per-axis `viz`** (object) + **capsule-level `heroViz`** (object). Naam alag rakhe taaki collision na ho. *(corrected 10 Aug 2026: "baked-animation pointer" ab kis cheez ki taraf ishaara nahi karta — naye engine mein koi baked animation hai hi nahi (§4 ka aakhri row). `"bpe"` aaj ek anaath string hai jo "Viz" section mein jaise ki taise chhap jaati hai. Live haalat: sirf `tokenization` ke paas value hai, baaki teen mein `viz: ""`. Naam-collision se bachne wali rooh unchanged — jab per-axis `viz`/`heroViz` objects aayenge, ye legacy string unse takrayegi nahi.)*

### COLD-READER STANDARD (doubts/bridges quality LAW) — *added 2026-07-02, AUTHORIZED correctness-fix*
> **THE ONE LAW:** har knowledge-artifact **cold-reader** se reconstructable ho. Cold-reader = **future-Nikhil, 6-12 mahine baad, is session ki ZERO memory.** Us pal samajh aana kaafi NAHI — 1 saal baad padhke bhi (a) pata chale WHAT discuss ho raha, aur (b) recall attempt ho sake. (Ye content-QUALITY law hai — render-completeness [§4 / FORGE_DESIGN §4 #4] se ALAG: completeness = "build field DIKHATA?", cold-reader = "gist mein doubt SAAF hai?".)

**Field-by-field bar:**
- **Q-fields** (`doubts[].q`, `bridges[].q`): **ATOMIC** (ek confusion, do nahi) · **SUBJECT explicitly named** (kya / kis cheez ki — koi dangling `ye`/`woh`/`Map`/`second-enemy`/`(pehle-guess)` nahi) · **ANSWER-HIDDEN** (question khud jawab na de) · **RICH confusion-journey** (maine-socha-X-phir-Y-contradict-laga — cold-Nikhil pehchaane KAHAN atka; ATOMIC = ek confusion RICHLY-elaborated, skeletal/terse NAHI) · **NO near-duplicate** (do doubts ek hi cheez na poochein).
- **A-fields** (`doubts[].a`, `bridges[].a`): **complete standalone** — mechanism + why, concise par poora; akele padho to poora resolve ho.
- **AUTHORED-PROSE** (`weld` · `deep` · `mechanism` · `hook` · `why` · `traps` · `threeWays` · `interviewLines` · `bolo`): cold-legible hona chahiye — par **SACRED.** Rule **FUTURE authoring** govern karti; existing sirf **VERIFY** (rewrite NAHI — sacred immutability, §5).
- **LOG-fields** (`calibration` · `reJirahDone` · `status`): bar = clear + dated; full knowledge-test nahi.
- **TAXONOMY (kya `doubts[]` mein jaata):** SIRF genuine knowledge stuck-points. Curriculum-planning ("IVF kitni depth"), status-notes ("[RESOLVED] 4 din pehle nervous"), deferral-notes ("chop/RAG abhi nahi seekhna"), generic-vocab — **kabhi NAHI** (lifelong bank mein clutter).

**3 failure-patterns (yahi pakadne — Gate 2 inhe flag karta):**
1. **CRYPTIC** — subject un-anchored. ❌ `"har layer pe SAME KV?"` → ✅ `"Transformer ke har layer ki apni ALAG KV cache hoti, ya saari layers ek hi share karti?"`
2. **FRAGMENT** — adjacent doubt pe latka, akele zero. ❌ `"Zyada temp = ?"` → ✅ `"Temperature ZYADA karne pe next-word menu sharp hoti ya flat — aur garbage-word ka chance badhta ya ghatta?"`
3. **META / TO-DO** — knowledge-doubt hai hi nahi. ❌ `"chop/RAG nahi seekhna?"`, `"IVF kitni depth?"`, `"[RESOLVED] 4 din pehle nervous, ab?"` → **PRUNE** (koi cold-reader jawab banta hi nahi).

**ATOMIC ≠ terse (2026-07-03):** richness confusion-JOURNEY mein hai (maine-socha-X-phir-Y-contradict-laga), extra confusions add karke NAHI (woh atomic todta). Ek confusion RICHLY-elaborated = GOOD · terse-but-cryptic = BAD · do confusions ek doubt mein = BAD. FRAGMENT ka ✅ example (temperature) already rich — wahi bar.

**Do gates (dono chahiye):**
- **GATE 1 — CAPTURE (clean-at-birth):** OS THE METHOD **step-10** (LOCK — doubts[] pehli baar likhte) + THREAD OPENER **4a** (later back-writes — Re-Jirah/recall cracks). Claude har doubt standard-pe DRAFT kare (Nikhil ke shabd, INVENT nahi) → Nikhil BATCH glance ('go' / 'yeh do fix', per-line NAHI) → PHIR save. Raw stuck-point seedha `doubts[]` mein kabhi NAHI.
- **GATE 2 — VERIFY (slip-catcher):** §5 — har LOCK/SAVE pe saare `doubts[]`/`bridges[].q` standard ke against verify; koi cryptic/fragment/meta/near-dup → flag → approval se fix → tabhi "done".

**APPLIES TO:** going-forward CAPTURE + RE-JIRAH + backfill — har doubt is test se guzre.
**RESEARCH base (locked — dobara research NAHI):** Wozniak 20-rules (R4 atomic · R11 anti-interference · R12 wording) + Zettelkasten (autonomous notes) + Tulving (encoding-specificity) + curse-of-knowledge — sab isi cold-reader self-containment pe converge.

## 4. ENGINE ADAPTER (gist capsule -> THE-FORGE.html render) — how it maps
> **PURA TABLE RE-VERIFIED 10 Aug 2026 — aur yeh table 9 Aug ke rebuild ke baad JHOOTHA ho chuka tha.**
> Niche jo bhi "was" likha hai woh is table ka apna purana text hai (design-era HUD ka naqsha, jo engine
> kabhi banaya hi nahi gaya ya jo rebuild mein chala gaya). Har naya row `setup/build_forge_html.mjs`
> **padh ke** likha gaya hai — woh generator hi aakhri sach hai, kyunki `THE-FORGE.html` uska output hai
> aur ek command se dobara ban jaata hai (`node setup/build_forge_html.mjs`). Jo render-naam (SAWAL/JAWAB,
> SCAR TIMELINE, CONCEPT BANK, anvil, rack) yahan the — woh screen pe **kahin nahi** hain: grep karke dekho,
> `grep -c "SCAR TIMELINE\|CONCEPT BANK\|AAJ REVISE\|IDMAP" THE-FORGE.html` = 0.
> **Row padhne ka tareeka: pehle grep chalao, tab maano.**

| Gist field | HUD render (verified 10 Aug 2026) |
|---|---|
| `faultLines[axis].strike` | per-daraar sawal, axis-card ke andar italic/blue line (`grep -n 'class="strike"' setup/build_forge_html.mjs`). *(was: "per-daraar **SAWAL** (interviewer)" — mapping wahi hai, par screen pe koi "SAWAL" label nahi hai; card ka heading `<axis> — <title>` + status-pill hai.)* |
| `faultLines[axis].weld`   | per-daraar jawab, **hamesha khula** — plain div, koi click-reveal nahi (`grep -n 'class="weld"' setup/build_forge_html.mjs`). *(corrected 10 Aug 2026: row poora yeh tha — "per-daraar **JAWAB** (deep, click-reveal)". Click-reveal ab **`deep`** ka hai, `weld` ka nahi; do alag layers ko ek row ne mila diya tha. "JAWAB" naam bhi screen pe nahi hai.)* |
| `faultLines[axis].status` | daraar status colour — axis-card ka left border (`held` green · `cracked` red · `deferred` amber): `grep -n "axis.held" setup/build_forge_html.mjs`. Status khud pill mein bhi chhapta. |
| `faultLines[axis].deep`   | **RENDER HOTA HAI** — per-axis `<details>` "deep — poora khol", weld ke neeche (`grep -n "deep — poora khol" setup/build_forge_html.mjs`). *(**corrected 10 Aug 2026** — row 6/30 se "MUST RENDER … Currently embedded-but-not-rendered = SKIP = fix PENDING (`FORGE_DEEP_RENDER_BRIEF.md`)" kehta raha, aur 9 Aug ka rebuild use render karne laga. Fix asal mein `FORGE_DEEP_RENDER_BRIEF.md` ke "next Design session" se nahi, **`setup/build_forge_html.mjs`** se aaya — brief file abhi bhi `learning-layer/` mein hai, par ab woh vehicle nahi, RECORD hai.)* |
| `deep` (capsule-level)    | **RENDER HOTA HAI** — apna section "Deep — the god-tier re-learn layer" (`grep -n "god-tier re-learn layer" setup/build_forge_html.mjs`). *(corrected 10 Aug 2026: same purana "SKIP = fix PENDING" jhoot, same fix.)* |
| `faultLines[axis].viz` / `heroViz` | schema abhi bhi RESERVED (live capsules mein yeh keys hain hi nahi — check: `node -e "console.log(Object.keys(require('./dressing-room/state/capsules/tokenization.json')))"`), **par "NOT rendered" ab galat hai**: jis din yeh keys aayengi, render apne-aap ho jayega — per-axis koi bhi extra key `kv` line ban ke chhapti hai (`grep -n "axis.,.title.,.strike" setup/build_forge_html.mjs` wali filter-line), aur capsule-level `heroViz` "Fields with no designed section yet (rendered raw — never skipped)" section mein aa jayega. *(corrected 10 Aug 2026: pehle likha tha "reserved, NOT rendered … render legitimately pending". Reserved-hona sach hai; not-rendered ab nahi — designed section abhi nahi hai, **skip** ab possible nahi.)* |
| `threeWays`               | apna section **"Three ways"** (poora object, pretty-printed) — `grep -n 'sec("Three ways"' setup/build_forge_html.mjs`. *(was: "**axis-i** 3 NAZARIYE cards (ceo/junior/skeptic)" — data wahi ceo/junior/skeptic hai, par render axis-i ke andar cards ka nahi, apne alag section ka hai.)* |
| `bolo`                    | apna section **"Bolo — the sacred out-loud answer"**, `bolo_by` uske heading mein (`grep -n "the sacred out-loud answer" setup/build_forge_html.mjs`). *(was: "**axis-a** TERA BOLO (concept-level). b..i = placeholder until per-axis Bolo" — ab bolo kisi axis se juda hua nahi hai, aur koi b..i placeholder screen pe nahi hai. Note: "TERA BOLO" shabd `THE-FORGE.html` mein milta hai, par woh uske apne capsule PROSE ka hissa hai, engine ka label nahi.)* |
| `doubts` / `traps` / `interviewLines` / `calibration` | **chaar alag sections**, ek-ek — aur `doubts` ka heading apni ginti khud carry karta ("Doubts — his real ones, verbatim (N)"), rows verbatim `<pre>` mein: `grep -n "his real ones, verbatim" setup/build_forge_html.mjs`. *(corrected 10 Aug 2026: was "**CONCEPT BANK** collapsibles (panel bottom)" — na koi "CONCEPT BANK" naam hai, na collapsible; sab khule sections hain. `bridges` ka apna section bhi hai, jise yeh row naam hi nahi leta tha.)* |
| `lockedOn` + `reJirahDone` | `lockedOn` capsule ke meta-line mein; `reJirahDone` apna section — served due-dates ki list, khaali ho to "none yet — R1 abhi baaki" (`grep -n "Re-Jirah rounds done" setup/build_forge_html.mjs`). **Koi timeline-graphic nahi, koi due-banner nahi.** *(corrected 10 Aug 2026: was "**SCAR TIMELINE** (anvil) = real dates · **HOME due-banner** (rack) = 'aaj revise' alert" — dono engine mein maujood NAHI (`grep -c "SCAR TIMELINE\|AAJ REVISE" THE-FORGE.html` = 0). Woh kaam ab engine ka hai hi nahi — due/overdue ki asli surface CLI hai: `node scripts/deep.mjs due` (queue, cold, sirf strike-questions) aur `node scripts/rejirah.mjs due`. Yeh downgrade nahi, relocate hai.)* |
| per-axis `r3` + inline `trap` | gist capsule inhe carry nahi karta — **live check**: axis-keys aaj `axis,title,strike,weld,status,deep` hain, bas. *(corrected 10 Aug 2026: row poora yeh tha — "**baked only for tok** (gist capsule carries inhe nahi). Future: add per-axis r3 to schema if chahiye." Pehla aadha ab galat hai: tok ke liye BHI baked nahi raha; naye engine mein koi baked `AX`/`CONCEPTS` block hai hi nahi, `grep -c "const AX\b\|const CONCEPTS" THE-FORGE.html` = 0 — sirf SNAP baked hai, aur SNAP = capsules ki verbatim copy. Doosra aadha ("gist capsule carries inhe nahi") aaj bhi sach hai, aur "future: add r3 to schema if chahiye" ek khula option hai — abhi tak nahi liya gaya.)* |
| `viz` (legacy string) / animation / 1-page poster | `viz` string apne section "Viz" mein render hoti hai (`grep -n 'sec("Viz"' setup/build_forge_html.mjs`) — aaj sirf `tokenization` mein value hai (`"bpe"`), baaki teen mein khaali string. **Koi animation, koi poster engine mein nahi** (`grep -c "poster" THE-FORGE.html` = 0). *(corrected 10 Aug 2026: was "**baked only for tok**. Gist-only concepts → placeholder (no fake viz)." — baked animation/poster rebuild mein chale gaye; `"bpe"` ab ek pointer-string hai jo bina kisi baked animation ke chhapti hai. "no fake viz" wali rooh phir bhi zinda: engine kuch banata nahi.)* |

> **NOTE (2026-06-22) — engine reality vs spec:** Shipped `THE-FORGE.html` (~115KB vanilla) abhi **baked-only** hai — gist live `fetch()` NAHI karta (§1/§2 ka intended-flow drift), aur `deep`/`viz` **render bhi nahi** karta. Yeh teeno **next engine rebuild** pe reconcile honge (embeddings viz ship hone ke baad). Tab tak: **gist = canonical**, engine baked SNAP dikhata. Rebuild pe decide: live-fetch wapas laana hai ya baked rehna hai (abhi open). **Multi-file fetch (per-file store, §1): rebuild pe GitHub Gist API se `.files` auto-discover ya per-file raw URLs — abhi action nahi, baked SNAP never-empty rakhta.**
>
> **SUPERSEDED — woh "next rebuild" ho chuka (9 Aug 2026; verified 10 Aug 2026).** Upar wala NOTE ab HISTORY hai, current-state nahi; 9 Aug tak yeh file use current bata rahi thi. Line-by-line, aaj:
> · **~115KB** → aaj ka artifact bada hai kyunki SNAP mein chaaron capsules ki poori prose baked hai. **Size prose mein mat likho** — har rebuild pe badalta hai; generator khud print karta hai (`grep -n "capsules baked" setup/build_forge_html.mjs`), aur file ka size `ls -l THE-FORGE.html` se lo.
> · **baked-only / live-fetch open** → **fesla ho chuka: DONO.** SNAP = never-empty floor, phir live gist fetch, sab safal to swap (§2 step 3 ka corrected note dekho). "Abhi open" wala sawaal band hai.
> · **`deep`/`viz` render nahi karta** → ab teeno render hote (§4 table ke corrected rows).
> · **Multi-file fetch "abhi action nahi"** → action ho chuka, par yahan wale DONO tareeko se nahi: engine per-capsule raw URL khud maar leta hai (`<GIST>/<id>.json`, SNAP ki id-list se), aur GitHub Gist API se `.files` auto-discover wala kaam engine ke bajaye **`mirror.mjs`** karta hai (`grep -n "enumerateGist" scripts/mirror.mjs`) — jo har subah gist ko list karke naye capsules dhoondta, aur `mirror_config.json` ki `ids[]` ko sirf FLOOR maanta.
> · **"gist = canonical"** → yeh wali line ekdum zinda hai, aur poori file mein sabse zaroori.

> **CORRECTION (2026-06-30) — deep-render = COMPLETENESS, defer NAHI:** Upar wala NOTE (aur is adapter ke purane "NOT rendered yet" rows) deep-not-rendered ko *"next rebuild pe aayega"* (accepted defer) frame karta tha — **woh LOOPHOLE tha** (operationally = content silently chhupa raha). Actual contract (FORGE_DESIGN.md §4 #4 — completeness): gist ka **HAR content field** build mein **render + byte-for-byte + reachable** hona ZAROORI. Carry-as-data-but-don't-render = field SKIP = TEXT-VERBATIM violation. **`deep` (per-axis + capsule) render = MUST-FIX** — defer NAHI; fix-vehicle = `FORGE_DEEP_RENDER_BRIEF.md` (next Design session). Adapter ke "NOT rendered yet" deep-rows ab "MUST RENDER (pending fix)" padho. (**`viz`/`heroViz` alag case** — schema genuinely reserved/un-finalized, render legitimately pending; jab schema lock ho tab completeness inpe bhi lागू hoga.)

> **FIX LANDED (verified 10 Aug 2026) — is CORRECTION ka kaam ho gaya.** Upar wali 6/30 ki ruling (loophole ka naam lena, aur "MUST RENDER" likhna) **sahi thi aur waise hi khadi hai** — badla sirf itna ki ab woh sirf likhi hui nahi, code mein hai. `deep` dono jagah render hota (§4 table), aur skip ab structurally possible nahi (generic walker + per-capsule "0 skipped" footer, §2.5 CLOSED note). Do cheezein is fix ke baare mein sach-sach: (1) **fix-vehicle badal gaya** — `FORGE_DEEP_RENDER_BRIEF.md` (abhi bhi `learning-layer/` mein maujood) ke "next Design session" se nahi, `setup/build_forge_html.mjs` se aaya; brief ab record hai, plan nahi. (2) **`viz`/`heroViz` ki "alag case" wali chhoot ab bekaar ho gayi** — schema abhi bhi reserved hai (live capsules mein woh keys nahi), par un par completeness ka intezaar nahi karna padega: jis din key aayegi, walker use render kar dega. Chhoot ki zaroorat hi khatam.

### Re-Jirah scheduling (date-driven, computed live)
> **RE-VERIFIED 10 Aug 2026 — schedule ka MAALIK badal chuka hai.** Yeh block engine ko schedule ka computer batata tha. Aaj: **numbers ek genome-file mein rehte hain aur CLI unhe compute karti hai; engine schedule compute karta hi nahi** (`THE-FORGE.html` mein koi due/overdue logic nahi — `grep -c "AAJ REVISE" THE-FORGE.html` = 0). Rule wahi hai, jagah alag.
- Schedule = `lockedOn` + **3 din / 2 hafte / 6 hafte** ~~(engine `new Date()` se compute, no stored countdown)~~. *(corrected 10 Aug 2026: teen number ab prose se nahi, **genome** se aate — `dressing-room/state/forge_profile.json` → `rejirah_intervals_days` (aaj `[3, 14, 42]`, jo theek 3d/2wk/6wk hai). Padho live: `node -e "console.log(require('./dressing-room/state/forge_profile.json').rejirah_intervals_days)"`. **Yeh mutable hai** — us file ka aakela likhne-wala `bootroom.mjs` hai, aur woh bhi sirf mutation-pipeline se, captain ki manzoori pe (`grep -n "sole write target" scripts/bootroom.mjs`). Toh yeh teen number kal alag ho sakte hain, aur tab bhi is line ko nahi, genome ko sach maano. Compute karne wale: `deep.mjs` (`grep -n "rejirah_intervals_days" scripts/deep.mjs` — default `[3, 14, 42]` sirf tab jab profile na mile) aur `capsule_bridge.mjs` (`grep -n "DEFAULT_INTERVALS" scripts/capsule_bridge.mjs`, jiska comment is hi §4 ka hawala deta). "No stored countdown" wali baat abhi bhi sach: sab kuch `lockedOn` se live compute hota.)*
- Round status: `done` (date in `reJirahDone`) · `overdue` (date < aaj) · `due` (date == aaj) · `up` (date > aaj). *(verified 10 Aug 2026 — yeh chaar states aaj bhi live hain, engine mein nahi, `capsule_bridge.mjs` mein: `grep -n "overdue" scripts/capsule_bridge.mjs`; `deep.mjs rejirahStatus()` bhi yahi derive karta.)*
- ~~HOME page: koi round `due`/`overdue` ho → red banner "AAJ REVISE", click → concept khulta. Warna agla due date soft-show.~~ *(corrected 10 Aug 2026: engine mein aisa koi banner **kabhi mila hi nahi** aaj ke build mein — `grep -c "AAJ REVISE" THE-FORGE.html` = 0. Woh surface ab CLI hai: **`node scripts/deep.mjs due`** = queue, **cold, sirf strike-questions, notes band** — aur `node scripts/rejirah.mjs due` batata hai kaunse AXES aur kitna sakht. SessionStart brief bhi due-halat carry karta hai. Banner gaya nahi — kahin aur chala gaya, aur behtar jagah.)*
- **Write path (Re-Jirah complete pe):** Claude `reJirahDone` mein woh due-date add karta → us **EK** capsule ka file (`<id>.json`) ka updated version deta → Nikhil us EK file **replace** (baaki untouched) → (offline ke liye SNAP-refreshed naya THE-FORGE.html bhi). Tab woh round `done` dikhta, agla due surface hota.
  *(corrected 10 Aug 2026 — path wahi hai, par ab uske teen live parze hain jo yeh line nahi jaanti: (1) round pehle **`node scripts/rejirah.mjs close <concept>`** se band hota, jo apne append-only `rejirah_log.jsonl` mein likhta aur **exact patch-line print** karta — Claude patch "banata" nahi. (2) Jab tak paste gist tak nahi pahunchta, round **PENDING** dikhta (`node scripts/rejirah.mjs pending`) — aur yeh proof hai ki paste utra, andaza nahi. **PAANCH organ tab tak maante hain round hua hi nahi** — fsrs · deep · capsule_bridge · dugout · shipped (`grep -n "WHAT DEPENDS ON" scripts/rejirah.mjs`). (3) "SNAP-refreshed naya THE-FORGE.html" ab koi deta nahi — `node scripts/mirror.mjs && node setup/build_forge_html.mjs`. **Aur ek line jo yeh block chhupa jaata tha:** koi bhi script `capsules/` mein nahi likh sakti — woh mirror.mjs ka read-only ilaaka hai, isliye Re-Jirah ka result gist se hi ghoom ke wapas aata hai. Yahi wajah hai ki paste ke bina kuch nahi hota.)*

- ~~IDMAP: `tokenization->tok, embeddings->emb, inference->inf, neuralnet->nn, training/rlhf->rlhf, context->ctx, hallucinations->hal, tooluse->tus, jagged->jag`.~~ **(corrected 10 Aug 2026: IDMAP ab kahin nahi hai — `grep -rn "IDMAP" --include=*.mjs --include=*.html .` = zero hits, engine mein bhi zero. Yeh short-id table ab historical hai; koi organ ise nahi padhta. Short ids (`tok`/`emb`/`inf`/`ctx`) sirf `doubtminer.mjs` ke selftest-fixtures mein bache hain — asli data nahi. Long id hi har jagah ka id hai.)** **Filename convention:** `<long-id>.json` (`tokenization.json`, `embeddings.json`, `inference.json`, `context.json`, ...) — *(verified 10 Aug 2026: yeh convention live hai; `ls dressing-room/state/capsules/` aur `mirror_config.json` ki `ids[]` dono yahi shape dikhate.)*
- ~~`curve.str` (retention ring): status mein number ho to wahi, warna **75** (fresh-lock, honest — overstate nahi).~~ *(corrected 10 Aug 2026: engine mein koi retention-ring nahi hai aur `curve` naam ka koi field capsule schema (§3) mein hai hi nahi — `THE-FORGE.html` mein "curve" sirf EK jagah milta hai, aur woh `context` capsule ki apni PROSE hai ("FRESHNESS: FRESH lock, curve.str = 75"), engine ka code nahi. Yani yeh row ek aise HUD ka niyam tha jo shipped engine mein kabhi utra hi nahi. **Rooh phir bhi kaam ki hai** — fresh lock pe number overstate mat karo — isliye rule mit nahi raha, sirf apni asli jagah pe rakha ja raha hai: retention ab FSRS ke paas hai (`dressing-room/state/cards.json` / `fsrs_store.json`), aur woh apni history `lockedOn` + `reJirahDone` se banata hai, kisi default 75 se nahi.)*

## 5. ARCHITECTURE RULES (hard)
- **NEVER replace, always layer.** ~~Baked AX/CONCEPTS untouched; gist overlays on top. Both live in code.~~ (`deep` bhi `weld` ke UPAR layer hua — replace nahi.) *(corrected 10 Aug 2026: **rule zinda hai, uska purana MECHANISM mar chuka.** Naye engine mein `AX`/`CONCEPTS` naam ke koi baked blocks hain hi nahi — `grep -c "const AX\b\|const CONCEPTS" THE-FORGE.html` = 0. Ab layering yahan hai: baked **SNAP** (capsules ki verbatim copy) neeche ki manzil, live gist uske UPAR overlay — dono ek hi file mein zinda, aur overlay tabhi lagta jab poora aata hai (`grep -n "fresh.every(Boolean)" setup/build_forge_html.mjs`). Aur `deep`-over-`weld` wali baat ab sirf niyam nahi, screen pe dikhti hai: weld khula, deep uske neeche `<details>` mein. Do-manzila dhaancha wahi, eenten alag.)*
- **NEVER SKIP a content field at RENDER (NEW, 6/30).** Gist ka har content field build mein render + byte-for-byte + reachable (completeness — FORGE_DESIGN.md §4 #4). Carry-as-data-but-don't-render = silent field SKIP = TEXT-VERBATIM violation. Layer karna ≠ chhupana. *(verified 10 Aug 2026 — **yeh rule ab enforce hoti hai, sirf maangi nahi jaati.** Generator mein designed-sections ki ek explicit list hai, aur uske BAAHAR ki har key raw render hoti hai; har capsule ke neeche ek footer "N data fields · X designed + Y raw — 0 skipped" chhapta hai (`grep -n "0 skipped" setup/build_forge_html.mjs`). Byte-for-byte wala hissa bhi structural hai: capsules generator mein `JSON.parse` → `JSON.stringify` hoke jaati hain, prose kabhi truncate/reword nahi hoti (`grep -n "sacred, never reworded" setup/build_forge_html.mjs`). **Naya field jodna ab safe hai — dikhega zaroor, chahe uska design abhi na bana ho.**)*
- **GATE 2 — CONTENT-VERIFY at LOCK/SAVE (NEW, 7/2).** Har LOCK ya SAVE (naya capsule YA existing edit/back-write) pe, us file ke saare `doubts[]` + `bridges[].q` **COLD-READER STANDARD (§3)** ke against VERIFY hote — koi **cryptic** (subject un-anchored) / **fragment** (adjacent pe latka) / **meta** (curriculum/planning/status/deferral) / **near-duplicate** slip ho → **FLAG** → Nikhil-approval pe fix → **TABHI file "done".** Gate 1 (capture — OS step-10 + opener-4a) clean-at-birth rokta; Gate 2 = safety-net jo slip pakadta. **Content-QUALITY verify hai — render-completeness verify (upar wala rule / FORGE_DESIGN §12) se ALAG layer:** woh "build har field dikhata?", yeh "gist mein doubt saaf hai?".
  *(**GATE 2 AB CODE MEIN HAI — added to this rule 10 Aug 2026, aur is rule ka apna sabse bada scar yahi hai.** 7/2 se 4 Aug 2026 tak yeh gate sirf PROSE thi aur **ek baar bhi nahi chali**: lock ke waqt jo akela artifact load hota hai woh forge skill hai, aur woh Gate 1 tak likh ke ruk jaati thi (`grep -n "NEVER ONCE FIRED" scripts/doubtminer.mjs`). Ab: `doubtminer.mjs` har raat capsules ke saare `doubts[]` is standard ke against scan karta — `gate2Flags()`, aur uske teeno pattern-sets is file ke apne naam-liye failure-patterns se aaye hain (`grep -n "GATE2_PATTERNS" scripts/doubtminer.mjs`; `grep -n "3 failure-patterns" scripts/doubtminer.mjs` uska hawala bhi deta). Do baatein sakhti se sach rakhni hain: **(1) FLAG-ONLY** — woh ek harf nahi badalta, badal bhi nahi sakta, kyunki capsules gist ka read-only mirror hain; repair captain ke haath se gist tak hi jaata (`grep -n "FLAG ONLY" scripts/doubtminer.mjs`). **(2) Ginti yahan MAT likho** — live padho: `node -e "const t=require('./dressing-room/state/tape_room.json');console.log(t.gate2.line, JSON.stringify(t.gate2.by_pattern))"`. Aur jo flagged hai woh 5 Aug se rematch-queue se BAHAR rehta hai (`eligible:false`, `ineligible_reason: "gate2: not cold-readable yet"` — `grep -n "not cold-readable yet" scripts/doubtminer.mjs`), taaki cold future-Nikhil ko bina-subject wala doubt na parosa jaye; gist pe wording theek hote hi woh apne-aap wapas aa jaata. **Detector ek fixed pattern-list hai — uska number MACHINE FLOOR hai, sach nahi; asli ginti usse zyada hi hogi.** Machine ne Gate 2 ko replace nahi kiya — usse rozana chalne wala bana diya; LOCK/SAVE pe aankh se verify karna aaj bhi lazmi hai. **Aur ek kami jise chhupana nahi:** yeh rule `doubts[]` AUR `bridges[].q` dono maangti hai, par machine sirf `doubts[]` chalta hai — `grep -n "bridges" scripts/doubtminer.mjs` = **zero hits**. Toh Gate 2 ka aadha ilaaka aaj bhi bina automation ke hai; `bridges[].q` par har LOCK/SAVE pe aankh chalani hi padegi.)*
- Engine kabhi regenerate nahi (per-concept). Design badle = naya THE-FORGE.html, README/yeh-file migration note ke saath. *(corrected 10 Aug 2026: **"per-concept regenerate nahi" wali rooh sahi hai — "engine kabhi regenerate nahi" ab galat hai.** Poora engine ab EK command se dobara banta hai: `node setup/build_forge_html.mjs`, jo `capsules/` padh ke `THE-FORGE.html` naye sire se likhta hai (atomically — tmp likh ke rename, `grep -n "renameSync" setup/build_forge_html.mjs`). Yeh rule ka ulanghan nahi, uska structural fix hai: content generator chhoota nahi, woh capsules se VERBATIM aata hai, isliye "regenerate" ab prose ko dobara likhne ka khatra nahi rakhta — bas markup dobara banta hai. **Jo abhi bhi mana hai: `THE-FORGE.html` ko haath se edit karna** — agla rebuild use chup-chaap kha jayega.)*
- **Locked capsule files = IMMUTABLE.** Naya concept = naya file (`<id>.json`); existing file sirf apne Re-Jirah/doubt pe edit hoti. Claude purane locked capsules KABHI re-emit nahi karta — yahi truncation + sacred-content-corruption ka **structural fix** (per-file store, §1/§2).
  *(rule unchanged, do precisions added 10 Aug 2026 kyunki is EK vaakya ko ulta padha ja chuka hai: **(1) IMMUTABLE ka matlab "kabhi mat likho" NAHI hai — "kabhi RE-EMIT mat karo" hai.** Isi vaakya ka doosra hissa khud kehta hai "existing file sirf apne Re-Jirah/doubt pe edit hoti", aur §6 mechanism ka naam bhi leta hai: "re-emit nahi, targeted update". Ek session ne ise ulta padh ke maan liya tha ki capsule kabhi likhi hi nahi ja sakti, aur baad mein khud likh ke theek kiya — woh sudhaar `rejirah.mjs` ke header mein padha ja sakta hai (`grep -n "It read that law backwards" scripts/rejirah.mjs`). Do likhaayi jaayaz hain aur dono **USKE** haath se, paste se: round-close pe `reJirahDone`, aur doubt ka back-write. **(2) `dressing-room/state/capsules/` mein koi organ nahi likhta — sirf `mirror.mjs`**, aur woh bhi gist se utaari hui verbatim bytes (`grep -n "Single writer of capsules" scripts/mirror.mjs`). Isliye koi bhi local "fix" agli subah ke pull mein mit jayega. Immutability ki asli guard yeh ownership hai, iraada nahi.)*
- **Schema-edit SIRF concept-lock boundary pe** (META-FREEZE). *(6/22 update = post-emb-lock; 6/24 store-update = inference-lock boundary; 6/29 progress-sync + to-schema-fy = context-lock boundary; 6/30 render-completeness CORRECTION = authorized correctness-fix, DATA-schema untouched; 7/2 cold-reader standard [§3 block + §5 Gate-2] = authorized correctness-fix, DATA-schema untouched; 7/8 rename cascade [OS "EXECUTION LAYER"→"LEARNING EXECUTION LAYER", OS v3.13 outwork-merge companion] = pure rename of referenced OS section, DATA-schema untouched — sab valid.)*
- **VIZ division-of-labor:** mechanism + exact values = Claude-locked · visual/UX/flow = Design-free · **coverage-not-density** (har mechanism visualize ho, par ek waqt ek focal point). Design export = **reference only, ship kabhi nahi** — Claude visual-language extract karke vanilla engine mein faithful content + working mechanism se re-implement karta. ~~*(Reality drift §6 FORGE_DESIGN.md — current Design build directly use ho raha.)*~~ *(corrected 10 Aug 2026: **woh drift khatam ho chuka.** Aaj ka shipped engine koi Design-export nahi hai — woh `setup/build_forge_html.mjs` ka output hai: ek hi file mein vanilla HTML + inline CSS + vanilla JS, koi framework nahi, koi import nahi, koi bahar ka asset nahi (`grep -c "import " setup/build_forge_html.mjs` sirf uske apne node built-ins par lagta hai, generated HTML mein zero). Yani rule ("reference only, ship kabhi nahi") ab realty se match karta hai — pehli baar. Viz ki division-of-labor khud unchanged.)*
- ~~THE-FORGE.html = laptop + git only. Yeh FORGE_SPEC.md = project files mein.~~ **(corrected 10 Aug 2026 — yeh line 9 Aug ko §1 mein to theek kar di gayi thi, par yahan neeche purani hi padi rahi, aur ek hi file ke do hisse do alag jagah bata rahe the.** Aaj dono cheezein ek hi ghar mein hain, aur woh ghar GIT hai: `THE-FORGE.html` repo root pe (`git ls-files THE-FORGE.html`), yeh spec `learning-layer/FORGE_SPEC.md` mein. Purani "laptop + git, NEVER project files" wali copy KHO GAYI thi — wahi out-of-git rot jo `THE_GAFFER.md` ke Drive-tag ne sikhaya (`grep -n "was lost" setup/build_forge_html.mjs` — generator apne header mein yahi scar likhta hai). **Sabak yaad rakho: prose mein likha hua LOCATION uss din jhoot ho jaata hai jis din file hilti hai — isliye jagah kabhi yahan se mat maano, `git ls-files` se dekho.**)*
- Gist = single source of truth. SNAP = uska faithful baked mirror (never-empty). **Build = gist ka COMPLETE mirror — content cut/skip nahi.** *(precision added 10 Aug 2026: "single source of truth" ab bhi bilkul sach hai, par SNAP gist ka SEEDHA mirror nahi — beech mein ek aur mirror hai. Poori chain: **gist (master, sirf uska paste) → `mirror.mjs` (roz subah pull, verbatim bytes, kabhi wapas nahi likhta) → `dressing-room/state/capsules/<id>.json` (read-only, akela maalik mirror.mjs) → `build_forge_html.mjs` → baked SNAP → engine**, aur browser mein engine phir gist ko khud fetch karta hai. Iska ek asli natija hai: **purane mirror pe rebuild = purana SNAP.** Isliye rebuild se pehle hamesha `node scripts/mirror.mjs`. Aur "COMPLETE mirror — cut/skip nahi" ab prose ka waada nahi raha, engine ke apne footer se naapa jaata hai (har capsule pe "designed + raw — 0 skipped").)*

## 6. CONTROLLER v0 SCHEMA — RESERVED in §3 (FINALIZE + POPULATE at first R1) *(reserved 2026-06-29, context-lock boundary)*
> Re-Jirah CONTROLLER v0 (OS "LEARNING EXECUTION LAYER" mein design-locked) ke per-axis + capsule-level fields ab §3 mein
> **RESERVED** hain (viz/heroViz jaise — shape sketched, gist mein abhi nahi). **FINAL shape + constants + capsule
> POPULATION first R1 RUN pe** (un-run = hypothesis; run se hi real values + tuned constants aate). **Locked capsules
> abhi tak in fields ke bina hain — R1 mein populate honge, EK-EK file, sacred Bolo preserve karke (re-emit nahi,
> targeted update).** Constants (interval multipliers, reset window, R-mid→R-late switch, weave fraction) = OS
> LEARNING EXECUTION LAYER mein v0, R1 pe tune.

Reserved per-axis fields (har `faultLines[axis]` pe — §3 mein sketched, R1 pe finalize+populate):
- **`axisType`** — `recall` | `reconstruct` | `defend` (kis tarah test ho).
- **`nextDue`** — per-axis next Re-Jirah date (global +3d/+2wk/+6wk replace; SM-2-lite se compute).
- **`lastResult`** — last round ka outcome (held-clean | held-struggle | cracked).
- **`calibrationGap`** — predicted-vs-actual gap (control-signal: confident+cracked → tighter interval + mode-bump).
- **`fluencyState`** — `tempered` | `fluent/dṛḍhabhūmi` (correctness ke upar speed+effortless dimension; sirf load-bearing core pe).

Capsule-level (R1 pe finalize):
- **`edgeMap`** — honest knowledge-boundary string ("yeh defend kar sakta, yeh nahi aur zaroorat bhi nahi"). Edge pe bluff NAHI = senior signal.
- **`confusionPairs`** — `[{a, b, differentiator}]` — X-vs-Y discrimination drill, actual error-log se.

**Lock rule:** in fields ka exact shape + constants (interval multipliers, reset window, R-mid→R-late switch, weave fraction) = **first R1 pe spec honge** jab pata chalega design reality-contact survive karta ya nahi. Tab tak schema §3 frozen.

> **STATUS RE-VERIFIED 10 Aug 2026 — "RESERVED" abhi bhi sach hai, par ab poora sach nahi.**
> **Gist ke andar:** saatoon fields aaj bhi GAYAB hain, waisa hi jaisa yeh section kehta hai — live check
> `node -e "console.log(Object.keys(require('./dressing-room/state/capsules/tokenization.json')))"` (koi
> `axisType`/`nextDue`/`lastResult`/`calibrationGap`/`fluencyState`/`edgeMap`/`confusionPairs` nahi) aur
> per-axis keys sirf `axis,title,strike,weld,status,deep`. Schema §3 frozen — waisa hi.
> **Code ke andar (5 Aug 2026 se, jo yeh section nahi jaanta):** saaton fields **AB EXIST KARTE HAIN — DERIVED**,
> `scripts/rejirah.mjs` mein, apne append-only `rejirah_log.jsonl` se compute hoke — gist mein populate karke nahi.
> Padho: `node scripts/rejirah.mjs state <concept>` · `grep -n "axisType:" scripts/rejirah.mjs` ·
> `grep -n "edgeMap = {" scripts/rejirah.mjs`. Yeh section ka ULANGHAN nahi hai, **iska palan** hai, aur woh file
> khud yahi kehti hai: "This is a DEFERRAL until R1 closes, which is exactly what the spec asks for — not a
> refusal" (`grep -n "not a refusal" scripts/rejirah.mjs`). Wajah bhi seedhi hai: derive lossless hai — galat
> constant ki keemat ek re-run hai, re-write nahi.
> **Do cheezein jo ab is section se aage ja chuki hain, aur jinhe yahan se mat maano:**
> · **`axisType` guess nahi hai** — nau axes ki typing PROJECT_OS ke apne 9-axes definitions se aayi hai
>   (`grep -n "AXIS_TYPE" scripts/rejirah.mjs`), aur woh code mein padi hai, is file mein nahi.
> · **Constants ka maalik ab yeh file nahi rahi** — interval numbers genome mein hain
>   (`forge_profile.json` → `rejirah_intervals_days`, sole writer `bootroom.mjs`), calibration ke targets
>   `calibration.mjs` ke, fluency ki seedhi `learning_state` wali. `rejirah.mjs` unhe udhaar leta hai aur
>   maalik ka naam likh deta hai — number duplicate karna hi drift ka rasta hai.
> · **Aur ek division jo yahan likhi hi nahi thi:** FSRS tay karta hai concept **KAB** wapas aayega
>   (scheduler of record), yeh controller tay karta hai **KAUNSE AXES aur KITNA SAKHT** — dono ab alag sawaalon
>   ke jawab dete hain, isliye do schedulers ke beech ka jhagda khatam (`grep -n "THE ARBITER" scripts/rejirah.mjs`).
> **Jo ab bhi baaki hai:** in fields ko gist ki capsules mein POPULATE karna, aur tabhi jab R1 ka data
> constants ko sach me tune kar de. Woh kadam abhi tak nahi utha — aur woh **uske paste se hi** uthega (§2 2b).

---
## CHANGELOG
- **2026-07-08 (rename cascade — OS v3.13 outwork-merge companion, AUTHORIZED):** OS "EXECUTION LAYER" section → **"LEARNING EXECUTION LAYER"** rename (OS mein naya **OUTWORK EXECUTION LAYER** peer add hua — do "execution layer" naam-collision khatam: LEARNING = decay+drill engine, OUTWORK = consistency+accountability engine). Is file mein sirf woh 4 spots badle jahan OS us section ko NAAM se reference karta: §3 schema-comment (controller v0 per-axis fields — "constants in OS EXECUTION LAYER" → "LEARNING EXECUTION LAYER"), §6 header ("OS EXECUTION LAYER mein design-locked" → "LEARNING EXECUTION LAYER"), §6 body ("OS EXECUTION LAYER mein v0" → "LEARNING EXECUTION LAYER"), 2026-06-29 changelog line ("Companion: OS EXECUTION LAYER" → "LEARNING EXECUTION LAYER"). **Controller v0 DESIGN + SCHEMA (5 knobs + 4 mechanics + reserved per-axis/capsule fields) UNCHANGED — pure rename of the referenced OS section, koi schema-touch nahi. DATA-shape UNCHANGED.** §5 META-FREEZE ledger mein 7/8 add. Companion: PROJECT OS v3.13 (rename + outwork-layer wire + 3 merges: ledger/decay-engine/scrimmage-scout-dossier + cadence-guards ported). META-FREEZE exception: rename-consistency (referenced OS section renamed — reference accurate rakhna), Nikhil explicit + repeated, big-integration boundary (v3.10 jaise).
- **2026-07-03 (rich-doubt + batch-glance — AUTHORIZED correctness-fix, refines 7/2 over-tighten):** COLD-READER STANDARD ki DO clauses surgically corrected — (1) §3 Q-field bar (`doubts` field-rule + STANDARD block) `shortest-wording (terse OK)` → **RICH confusion-journey** (maine-socha-X-phir-Y; cold-Nikhil pehchaane KAHAN atka; ATOMIC = ek confusion RICHLY-elaborated, skeletal NAHI); (2) §3 GATE-1 capture `Nikhil line-by-line approve` → **BATCH glance** ('go'/'yeh do fix'). Plus §3 mein "ATOMIC ≠ terse" clarifier + header note. **Root cause:** 7/2 ne cryptic/fragment/meta theek kiya (VALID, stays) par 'shortest-wording' + 'line-by-line approve' over-shot → skeletal Q (cold-Nikhil topic pehchaane par 'kahan atka' nahi) + high approval-tax. Baaki 7/2 cold-reader (failure-patterns, taxonomy, subject-anchor, answer-HIDDEN, no-near-dup, Gate-2 verify) FULLY UNCHANGED. **DATA-schema UNCHANGED.** Companion: PROJECT OS v3.9. First R1 (tokenization, 7/3) ne surface kiya. META-FREEZE exception (correctness loophole-fix, explicitly requested, recurrence rokta — v3.7/v3.8 jaise).
- **2026-07-02 (cold-reader standard — AUTHORIZED correctness-fix):** §3 "Field rules" mein `doubts` + `bridges` upgrade (ATOMIC · SUBJECT-named · answer-HIDDEN · no near-dup · TAXONOMY = sirf genuine knowledge stuck-points) + naya **"COLD-READER STANDARD"** block (the one law: Q-fields / A-fields / authored-prose / log-fields / taxonomy + 3 failure-patterns + examples + 2 gates + research-base). §5 mein **"GATE 2 — CONTENT-VERIFY at LOCK/SAVE"** rule (har `doubts`/`bridges` cold-reader ke against verify; cryptic/fragment/meta/near-dup → flag → fix → tabhi done) + META-FREEZE ledger mein 7/2 add. **Root cause:** doubt CAPTURE-time quality-bar spec mein tha hi nahi (sirf "har stuck-point capture") → ~1/3 doubts (107 mein se) cryptic/fragment/meta (context ~73%). **DATA-schema UNCHANGED** (fields same; QUALITY-BAR + verify-gate add). Companion: PROJECT OS v3.8 (Gate 1 = step-10 + opener-4a). Research locked: Wozniak (R4/R11/R12) / Zettelkasten / Tulving / curse-of-knowledge. Next (cure-half): 4 capsule-remediation threads (tok → emb → inf → ctx, fresh clean thread each). META-FREEZE exception (correctness loophole-fix, explicitly requested, recurrence rokta).
- **2026-06-30 (render-completeness — AUTHORIZED mid-Re-Jirah correctness-fix):** `deep` (per-axis + capsule) RENDER reclassified: "accepted defer" → **COMPLETENESS REQUIREMENT** (governing rule = FORGE_DESIGN.md §4 #4, completeness contract). §4 adapter deep-rows "NOT rendered yet" → "MUST RENDER (pending fix)"; §4 NOTE correction added; §5 "NEVER SKIP a content field at RENDER" rule added; §2.5 capture≠render note added. **Root cause:** `deep` gist + build DATA dono mein embedded tha par screen pe render nahi hota → re-read shallow + concepts judte nahi + interview-fail risk. **DATA-schema UNCHANGED** (fields same). Fix-vehicle = `FORGE_DEEP_RENDER_BRIEF.md`. Companion: FORGE_DESIGN.md v2 + PROJECT OS v3.7. (`viz`/`heroViz` exempt — schema genuinely reserved.)
- **2026-06-29 (context-lock boundary):** PROGRESS-SYNC — header lock-count 2→4 (tok/emb/inf/context; context #04 locked 6/28). Controller v0 fields **RESERVED in §3** (per-axis: `axisType`/`nextDue`/`lastResult`/`calibrationGap`/`fluencyState`; capsule-level: `edgeMap`/`confusionPairs`) — shape sketched (viz/heroViz pattern), see §6. FINAL shape + constants + capsule-POPULATION at first R1. **Locked capsules UNTOUCHED** (immutable — R1 mein real data se populate, targeted per-file, sacred Bolo preserve). (Companion: OS "LEARNING EXECUTION LAYER" + OPPONENT_SCOUT.md.)
- **2026-06-24 (inference-lock boundary):** STORE single `forge-capsules.json` array → **PER-FILE** (`<id>.json`, one capsule each). Write-path per-file (new = 'Add file'; edit = replace one file). Locked files **immutable** (Claude purane capsules re-emit nahi karta). Capsule **schema UNCHANGED**. Engine multi-file fetch = pending rebuild ka hissa (§4 NOTE).
- **2026-06-22 (post-emb-lock):** 3-layer notes model added — `deep` field (capsule-level + per-faultLine, markdown string, verbatim-faithful) in gist + schema. `viz` (per-axis) + `heroViz` (capsule-level) **RESERVED** (Part-2 finalize). New §2.5 LOCK-time capture. Engine render-drift NOTE (§4). VIZ division-of-labor in §5.
- *Earlier:* tokenization (#01) locked 6/15; embeddings (#02) locked 6/21; inference (#03) locked 6/24; context (#04) locked 6/28; vanilla engine rebuild (baked-only).
