{
  "version": 1,
  "_comment": "Canon config for learning_state.mjs (Agent #4) — committable, tunable, NOT PII. thresholds = v0 SEED (structure real; latency/runnable numbers are HYPOTHESES calibrated at first R1 run — edit + re-run, lossless since everything derives from reps_log). maidan.stages cross-check concepts.json buckets; maidan.handoffs = real RAG data-flow (domain architecture, NOT a pedagogical prereq-graph — that is Nidhi's domain, deferred). learning_state.mjs READS this only (single-writer preserved); missing ⇒ these defaults.",
  "thresholds": {
    "held_streak": 2,
    "fluent_streak": 3,
    "latency_fast_ms": 8000,
    "stage_runnable_frac": 0.75,
    "warming_up_min_reps": 1,
    "stall_reps": 6,
    "_opened_2026_08_18": "warming_up_min_reps 12 → 1 (fluency states + maidan focus compute on what he has, n printed). held_streak 2 / fluent_streak 3 are the LADDER's DEFINITIONS (🟡 held = 2 consecutive correct, 🟢 fluent = 3 consecutive cold-fast — learning-layer canon), not speak-gates; latency_fast_ms / stage_runnable_frac / stall_reps are definitions too. OPENED 18 Aug 2026 — ORGANISM_OVERHAUL §5.4 (his 1 Aug 'why are we setting numerical limits … everything fully opened' + 13 Aug 'I do not have 30 days to wait for data' + 18 Aug 'full permission'): a guessed number may be a GUARD or a WINDOW, never a BUDGET or a CALENDAR GATE. A speak-gate at 1 is arithmetic (you cannot compute on an empty set), not a wall — the organ speaks on what he has and prints n. Verify: node scripts/limits.mjs (0 guessed gates)."
  },
  "maidan": {
    "stages": [
      { "id": "fundamentals", "label": "Fundamentals", "order": 1, "concepts": ["tokenization"] },
      { "id": "rag_pipeline", "label": "RAG pipeline", "order": 2, "concepts": ["chunking", "embeddings", "retrieval", "rag_eval"] },
      { "id": "agents", "label": "Agents", "order": 3, "concepts": ["tool_use"] }
    ],
    "handoffs": [
      { "from": "tokenization", "to": "embeddings", "label": "text → vectors" },
      { "from": "chunking", "to": "embeddings", "label": "chunks → vectors" },
      { "from": "embeddings", "to": "retrieval", "label": "vectors → top-k" },
      { "from": "retrieval", "to": "rag_eval", "label": "results → eval" }
    ]
  }
}
