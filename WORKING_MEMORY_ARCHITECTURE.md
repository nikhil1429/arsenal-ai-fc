# WORKING MEMORY ARCHITECTURE — the Ambient Cyborg Brain

> **HOW TO READ THIS FILE (doc-rot audit, 10 Aug 2026).** This is the **DESIGN SPEC as written
> before the build**, and Phases 0–5 have since SHIPPED and drifted from it. Every sentence below
> is therefore one of two things and is now marked as such: a **design intent** (what we set out to
> build) or a **live fact** (what the code does today). Where the two disagree, the code wins and
> the inline `(corrected 10 Aug 2026: …)` note says so with its evidence. Nothing was deleted —
> the original design sentence is preserved so the delta between plan and build stays readable.
> **Never derive a status, a count, a cadence or a budget number from this file** — every such
> claim now carries the command that reads it live.

> Build spec. Produced from a 6-lens design panel (memory · real-time cognition · capture ·
> ADHD augmentation · orchestration · integration) + adversarial critique + synthesis.
> Goal: the Gaffer + the Claude brain **see me everywhere, remember everything, think about
> me 24/7** across every surface — so the machine carries the memory/context/analysis load
> and Nikhil just learns and codes. Everything here **layers on** the existing organism; it
> replaces nothing.

## The four laws (any design that breaks these is disqualified)
1. **Single writer per file.** `thalamus.mjs` owns `afferent.jsonl` + `workspace.json`. Every new organ gets its **own** file; it never co-writes an existing one.
   - *(corrected 10 Aug 2026 — the owned set is FIVE files, not two: `grep -n "single writer — this file alone writes" scripts/thalamus.mjs` → `afferent.jsonl · workspace.json · salience_ledger.jsonl · wake.json · wake_queue.jsonl`. The two named above are the ones this design adds to; the other three predate it.)*
   - *(corrected 10 Aug 2026 — "**it never co-writes an existing one**" has TWO known live exceptions, one deliberate and one a real breach. DELIBERATE: `brain_ledger.jsonl` is a **shared append lane by design** with six live appenders (brain · cortex · council · selfknowledge · nightshift · dmn); `brain.mjs` owns the SCHEMA only — see the header note at `grep -n "shared append lane" scripts/talk.mjs`. BREACH, **NEEDS THE CAPTAIN'S RULING, do not silently "fix" in code**: `identity_facts.pending.jsonl` has TWO writers — `grep -n "PENDING_FACTS" scripts/hippocampus.mjs scripts/mcp-memory.mjs` shows `hippocampus.mjs` REWRITES the whole file via `writeAtomic` while `mcp-memory.mjs` APPENDS to it, i.e. a rewrite racing an append. `mcp-memory.mjs`'s own header still claims it "owns … identity_facts.pending.jsonl only". Both claims cannot be true.)*
2. **One budget meter.** Every Claude call routes through `brain.mjs headroom()` against the one ledger, with a **live-headroom reserve** so background thinking can NEVER throttle a live turn.
   - *(corrected 10 Aug 2026: the half that HOLDS is "**against the one ledger**" — every Claude spender meters into `brain_ledger.jsonl`. The half that does NOT hold universally is "**routes through `headroom()`**": only `cortex.mjs` and `council.mjs` import it (`grep -rn "import { headroom" scripts/*.mjs`), while `talk.mjs` and `selfknowledge.mjs` spend Claude and append to the ledger with **zero** `headroom` references (`grep -c headroom scripts/talk.mjs scripts/selfknowledge.mjs` → 0 and 0; the spend sites are `grep -n "brain_ledger\|BLEDGER" scripts/talk.mjs scripts/selfknowledge.mjs`). Read the law as: ONE ledger always; headroom-gating on the background lanes, not on the live voice lane.)*
3. **Async never blocks the voice.** Deep/Opus work is async and folds in at a quiet beat; the live turn only touches millisecond retrieval.
4. **Proposes, never auto-acts on canon** (`who_he_is`, drills, capsules, medical/Goalkeeper). Human approves. The medical boundary is sacred.

---

## THE CEILING (the honest answer to "is this the top?")

**Yes — there is a materially higher ceiling than a safe design.** The true maximum: an always-on, *paced*, multi-surface brain where every room (Code, Desktop, Gaffer, ambient ActivityWatch) writes through the one thalamus door; a **free** continuous distiller holds his 4 working-memory slots externally; and the **never-fired Opus cortex finally ignites** — thinking about him around the clock and pouring the whole 5x plan into deep reads every night, while a measured live-reserve guarantees it can never slow a live turn.

> *(corrected 10 Aug 2026 — "**the never-fired Opus cortex**" was true the day it was written and is FALSE now: the cortex has fired. Count them live —
> `node -e "const fs=require('fs');const c={};for(const l of fs.readFileSync('dressing-room/state/brain_ledger.jsonl','utf8').trim().split('\n')){try{const r=JSON.parse(l);if(String(r.job||'').startsWith('cortex'))c[r.job]=(c[r.job]||0)+1}catch{}};console.log(c)"`
> — which on 10 Aug returned real `cortex_wake` AND `cortex_consolidate` rows. Read the number live; never from this line.)*

**But the ceiling is a CONTROLLER, not a firehose.** One always-on Opus extended-thinking stream drains an ~800k window in under an hour — literal 24/7 Opus is *impossible* on the Max plan and would be the *wrong* design even if it were free. So:
- **Haiku** carries the always-on continuous layer (~1% of Opus cost).
- **Sonnet** carries the every-few-minutes synthesis.
- **Opus** is reserved for genuine doubts + the overnight flood.
- **Gemini-flash (free)** carries the distiller + dreamer at zero Max cost.
- One **governor** pins it all to a target burn rate.

> *(corrected 10 Aug 2026, TWO rot points in the block above.)*
> *(1) **"~800k window" is a 5x-plan number and the plan is no longer 5x.** He moved to 20x and the ORGANISM's ruled share was written into config on 9 Aug. Read it live — `node -e "console.log(require('./dressing-room/state/brain_config.json').budget)"` — which reports the live `window_capacity_est_tokens` / `weekly_capacity_est_tokens` plus his verbatim split ruling in `_split_ruling_2026_08_09`. `brain.mjs`'s own `DEFAULTS` still carries the OLD 800000/12000000 pair (`grep -n "window_capacity_est_tokens" scripts/brain.mjs`) — that is the fallback, **not** the live budget. The qualitative point ("one always-on Opus stream drains a window in about an hour") survives; the number does not.*
> *(2) **The "Sonnet carries the every-few-minutes synthesis" rung was NEVER BUILT as described.** No every-few-minutes Sonnet job exists. List the real jobs and their models live — `node -e "console.log(require('./dressing-room/state/brain_config.json').jobs.map(j=>[j.id,j.model,j.window,j.at||''].join(' | ')).join('\n'))"` — and every Sonnet row is a scheduled midday/evening/overnight job, not a continuous layer. What actually keeps the working set current every few minutes is the **Gemini-flash distiller**, on a scheduler cadence, for zero Max cost. See Layer 4 below for the same correction at the source.*

The real wall sits in four honest places no architecture removes:
1. **Claude Desktop has no hook API** → its capture is model-mediated (an MCP tool the model chooses to call) + the clipboard turnstile as the deterministic backstop. This is his *primary* study room and its capture is structurally softer than Code's.
2. **claude.ai / Colab passive capture** needs a DOM-brittle browser extension he must install and keep enabled — aspirational, not day-one.
3. The **800k/window, 12M/week caps are Anthropic-unpublished estimates** — the machine must LEARN the real ceiling by probing at 3am.
   - *(corrected 10 Aug 2026: the WALL is still real and still unpublished — that half stands. The NUMBERS are stale: 800k/12M was the 5x-era pair and the live config carries the 20x-era organism share since 9 Aug. Read both live: `node -e "const b=require('./dressing-room/state/brain_config.json').budget;console.log(b.window_capacity_est_tokens,b.weekly_capacity_est_tokens)"`. The learning mechanism is built and is an EWMA, not a ratchet — `grep -n "function blendCeiling" scripts/brain.mjs`.)*
4. **No pixel sight** — ActivityWatch is metadata, not pixels; anything he reads silently on screen stays invisible unless a nerve reports it.
   - *(corrected 10 Aug 2026 — true of the AMBIENT layer only, and that qualifier is load-bearing. The ambient nerve genuinely sends metadata and says so: `grep -n "Metadata only" scripts/context.mjs` → "Metadata only (AW app+title) — NO pixels, never the screen's contents." But a vision lane DOES exist elsewhere in the organism: the Dugout's **Watcher (T2)**, vision-only, on his DECLARED screen, inside a live voice session he opened — `grep -n "T2 Watcher" scripts/dugout.mjs` and `grep -n "vision:" scripts/dugout.mjs`. So the honest wall is: **no PASSIVE, always-on pixel sight**; opt-in in-session sight exists.)*

Within those four walls, this design **is** the ceiling.

---

## THE SPINE (unchanged, sacred)
`thalamus.mjs` (:4113) stays the SOLE writer of `afferent.jsonl` + `workspace.json` and the single `POST /afferent` door every nerve calls. `brain.mjs` stays the ONE budget meter via `headroom()`. Nothing below co-writes an existing file. The affect firewall + verbatim/gut-word laws apply at every **new** door, not just the voice door.

*(verified 10 Aug 2026: `:4113` is correct and hardcoded — `grep -n "const PORT = 4113" scripts/thalamus.mjs`; the door is real — every nerve in this document POSTs `/afferent`. The port registry, for anyone tracing a lock: `grep -n "Port registry:" scripts/brain.mjs` → 4111 turnstile · 4112 cortex · 4113 thalamus · 4114 dugout · 4115 brain tick-lock · 4116 brain daemon-singleton. Two corrections carry down from Law 1 and Law 2 above: the thalamus owns FIVE files not two, and "Nothing below co-writes an existing file" is **not** true of `identity_facts.pending.jsonl` — see Law 1.)*

## Layer 1 — Universal capture (every surface → an afferent source, zero tap)
- **Claude Code (crown jewel — the only surface with deterministic hooks).** New checked-in `.claude/settings.json` hooks: `SessionStart` injects the rehydrate cartridge + working_set; `UserPromptSubmit` injects a timeboxed (~200ms, skip-on-timeout) recall hint; `Stop`/`PostToolUse` fire-and-forget POST the turn to `:4113` (modality `code`/`tool`). Each shells a tiny `hooks/afferent-post.mjs` with a hard ~200-300ms timeout that fails silently if `:4113` is down — a dead daemon never taxes a live CLI turn. **Scrub text before any git op** (repo is PUBLIC).
  - *(corrected 10 Aug 2026 — FOUR fixes to this bullet, read the wiring live with `node -e "const h=require('./.claude/settings.json').hooks;for(const k of Object.keys(h))console.log(k+': '+h[k][0].hooks.map(x=>x.command).join(' · '))"`.)*
  - *(a) **`PostToolUse` was never wired and no `tool` modality has ever been emitted.** The live event list is `UserPromptSubmit` · `Stop` · `SessionStart` · `PreCompact` — no `PostToolUse` entry exists. And the nerve itself only recognises two events: `grep -n "hook_event_name" hooks/afferent-post.mjs` shows it branches on `UserPromptSubmit` → `source:"claude-code"` and `Stop` → `source:"claude-code-teaching"`, and **`return die()` for anything else**. `grep -n "modality: \"code\"" hooks/afferent-post.mjs` — one hardcoded modality, always `code`. Any organ written to filter on modality `tool` would filter an empty set forever.*
  - *(b) **"Each shells a tiny `hooks/afferent-post.mjs`" is wrong** — `afferent-post.mjs` is ONE of several commands per event, and the others are unrelated organs with their own budgets: `UserPromptSubmit` also fires `forge_session.mjs contract`, `teaching_contract.mjs print`, `teaching_audit.mjs hook`, `hippocampus.mjs recall-hint`; `Stop` also fires `teaching_audit.mjs hook`; `SessionStart` fires five commands, none of them this nerve. Only `afferent-post.mjs` carries the timeout law.*
  - *(c) **The timeout is 250ms, exactly, and it is on the POST — not on the process.** `grep -n "setTimeout(() => ctrl.abort(), 250)" hooks/afferent-post.mjs`. The "~200-300ms" range reads as a tolerance; the code has one number.*
  - *(d) **The recall hint is LEXICAL and has no skip-on-timeout guard.** `grep -n "mode === \"recall-hint\"" scripts/hippocampus.mjs` → it calls `recallReflexLexical(text)`, not the cosine surface, and its own measured note above it reads "node boot + import + scoring 18 surfaces = 142-198 ms wall clock per invocation (3 runs)". So it is **measured to fit inside** the 250ms law, not **timeboxed by** it — nothing in the hook aborts a slow run. (Also true and unchanged: it reads the hook's stdin JSON so his Hinglish is never shell-mangled, and it stands down for headless organs on `ARSENAL_ORGAN=1`.)*
  - *(e) **"Scrub text before any git op" is not where the scrub happens** — the scrub is at CAPTURE time and it DROPS the whole event rather than redacting it: `grep -n "SECRET_RE" hooks/afferent-post.mjs` → a secret-shaped prompt exits silently and is never written at all. Stronger than the design promised, and in a different place. The `afferent.jsonl` it would have landed in is gitignored anyway (`git check-ignore -v dressing-room/state/afferent.jsonl`).*
- **Claude Desktop (primary study room — the honest floor, no hooks).** A `scribe` MCP tool the model calls at each concept-close + a strong project instruction (modality `desktop-study`). **Backstop:** existing `turnstile.mjs` clipboard path (copy = captured). Model-mediated = softer than Code.
  - *(corrected 10 Aug 2026: **there is no MCP tool named `scribe`.** The four tools the server exposes are `recall` · `note` · `get_context` · `remember_fact` — `grep -n "{ name: \"" scripts/mcp-memory.mjs`. The capture tool is **`note(kind,text)`**, and it is the one that emits modality `desktop-study` (`grep -n "modality: \"desktop-study\"" scripts/mcp-memory.mjs`). The name `scribe` survives only as the note record's filename, `scribe_log.jsonl`. The turnstile backstop is real and live — `grep -n "LOCK_PORT = 4111" scripts/turnstile.mjs`, 4s clipboard cadence, ignore-by-shape.)*
- **ActivityWatch → `context.mjs`.** Graduates `presence.mjs` from stall-only to **delta-only ambient context** (app/title/URL on CHANGE, ~60s floor) → `:4113` modality `context`, so every bound moment carries what-app/what-concept.
  - *(corrected 10 Aug 2026 — BUILT, delta-only and ~60s floor confirmed (`grep -n "FLOOR_MS" scripts/context.mjs`), modality `context` / source `activitywatch` confirmed. Three refinements. **(i) There is no URL** — the emitted event carries `app` + `title` ONLY (`grep -n "modality: \"context\", source" -A 2 scripts/context.mjs`); AW's window bucket has no URL field and the browser-watcher bucket was never wired. **(ii) `presence.mjs` was not replaced** — it still runs its own per-minute sense task beside this (`schtasks /query /tn ArsenalFC-Presence /fo LIST /v`); `context.mjs` is an ADDITIONAL organ that owns only `context_state.json`. **(iii) "so every bound moment carries what-app/what-concept" over-promises the concept half** — a window title is canonicalised through the ONE concept registry and most titles are not concepts. Read the real yield live: `node scripts/context.mjs status` (on 10 Aug it reported a small single-digit fraction of emits carrying a canon concept). This is also why the distiller later demoted `context` out of its his-words window — Layer 2 (d).)*
- **Colab.** Existing clipboard turnstile + `paste-session` skill — copy is the whole move.
  - *(verified 10 Aug 2026: both halves live — the turnstile daemon holds `:4111` on a 4s clipboard cadence and ingests by SHAPE (`grep -n "PRIVACY BY SHAPE" scripts/turnstile.mjs`), and the `paste-session` skill exists. Unchanged since design.)*
- **claude.ai web (aspirational).** A `MutationObserver` browser extension → `:4113` modality `web`. Read-only, DOM-brittle. **Cut for day-one.**
  - *(verified 10 Aug 2026 — STILL NOT BUILT, exactly as the design intended. No extension exists: `git ls-files | grep -iE "extension|mutationobserver"` returns nothing and there is no extension directory in the repo root. No modality `web` is emitted anywhere: `grep -rn "modality: \"web\"" scripts/ hooks/` is empty. This bullet is the rare one that is correct precisely because nobody acted on it.)*

## Layer 2 — Working memory (the ADHD-tax remover)
`distiller.mjs` — new daemon, SOLE writer of a new `working_set.json`, reads `afferent.jsonl` + `workspace.json` **read-only**, runs the **free Gemini-flash pool** (registered as an 8th tank in `fuelboard.mjs` so it can't starve embed/dugout/dmn). Every ~5 min / N events it maintains a ~1KB externalized **4-slot whiteboard**: *concept-in-motion · open-loop · where-he-left-off · next-obvious-step.* Zero Max budget. **This file re-hydrates him after any context switch.**

*(corrected 10 Aug 2026 — BUILT and running, with four drifts from the sentence above. Read the organ's own contract with `grep -n "LAWS:" -A 4 scripts/distiller.mjs`.)*
- *(a) **It owns TWO files now, not one.** `SINGLE WRITER of working_set.json AND distiller_latency.jsonl (the G16 switch-to-read counter, 10 Aug 2026)` — `grep -n "LATENCY_LOG" scripts/distiller.mjs`. Both are gitignored (`git check-ignore -v dressing-room/state/working_set.json dressing-room/state/distiller_latency.jsonl`). Its read-only report is `node scripts/distiller.mjs latency`.*
- *(b) **It reads FOUR files read-only, not two** — `afferent.jsonl` + `workspace.json` + `presence_log.jsonl` + `drills.json`. The last one matters: `next_step` is **never invented**, it comes from `drills.json` or stays empty.*
- *(c) **"Every ~5 min" is wrong — the live cadence is 15 minutes**, set by the scheduler, not by the code. Read it live: `schtasks /query /tn ArsenalFC-Distiller /fo LIST /v` (on 10 Aug: `Repeat: Every: 0 Hour(s), 15 Minute(s)`, task enabled). There is no "/ N events" trigger — the scheduler is the only clock. It runs as a repeating task, **not** as a resident daemon, so "new daemon" over-states it.*
- *(d) **The 4 slots are NOT filled from an undifferentiated stream.** Two hard screens were added after the design and both are load-bearing: `context` (window captions) was moved OUT of the his-words window into a single labelled `current_window` line (`grep -n "INTERACTIVE_LEGACY" scripts/distiller.mjs` keeps the pre-audit list frozen beside the live one), and `claude-code-teaching` / `gemini-study-teaching` are denied as HIS words (`grep -n "NOT_HIS_SOURCES" scripts/distiller.mjs`) — without that screen the organism read its own prose back as his open loop. The 8th-tank claim holds: `grep -n "T8" scripts/fuelboard.mjs` → `{ id: "T8", name: "Distiller", region: "working-set", … }` and the header reads "eight tanks, one gauge". Selftest green on 10 Aug: `node scripts/distiller.mjs selftest` → ALL CHECKS PASSED.*

## Layer 3 — Shared retrieval + injection
`organism-memory` MCP (`scripts/mcp-memory.mjs`) registered in BOTH `.mcp.json` and Claude Desktop's own config. Tools: `recall(query)`, `note(kind,text)`, `get_context()` (= `buildRehydrateCartridge()` + working_set), `remember_fact()`. Writes route THROUGH `POST :4113/afferent` (thalamus stays sole writer). `remember_fact` **stages** to a new `identity_facts.pending.jsonl` and needs a separate human confirm (Law 4). `recallReflex` is promoted from a Gaffer-internal function to a **shared** `recall()` over ONE cosine surface merging `episodes.jsonl` + `recall_index.jsonl` at read time.

*(verified + corrected 10 Aug 2026 — this paragraph is the most accurate in the file; four refinements. Server registration confirmed in-repo: `grep -n "organism-memory" .mcp.json` (the Desktop-side registration lives outside the repo and is NOT verifiable from here — **NOT VERIFIED 10 Aug 2026**, treat as a claim). All four tool names are exact — `grep -n "{ name: \"" scripts/mcp-memory.mjs`. Selftest green on 10 Aug: `node scripts/mcp-memory.mjs selftest` → ALL CHECKS PASSED.)*
- *(a) **`recall()` merges THREE surfaces, not two** — `episodes.jsonl` + `recall_index.jsonl` + the MCP's own `scribe_log`, deduped, **with a lexical fallback when the embedding pool is dry**. `grep -n "surface merging" -A 2 scripts/mcp-memory.mjs`. The fallback is the part a reader must not miss: recall degrades, it does not fail.*
- *(b) **`note()` does MORE than route to the thalamus** — for the hippocampus-known kinds it ALSO hands the moment to `markMoment` (its owner-writer) "so the moment becomes a real, recallable episode **even when the bus is down**". `grep -n "HIPPO_KINDS" scripts/mcp-memory.mjs`. There is a `node scripts/mcp-memory.mjs resync` mode that re-posts notes whose thalamus POST failed.*
- *(c) **The staged-facts file does not live where a reader would guess.** It is `dressing-room/hippocampus/identity_facts.pending.jsonl`, NOT under `dressing-room/state/` — `grep -n "PENDING_FACTS = " scripts/mcp-memory.mjs scripts/hippocampus.mjs`. That location is what makes it private: the whole `dressing-room/hippocampus/` directory is gitignored (`grep -n "dressing-room/hippocampus/" .gitignore`), and the same is true of `scribe_log.jsonl`, which also lives there despite `.gitignore` naming a `dressing-room/state/scribe_log.jsonl` path that no code writes. The staging law itself holds exactly as written — `grep -n "staged: true" scripts/mcp-memory.mjs` returns the refusal text "needs your explicit confirm before it becomes canon (Law 4)". **But see Law 1: this file has two writers and needs his ruling.***
- *(d) `get_context()` gained a **third** part after this was written: the rehydrate cartridge + the distiller working set **+ the teaching card** (`grep -n "loadTeachingCard" scripts/mcp-memory.mjs`) — because `get_context` is the door CLAUDE.md mandates and it was the one with no teaching rules behind it.*

## Layer 4 — Continuous paced cognition (fires the never-fired cortex)
The 15-30min cron becomes a resident pacer **folded into `brain.mjs`** (`brain.mjs --daemon`, 60-90s poll) — NOT a separate scheduler. It computes `target_burn = remaining_headroom / time_to_window_edge` and dispatches:
- **Haiku PULSE** (60-90s, engaged-mode only, hard daily cap, metered every pulse): semantic watch of the afferent tail above thalamus's deterministic salience; escalate/hold.
- **Sonnet SYNTHESIS** (every few min): folds recent work into a live session-read; keeps `day_cartridge` + `working_set` current in real time.
- **Opus DEEP-READ** = existing `cortex.mjs` (:4112), fired on genuine `tau1` doubts + flooded overnight.

The pacer **never writes `wake_queue`** — it only POSTs afferent and lets the thalamus decide+enqueue, so there's exactly one scheduling authority and one wake writer.

*(corrected 10 Aug 2026 — the pacer is BUILT and resident; four claims in this section have moved. The wake-authority law at the end is the one thing that has not moved an inch: `grep -n "never writes wake_queue" scripts/brain.mjs` — asserted in the header, in `targetBurn`'s comment, and printed on every daemon boot. Do not weaken it.)*
- *(a) **"NOT a separate scheduler" did not survive contact — BOTH lanes run today.** The resident pacer is live (`schtasks /query /tn ArsenalFC-BrainDaemon /fo LIST /v` → `node scripts\brain.mjs daemon`, enabled) **and** the old cron is still enabled beside it (`schtasks /query /tn ArsenalFC-BrainTick /fo LIST /v` → `scripts\brain.mjs tick`, repeating every 30 minutes on 10 Aug). They do not double-spend: a tick-lock on `:4115` serialises them and a singleton bind on `:4116` stops the daemon piling up (`grep -n "Port registry:" scripts/brain.mjs`). Coexistence is the live design; "folded into, replacing the cron" is the design intent.*
- *(b) **The poll is 15 seconds, not 60-90.** Read it live: `node -e "console.log(require('./dressing-room/state/brain_config.json').daemon)"` → `poll_ms: 15000`, with his approved-ladder note. `grep -n "poll_ms) || 75000" scripts/brain.mjs` shows 75000 survives only as the code-side FALLBACK when config is silent. This is why (c) matters.*
- *(c) **The Haiku pulse is no longer beat-counted, and its spacing is NOT the daemon poll.** After the pacer went to 15s, a beat-counted gate would have silently quintupled the pulse — so the gate was pinned in SECONDS: `grep -n "min_spacing_s" scripts/brain.mjs dressing-room/state/brain_config.json` → live `min_spacing_s: 150`, derived from the old world (2 beats × ~75s). `every_n_beats` remains in config **only as the derivation record**. Its status has also flipped twice: PAUSED 2 Aug (it was 32.4% of the rolling week for a verdict nothing could act on), then UNPAUSED 9 Aug as a **pure instrument** with weight 0.00 in the thalamus. Never read `enabled` from prose — `node -e "console.log(require('./dressing-room/state/brain_config.json').pulse.enabled)"`.*
- *(d) **The Sonnet SYNTHESIS rung was NEVER BUILT.** The daemon beat runs `targetBurn` then `tick()` then, every `min_spacing_s`, the pulse — read the loop at `grep -n "mode === \"daemon\"" scripts/brain.mjs`. There is no every-few-minutes synthesis dispatch. `day_cartridge` is an **overnight Opus job**, not a live one, and `working_set` is kept current by the **Gemini-flash distiller** on the scheduler's 15-min cadence (Layer 2). Confirm the whole job table live with `node -e "console.log(require('./dressing-room/state/brain_config.json').jobs.map(j=>[j.id,j.model,j.window,j.at||''].join(' | ')).join('\n'))"`. Anyone planning against "Sonnet keeps the working set live" is planning against a rung that does not exist.*
- *(e) **The Opus DEEP-READ has fired** — `:4112` is correct (`grep -n "4112" scripts/cortex.mjs`), `tau1`-gated wakes are real, and the ledger carries both `cortex_wake` and `cortex_consolidate` rows (count them with the command under THE CEILING above). "the never-fired cortex" language elsewhere in this file is pre-ignition.*

## Layer 5 — The governor (pinning the 5x plan)
- `reserveNow()` replaces the static `day_reserve_frac=0.25` with a **measured live reserve** (AW window-title + AFK + per-surface heartbeat files). While live: `background_allowed = cap − used − live_reserve` (Law 3 by construction). Idle: reserve collapses to ~0 and the pacer floods to the 95% overnight target.
  - *(corrected 10 Aug 2026 — BUILT (`grep -n "function reserveNow" scripts/brain.mjs`) with three drifts.)*
  - *(i) **`day_reserve_frac` is not 0.25 any more, and it was never "replaced".** `reserveNow` RETURNS it as the live-floor — the function is a 3-step ladder on idle minutes: `<8 min → day_reserve_frac` · `≥8 → halfway` · `≥20 → overnight_target_frac`. And the live value moved on the 9 Aug unleash: read it with `node -e "console.log(require('./dressing-room/state/brain_config.json').budget.day_reserve_frac)"`. `brain.mjs`'s `DEFAULTS` still carries the old 0.25 (`grep -n "day_reserve_frac" scripts/brain.mjs`) — fallback, not truth.*
  - *(ii) **The signals are not "AW window-title + AFK + per-surface heartbeat files".** `grep -n "function liveSignal" scripts/brain.mjs` → it takes the freshest of THREE tail reads: interactive rows in `afferent.jsonl`, `focus` rows in `presence_log.jsonl`, and `dugout_stamps.jsonl`, and returns a single `{ idle_min }`. **No per-surface heartbeat files were ever built** — that phrase appears nowhere in the code. AW reaches this only indirectly, as `context` afferents. No signal at all ⇒ `{}` ⇒ assume live ⇒ protect him.*
  - *(iii) **The formula `background_allowed = cap − used − live_reserve` is not the shape that shipped.** `grep -n "function headroom" scripts/brain.mjs` → the reserve scales the **cap** (`cap = cap0 * reserveNow(...)` during study hours) and then `allowed = max(0, min(cap − used, weeklyCap − weekly))`. Same intent, different arithmetic — and the weekly clamp in the real one has no counterpart in the design line.*
- **Overnight ceiling-probe (EWMA):** replaces the one-way `Math.max` ratchet with an EWMA that deliberately probes the knee at 3am (a lockout there is free); learns the true `observed_window_ceiling`; probe cutoff ~05:30 so any CLI backoff clears before the 07:30 study window.
  - *(verified 10 Aug 2026 — this bullet SHIPPED AS WRITTEN, the one Phase-0 line with no drift. `grep -n "function blendCeiling" scripts/brain.mjs` is the EWMA (alpha 0.4, floored at the estimate so a low read can never starve the brain — that floor was added after a limit event once recorded `ceiling=1` and switched the overnight engine off). The 05:30 cutoff is real and hardcoded: `grep -n "05:30" scripts/brain.mjs` → the overnight tail backs off to the day fraction from 05:30 to `overnight.end`. Read the live overnight window with `node -e "console.log(require('./dressing-room/state/brain_config.json').overnight)"` rather than assuming 07:30.)*
- `maxThinkingFor(mode, headroom)`: scales `MAX_THINKING_TOKENS` — **16k live / 32-60k overnight** — AND derives cortex's `min_headroom_tokens` from that budget (not the flat 50000) so the deepest reads can't overshoot the meter.
  - *(corrected 10 Aug 2026 — BUILT and WIRED, two numbers wrong. **The overnight depth is a single value, 48k — not a "32-60k" range**: `grep -n "function maxThinkingFor" scripts/brain.mjs` → `phase === "study" ? 16000 : 48000`, then clamped so it never exceeds half the remaining window. Its own selftest pins both (`grep -n "THINKING DEPTH" scripts/brain.mjs`). And **"(not the flat 50000)" is backwards**: `grep -n "mtf.min_headroom_tokens" scripts/cortex.mjs` → `Math.max((cfg.deep && cfg.deep.min_headroom_tokens) || 50000, mtf.min_headroom_tokens)` — the flat 50000 was kept as the **FLOOR** and the derived value only ever raises it. Safer than the design; not what the design says. The wiring itself is confirmed: `grep -n "import { headroom" scripts/cortex.mjs` shows cortex importing `maxThinkingFor` from `brain.mjs`, and it is applied on BOTH deep paths (the wake read and the nightly consolidation).)*

## Layer 6 — Overnight deepening
`dmn.mjs` stays the **cheap Gemini dreamer** (grafting raw Opus onto it would create a second unmetered spend path — a Law 2 breach). Concept-graph synthesis into a new `concept_graph.json` routes through cortex via a NEW `consolidation` wake type enqueued by the thalamus — keeping exactly ONE Opus spend path. dmn's away-gate (never dream over his shoulder) is preserved.

*(corrected 10 Aug 2026 — BUILT, and the LAW was kept while the MECHANISM was deliberately changed. Read the builder's own note: `grep -n "DESIGN NOTE: the architecture suggested" scripts/cortex.mjs`.)*
- *(a) **There is no `consolidation` wake type and the thalamus enqueues nothing for it.** It shipped as a direct cortex MODE — `node scripts/cortex.mjs consolidate` — on a nightly schedule (`schtasks /query /tn ArsenalFC-ConceptGraph /fo LIST /v` → `node scripts\cortex.mjs consolidate`, daily, enabled on 10 Aug). The reason is written down in the code: same single Opus path, less surgery, no new afferent-recognition path, thalamus stays sole wake writer **untouched**, and a scheduled nightly batch fits a mode better than a reactive wake. **The Law-2 promise — exactly ONE metered Opus spend path — is intact; the wake-type is not.** Anyone grepping for a `consolidation` wake will find nothing and wrongly conclude Layer 6 was never built.*
- *(b) **`concept_graph.json` now has a documented CONTRACT and a real reader**, neither of which existed at design time. The shape is frozen at `grep -n "THE concept_graph.json CONTRACT" scripts/cortex.mjs` — `schema_version` · `nodes` · `edges` · `clusters` · `next_unlocks` · the grounding guarantee that every id is a member of `concepts.json`. Its only reader is `setpiece.mjs` (`grep -n "concept_graph" scripts/setpiece.mjs`), which compiles `confused-with` edges into contrast drills. **Nothing here may be renamed without updating that reader in the same change.***
- *(c) **dmn stays the free Gemini dreamer, as promised** — `grep -n "generatePool" scripts/dmn.mjs` runs `gemini-flash-latest`, and its tank accounting is careful enough to tell a Gemini 429 apart from a Claude-side wall so it never faults a tank it did not spend (`grep -n "const geminiFault" scripts/dmn.mjs`). Note the one nuance the design line does not carry: rollouts may fall back to `claude -p` when a lane has no Gemini key, so "cheap" is the rule and not an absolute.*

---

## The cognition engine (model ladder + governor)
| Rung | Model | Cadence | Role | Cost |
|---|---|---|---|---|
| Pulse | **Haiku** | 60-90s (engaged only) | semantic watch above salience; escalate/hold | ~1% of Opus |
| Synthesis | **Sonnet** | every few min | live session-read; keep working_set/cartridge current | moderate |
| Deep read | **Opus** (cortex :4112) | on `tau1` doubt + overnight flood | extended thinking 16k live / 32-60k night | high, gated |
| Distiller + dreamer | **Gemini-flash** (free) | ~5 min / hourly | working_set + overnight rollouts | **zero Max** |

> *(corrected 10 Aug 2026 — **this table is the DESIGNED ladder, not the running one.** Three of its four rows have moved and one row does not exist. Row-by-row, verified against code:*
> - *Pulse — **cadence is 150s, seconds-pinned**, not "60-90s" (`grep -n "min_spacing_s" scripts/brain.mjs`); engaged-only, hard cap and per-pulse metering all hold. "~1% of Opus" was **measured false and corrected in code**: every `claude -p` re-pays a full CLI boot, so the pulse measured 32,480 tok/call against an Opus job's ~20k — see the arithmetic at `grep -n "THE CAP WAS DENOMINATED IN THE WRONG UNIT" scripts/brain.mjs`. Post-lean cost is re-measured off the ledger; `node scripts/brain.mjs status` prints the live tok/pulse.*
> - *Synthesis — **this row does not exist in the build.** No every-few-minutes Sonnet layer was ever wired (Layer 4 (d) above has the evidence). Delete it from any plan you derive from this table.*
> - *Deep read — `:4112` correct, `tau1`-gated correct; **thinking depth is 16k live / 48k overnight**, not "32-60k" (Layer 5 above).*
> - *Distiller + dreamer — **distiller is 15 min, not ~5** (`schtasks /query /tn ArsenalFC-Distiller /fo LIST /v`); free-pool and zero-Max hold.*
> *For the live ladder, always read `node -e "console.log(require('./dressing-room/state/brain_config.json').jobs.map(j=>[j.id,j.model,j.window,j.at||''].join(' | ')).join('\n'))"` and the daemon/pulse blocks beside it — never this table.)*

**Net:** Haiku thinks about him all day at ~1% cost, Sonnet keeps his working-set live, and every night the pacer pours the entire remaining plan into deep Opus reads + dmn rollouts (two overlapping 5h windows across the 9.5h night → ~1.5M tokens/night) — while the live reserve keeps his live turns untouched. **Gemini Live carries all voice for free, so ~100% of Max stays available for Claude cognition; the two never compete for the same meter.**

*(corrected 10 Aug 2026 — the STRUCTURE of this paragraph survives; the arithmetic and one clause do not. **"~1.5M tokens/night" was derived from the 5x-era 800k window and is stale** — the live window/weekly pair is read with `node -e "console.log(require('./dressing-room/state/brain_config.json').budget)"`, so re-derive the night from that, never from this sentence. **"Sonnet keeps his working-set live" is false** — that rung was never built and the working set is the Gemini-flash distiller's (Layer 2/Layer 4 above). **"~1% cost" for Haiku was measured false** — see the table note. What DOES hold, and is worth keeping: the two meters really are separate — the voice bridge runs on Gemini Live's free tier (`grep -n "Gemini Live" scripts/dugout.mjs`) while Claude cognition rides the one `brain_ledger.jsonl`, so voice cannot eat study budget.)*

---

## What the ADHD tax removal actually buys
- **Externalized working memory** — `working_set.json` holds his 4 slots continuously, on the free pool. His prefrontal cortex never carries context across an app-switch again. *The single biggest ADHD-PI tax, lifted for zero Max budget.*
  - *(corrected 10 Aug 2026: "**continuously**" means **every 15 minutes**, on a scheduler, not a resident loop — Layer 2 (c). The 4 slots and the free pool hold. Read the live card with `node -e "console.log(require('./dressing-room/state/working_set.json'))"`, and its switch-to-read lag with `node scripts/distiller.mjs latency`.)*
- **Zero capture tax on every surface** — type = captured (Code hooks), copy = captured (turnstile), study = captured (scribe MCP + `/forge`), ambient = captured (context.mjs). He never runs a log command again.
  - *(corrected 10 Aug 2026: "**scribe MCP**" is the tool `note(kind,text)` on the `organism-memory` server — there is no tool named `scribe` (Layer 1). And "type = captured (Code hooks)" is narrower than it reads: only `UserPromptSubmit` and `Stop` are captured, so **his typed prompt and my reply are captured, tool calls are not** — `PostToolUse` was never wired. A fifth lane not in this list has since been added: harvested Gemini sittings (modality `gemini`, 9 Aug) — `grep -n "gemini" scripts/distiller.mjs` shows it joined the interactive window.)*
- **No re-explaining across rooms** — a doubt raised by voice is present when he opens Code; a Desktop confusion resurfaces in the Gaffer.
  - *(verified 10 Aug 2026 — the wiring exists end to end: an MCP `note` lands as modality `desktop-study` at the thalamus door, and the same modality is in the distiller's interactive window AND in the brain's live-signal list (`grep -rn "desktop-study" scripts/distiller.mjs scripts/brain.mjs scripts/thalamus.mjs`). The thalamus's own selftest asserts an `organism-memory` note "is HIS voice too — it scores". One nuance the sentence hides: this works because `note()` is called, and calling it is model-mediated on Desktop — so the cross-room promise is only as strong as Layer 1's honest floor.)*
- **Context-switch recovery** — after a stall or surface change, `get_context()` / SessionStart re-serves exactly where he was ("you were mid-thought on kv-cache in this doc"). Re-entry is a read, not a decision.
  - *(verified 10 Aug 2026 — both doors exist and both really carry it. `get_context()` splices cartridge + working set + teaching card (Layer 3 (d)); SessionStart runs `learnstate.mjs brief`, which reads `working_set.json` and splices the hippocampus cartridge (`grep -n "working_set.json" scripts/learnstate.mjs`, `grep -n "buildRehydrateCartridge" scripts/learnstate.mjs`). Two things the design did not anticipate and a reader must know: the brief is a **BUDGETED 12k snapshot** rendered through `scripts/context_manifest.mjs`, worst-priority-first, with a footer naming anything trimmed — it is not the whole store; and the working set's own freshness is age-tagged, so a stale card reads as stale rather than as truth.)*
- **No budget anxiety** — lockouts only ever happen at 3am while he sleeps.
- **Proactive escalation without a decision** — the Haiku pulse decides FOR him whether a moment deserves a deep read.
  - *(corrected 10 Aug 2026 — **the pulse does not decide; it only NAMES.** By construction it POSTs an afferent and the thalamus alone decides whether the moment crosses the wake bar (`grep -n "the thalamus stays the sole authority" scripts/brain.mjs`). And as of the 9 Aug unpause its salience weight is **0.00** — it moves no salience at all, deliberately, until the metered probes fit a weight (`node -e "console.log(require('./dressing-room/state/brain_config.json').pulse._g14_unpause_note)"`). So today this bullet buys an INSTRUMENT reading, not an escalation. Its first real consumer is the pre-answer gate, not the wake queue.)*
- **Calibration guard** — full turns (with gut-word) are captured, not just the clean reps he chooses to log, so the record reflects his real confusion.
  - *(corrected 10 Aug 2026 — **the captured turn carries NO gut-word.** The nerve emits exactly five fields: `grep -n "const evt = {" -A 6 hooks/afferent-post.mjs` → `modality · source · text · cwd · ts`. The gut-word lives on REPS, which arrive through `capture.mjs` / the turnstile / `/forge`, on a different lane entirely. What this bullet really buys is that **both sides of the turn** are captured — his prompt AND what he was taught (`source: "claude-code-teaching"`) — uncapped since his 6 Aug "there should be no limit" ruling, which the header records was silently truncating 46% of teaching messages at exactly the place the check-question lives. That is the real calibration win; the gut-word half is not built here.)*
- **Nothing to manage** — pacer, reserve, ceiling-hunt, thinking-depth all self-tune. Zero executive-function load.
  - *(verified 10 Aug 2026: all four self-tune in code — `reserveNow` · `blendCeiling` · `maxThinkingFor` · `targetBurn`, all exported from `brain.mjs` (`grep -n "^export {" scripts/brain.mjs`). One honest caveat the bullet omits: the pulse's pause/unpause and the budget's 20x re-split were **captain decisions written into `brain_config.json` by hand**, not self-tuned — the governor tunes within a budget, it does not set the budget.)*

---

## Phased build (each phase proven green before the next — CLAUDE.md law)

> **STATUS, 10 Aug 2026 — READ THIS BEFORE PLANNING ANY OF IT.** This was written as a
> forward plan and every phase below reads like open work. **Phases 0-5 have SHIPPED.**
> Phase 6 has not, by design. Do not re-plan a built phase; that is the exact failure this
> repo has scars for. Verify any phase in one line before you touch it — the `touches:`
> lists below now carry a live command instead of a line number, because **four of the six
> original line citations had already drifted** by the time this audit ran (`brain.mjs`'s
> self-tune block is nowhere near line 463-490; `cortex.mjs`'s env and gate lines had all
> moved). **Never cite a line number in this file again — cite a grep.**

- **Phase 0 — Governor hardening** (cheap, high-value, no new spend path). `reserveNow()` + `maxThinkingFor()` + cortex `min_headroom_tokens` fix + overnight ceiling-probe EWMA (05:30 cutoff). All additive to `brain.mjs headroom()`, self-tests stay green. **Ship first.**
  - *touches:* `scripts/brain.mjs` (headroom, self_tune ~463-490), `scripts/cortex.mjs` (env 100/122, gate 173), per-surface heartbeat files.
  - **SHIPPED.** *(corrected 10 Aug 2026 — anchors replaced with greps, because all three cited line numbers had drifted. `grep -n "function headroom\|function reserveNow\|function blendCeiling\|function maxThinkingFor" scripts/brain.mjs` · `grep -n "cfg.budget.self_tune" scripts/brain.mjs` · `grep -n "MAX_THINKING_TOKENS" scripts/cortex.mjs` · `grep -n "mtf.min_headroom_tokens" scripts/cortex.mjs`. **"per-surface heartbeat files" were never built** — see Layer 5 (ii); the live-signal is three tail reads, not heartbeat files. Prove the phase green with `node scripts/brain.mjs selftest` and `node scripts/cortex.mjs selftest`.)*
- **Phase 1 — Working memory + Code capture (the ADHD-tax core).** `distiller.mjs` (sole writer `working_set.json`, free pool, 8th fuelboard tank) + net-new `.claude/settings.json` Code hooks (capture + injection, fail-silent, scrub-before-git).
  - *touches:* `scripts/distiller.mjs` (new), `working_set.json` (new), `.claude/settings.json` (new), `hooks/afferent-post.mjs` (new), `scripts/fuelboard.mjs`.
  - **SHIPPED.** *(corrected 10 Aug 2026: all five artefacts exist — `ls scripts/distiller.mjs hooks/afferent-post.mjs .claude/settings.json` and `git ls-files .claude/settings.json .mcp.json hooks/afferent-post.mjs`. Two drifts: the distiller is now sole writer of **two** files (Layer 2 (a)), and `.claude/settings.json` is no longer "net-new Code hooks" — it now carries five unrelated organs on `UserPromptSubmit` alone, so **treat it as a shared, contended file, not this phase's private one**. Green on 10 Aug: `node scripts/distiller.mjs selftest` → ALL CHECKS PASSED.)*
- **Phase 2 — Shared retrieval + Desktop bridge.** `organism-memory` MCP (recall/note/get_context/remember_fact→staged) in `.mcp.json` + Desktop config; scribe tools routing reps through `capture.mjs`; shared `recall()` over merged index.
  - *touches:* `scripts/mcp-memory.mjs` (new), `scribe_log.jsonl` (new), `identity_facts.pending.jsonl` (new), `.mcp.json`, Desktop config (outside repo), `scripts/hippocampus.mjs`, project instructions.
  - **SHIPPED, with one sub-item never built.** *(corrected 10 Aug 2026: **"scribe tools routing reps through `capture.mjs`" did not happen** — `grep -n "capture" scripts/mcp-memory.mjs` is empty; the MCP imports `hippocampus.mjs` and `learnstate.mjs` only, and moments become episodes via `markMoment`, not reps via `capture.mjs`. Reps still arrive through the turnstile / `capture.mjs` / `/forge` lanes, untouched by this phase. Also: both new files live under `dressing-room/hippocampus/`, not `dressing-room/state/` — Layer 3 (c). Desktop config is **NOT VERIFIED 10 Aug 2026** (it lives outside the repo; treat as a claim). Green on 10 Aug: `node scripts/mcp-memory.mjs selftest` → ALL CHECKS PASSED.)*
- **Phase 3 — Ignite the deep brain.** `brain.mjs --daemon` pacemaker + `context.mjs` ambient bridge → the never-fired cortex gets a real multi-surface river and FIRES. Verify a real `tau1` wake enqueues and cortex serves it.
  - *touches:* `scripts/brain.mjs` (--daemon), `scripts/context.mjs` (new), `scripts/thalamus.mjs` (unchanged), `scripts/cortex.mjs` (confirm fires).
  - **SHIPPED — and the verification the phase demanded has passed.** *(corrected 10 Aug 2026: `cortex_wake` rows exist in the ledger (count them with the command under THE CEILING). Both daemon args work — `grep -n "mode === \"daemon\" || mode === \"--daemon\"" scripts/brain.mjs` — and the scheduler uses the bare `daemon` form, so a grep for `--daemon` alone will miss the live task. `context.mjs` runs as a **resident daemon** started from `setup/START_DAEMONS.vbs` (`grep -n "context.mjs daemon" setup/START_DAEMONS.vbs`), NOT from a schtasks entry: the old per-minute `ArsenalFC-Context` task was retired (`grep -n "ArsenalFC-Context" setup/INSTALL_CYBORG_TASKS.ps1`) because 1,440 cold starts a day bought ~145 emits. Live counters: `node scripts/context.mjs status`. Green on 10 Aug: `node scripts/context.mjs selftest` → ALL CHECKS PASSED.)*
- **Phase 4 — Haiku pulse (measured, not assumed).** `haiku_pulse` job, hard daily cap, metered every pulse. Prototype + measure real per-call CLI cost for several days before trusting "continuous". **The single most fragile piece.**
  - *touches:* `scripts/brain.mjs`, `brain_ledger.jsonl`.
  - **SHIPPED — and it earned its "most fragile" billing twice.** *(corrected 10 Aug 2026: the measurement this phase demanded WAS done and it **refuted the design's cost assumption** — 32,480 tok/call, of which ~31,970 is `claude -p` boot tax; at that price the "cheap" layer cost 1.61x an Opus job and took 86.6% of one day's spend. It was PAUSED 2 Aug, re-derived, then UNPAUSED 9 Aug as a pure instrument with thalamus weight 0.00. Full arithmetic: `grep -n "THE CAP WAS DENOMINATED IN THE WRONG UNIT" scripts/brain.mjs` and the `_note` / `_g14_unpause_note` keys in `node -e "console.log(require('./dressing-room/state/brain_config.json').pulse)"`. **Never read its enabled/paused state from prose — it has flipped twice.** Note also `brain_ledger.jsonl` is a shared append lane (Law 1), not this phase's file.)*
- **Phase 5 — Overnight deepening + reboot-proofing.** `concept_graph.json` via a cortex `consolidation` wake (ONE Opus path); register `distiller`/`context`/`brain --daemon` reboot-proof.
  - *touches:* `scripts/cortex.mjs`, `concept_graph.json` (new), `scripts/dmn.mjs`, scheduled-tasks.
  - **SHIPPED — as a cortex MODE, not a wake.** *(corrected 10 Aug 2026: see Layer 6 (a) — `node scripts/cortex.mjs consolidate` on a nightly task, no `consolidation` wake type exists, and the ONE-Opus-path law is intact. `concept_graph.json` exists, has a frozen contract and one reader (`setpiece.mjs`). Reboot-proofing landed differently than "register three tasks": a **watchdog** re-launches the four resident daemons every pass — `grep -n "export const DAEMONS" scripts/daemon_watchdog.mjs` → turnstile :4111 · cortex :4112 · thalamus :4113 · brain :4116, and `schtasks /query /tn ArsenalFC-Daemon-Watchdog /fo LIST /v` for its cadence. **This is why `ArsenalFC-Cortex` and `ArsenalFC-Thalamus` read `Disabled` in schtasks and the daemons are nevertheless up** — do not "fix" those disabled tasks; the watchdog owns them now.)*
- **Phase 6 — Aspirational web capture** (only if he actually studies on claude.ai). MutationObserver extension, self-test-guarded.
  - **NOT BUILT — correctly.** *(verified 10 Aug 2026: no extension in the repo, no modality `web` anywhere — see the Layer 1 note. The condition "only if he actually studies on claude.ai" was never met; a **`gemini` harvest lane** was built instead, which is the same need answered on the surface he actually uses.)*

---

## Honest limits (read before building)
- **Claude Desktop has no hook API** — his primary study room's capture is model-mediated or clipboard only. No deterministic passive full-transcript path. Code (hooks) is the ONLY surface with guaranteed capture.
  - *(10 Aug 2026 — the hook-API claim is about an external product and **NOT VERIFIABLE from this repo; treat as a claim**. The in-repo half still holds: Code is the only surface with a deterministic, checked-in hook wiring (`.claude/settings.json`). One thing has changed around it — **"his primary study room" is no longer obviously Desktop.** A `gemini` harvest lane was built 9 Aug for the surface he actually studies on (`grep -n "gemini" scripts/distiller.mjs` shows it as a first-class interactive modality). That lane is model-mediated too, so it does not weaken the "only Code is guaranteed" law — but planning around "Desktop is the primary study room" is planning against a stale premise.)*
- **"Continuous" ≠ nonstop Opus** — physics wall. Paced controller, not firehose.
  - *(verified 10 Aug 2026 — held, and the build honoured it. The pacer is a governor with a burn-rate signal (`grep -n "function targetBurn" scripts/brain.mjs`), every job self-gates on `headroom()`, and the one place that tried to be "continuous" (the Haiku pulse) was measured, paused, re-derived and seconds-pinned rather than left running. This bullet is the design's most load-bearing restraint and nothing in the build has weakened it.)*
- **The plan caps are unpublished estimates** — until several nights of probing, daytime spend stays conservative; running hot will surface the real Max-5x limits faster than any docs.
  - *(corrected 10 Aug 2026: **"Max-5x" is stale — he is on 20x since 9 Aug**, and the organism runs on a ruled SHARE of that account, not the whole of it. His verbatim split ruling is preserved in config: `node -e "console.log(require('./dressing-room/state/brain_config.json').budget._split_ruling_2026_08_09)"`. The probing mechanism shipped and works (the EWMA above); the caps themselves are still estimates, so the sentence's spirit stands.)*
- **`haiku_pulse` "cheap enough to be continuous" is asserted, not derived** — each `claude -p` call carries real CLI overhead; at 60-90s it could run 500k-1M+ tokens/day and cannibalize the overnight Opus budget. **Hard-cap + meter from day one; measure before trusting.**
  - *(10 Aug 2026 — **this warning was RIGHT, and the measurement proved it worse than feared.** Not 500k-1M/day but 2,762,471 tokens = 32.4% of the rolling week, at 32,480 tok/call against a predicted "~1% of Opus". The hard-cap-and-meter discipline this bullet demanded is the only reason it was caught. Full trail: Phase 4 above. Leaving this bullet exactly as written — it is the single best-aged sentence in the document.)*
- **No pixel sight** — AW is metadata only; silent on-screen reading stays invisible. computer-use/OCR is out of scope (battery/CPU/privacy/brittle).
  - *(corrected 10 Aug 2026 — same qualifier as THE CEILING wall #4: true of the **ambient/passive** layer (`grep -n "Metadata only" scripts/context.mjs`), but the Dugout's opt-in **Watcher (T2)** does see his declared screen inside a live session (`grep -n "T2 Watcher" scripts/dugout.mjs`). "No passive pixel sight" is the accurate form. computer-use/OCR remain out of scope — nothing in `scripts/` does either.)*
- **Privacy surface widens** — full-turn FinOps capture may include client data; cross-surface capture co-occurs with biometric/medication context. Repo is PUBLIC → `afferent.jsonl`, `scribe_log.jsonl`, `working_set.json`, `identity_facts.pending.jsonl` stay gitignored + local; hook text scrubbed before commit; affect firewall + verbatim law at every new door.
  - *(**VERIFIED 10 Aug 2026 — all four files really are ignored, but two of them are ignored for a reason this line gets wrong, and that matters if anyone ever moves them.** Check them yourself, every time, before a push: `git check-ignore -v dressing-room/state/afferent.jsonl dressing-room/state/working_set.json dressing-room/hippocampus/scribe_log.jsonl dressing-room/hippocampus/identity_facts.pending.jsonl`. `afferent.jsonl` and `working_set.json` are ignored by their **own explicit `.gitignore` lines**. `scribe_log.jsonl` and `identity_facts.pending.jsonl` are ignored **only because the whole `dressing-room/hippocampus/` directory is** (`grep -n "dressing-room/hippocampus/" .gitignore`) — and `.gitignore` separately names a `dressing-room/state/scribe_log.jsonl` path that **no code writes** (`grep -n "SCRIBE_LOG" scripts/mcp-memory.mjs` → it is under `hippocampus/`). So the belt-and-braces line points at the wrong path and the directory rule is doing all the work. **Anything moved out of `dressing-room/hippocampus/` loses its cover silently.** Two later files in this design also need the same check and are NOT named above: `context_state.json` and `distiller_latency.jsonl` — both explicitly ignored today (`git check-ignore -v dressing-room/state/context_state.json dressing-room/state/distiller_latency.jsonl`). And per the correction at Layer 1 (e), the hook scrub is at CAPTURE time — it drops the event entirely — not "before commit".)*
- **Proposal volume grows** — nothing auto-writes canon (Law 4 holds), but the batch-glance review ritual must scale with it. `remember_fact` stages to `identity_facts.pending.jsonl` pending his confirm. Goalkeeper medical boundary untouched.
  - *(verified + extended 10 Aug 2026: the staging law holds in code (`grep -n "staged: true" scripts/mcp-memory.mjs`). The **"batch-glance review ritual"** this bullet worried about was in fact solved after the design, by a mechanism not named here: THE CAPTAIN'S CALL — pending facts ride a one-line card at an anchor he already hits rather than accumulating into a report (`grep -n "identity_facts.pending" scripts/captains_call.mjs scripts/context_manifest.mjs`). Anyone re-planning "a review ritual" should read that first.)*
- **Embeddings depend on the free Gemini pool** — a lane-dry outage means new moments land without vectors (swept later) and real-time recall degrades to lexical during the outage.
  - *(verified 10 Aug 2026 — the lexical fallback is real and is the shipped behaviour, not just an intent: `recall()` falls back to lexical when the pool is dry (Layer 3 (a)), and the `UserPromptSubmit` recall hint is **lexical by default, always** (`grep -n "recallReflexLexical" scripts/hippocampus.mjs`) — so the live-turn path never depends on the pool at all. Tank health, live: `node scripts/fuelboard.mjs` — the embed lane is its own tank, and the distiller's T8 was added specifically so it could not starve it.)*
