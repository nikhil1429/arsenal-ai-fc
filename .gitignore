# ============================================================
# Arsenal AI FC — .gitignore
# IMPORTANT: this file must be named ".gitignore" (WITH the dot),
# at the REPO ROOT (C:\Users\nikhi\GitHub\arsenal-ai-fc\.gitignore).
# On Windows, make sure it didn't save as "_gitignore" or ".gitignore.txt".
# ============================================================

# --- SECRETS & TOKENS — never commit (repo is public) ---
# These hold your Oura client_secret + access/refresh tokens.
oura_secrets.json
oura_tokens.json
# (also match them anywhere, just in case they move)
**/oura_secrets.json
**/oura_tokens.json

# --- logs ---
*.log

# --- node ---
node_modules/

# --- the whole-repo bundle (derived artifact; regenerate with `npm run bundle`) ---
ARSENAL_FC_FULL_REPO_BUNDLE.md

# --- OPTIONAL: personal health / medication data ---
# This repo is PUBLIC. The two files below contain your Oura biometrics
# (sleep, HRV, readiness) and your medication-intake timing. If you'd
# rather those NOT be public, uncomment them (or make the repo private):
dressing-room/state/readiness.json
dressing-room/state/intake_log.json

# --- per-run OUTPUT state (data, not code — regenerated every run) ---
dressing-room/state/timeaudit.json

# --- capture data: study/drill reps (personal learning data, not code) ---
dressing-room/state/reps_log.jsonl
# machine-local capture config (this PC's Google Drive inbox path — not canonical)
dressing-room/state/capture_config.json

# --- FSRS agent outputs (derived personal study data: concepts + schedule) ---
dressing-room/state/cards.json
dressing-room/state/fsrs_store.json

# --- Calibration agent output (derived personal study data: gap + danger topics) ---
dressing-room/state/calibration.json

# --- Nemesis agent output (derived personal study data: weak topics + receipts) ---
dressing-room/state/weaknesses.json

# --- Learning-state agent output (derived personal study data: fluency map + edges) ---
dressing-room/state/learning_state.json

# --- Manager (Agent #1) outputs + personal inputs (team sheet / notes / season / captain note / post-match) ---
dressing-room/state/team_sheet.md
dressing-room/state/manager_notes.json
dressing-room/state/season.json
dressing-room/state/captain_note.md
dressing-room/state/post_match/

# --- THE ORGANISM (organism-final) — per-run outputs + personal derived data ---
# capsule mirror (his learning content, verbatim)
dressing-room/state/capsules/
dressing-room/state/mirror_manifest.json
# throw-ins (his raw thoughts) + topic secret + routing state
dressing-room/state/loose_balls.jsonl
dressing-room/state/throwin_topic.txt
dressing-room/state/throwin_state.json
dressing-room/state/routed_balls.json
# sensory pass + proprioception
dressing-room/state/pulse.json
dressing-room/state/loop_vitals.json
# the twin (the book on the captain)
dressing-room/state/twin.json
dressing-room/state/predictions.jsonl
# touchline reads
dressing-room/state/pitch_read.json
dressing-room/state/pitch_read_history.jsonl
# set-piece drills + evening scorer ledgers
dressing-room/state/drills.json
dressing-room/state/slip.jsonl
dressing-room/state/trust_tiers.json
# advance scout + doubt engine
dressing-room/state/scout.json
dressing-room/state/doubt_grammar.json
dressing-room/state/lexicon.json
dressing-room/state/tape_room.json
# genome runtime (proposals + personal changelog; forge_profile.json IS committed canon)
dressing-room/state/mutations.jsonl
SEASON_CHANGELOG.md
# post-match sibling files
dressing-room/state/notebook.json
# the club wall (renders personal data)
dressing-room/state/wall_data.json
dressing-room/club/
# the brain runtime (usage ledger + generated artifacts)
dressing-room/state/brain_ledger.jsonl
dressing-room/state/brain_queue.json
dressing-room/state/brain_out/
# the dugout (live-voice notes + minutes ledger + think-time stamps — personal)
dressing-room/state/dugout_notes.jsonl
dressing-room/state/dugout_ledger.jsonl
dressing-room/state/dugout_stamps.jsonl
dressing-room/state/dugout_scrimmage.jsonl
dressing-room/state/dugout_reminders.jsonl
# the shadow engine (earned proactivity — personal behavioral data)
dressing-room/state/shadow_log.jsonl
dressing-room/state/proactivity_ledger.json
# semantic recall index (his words, embedded)
dressing-room/state/recall_index.jsonl
# dugout voice prefs (his chosen model/voice/depth register)
dressing-room/state/dugout_prefs.json
# dugout session resumption handle (a live session token — never committed)
dressing-room/state/dugout_session.json

# the hippocampus (M2) — his durable memory: episodes, identity facts, who-he-is. LOCAL ONLY.
dressing-room/hippocampus/

# the thalamus + cortex (M1) — his moments, words, frame-hashes: LOCAL ONLY.
# the public repo holds the machinery, never the moments (CYBORG_BRAIN.md §11)
dressing-room/state/afferent.jsonl
dressing-room/state/workspace.json
dressing-room/state/salience_ledger.jsonl
dressing-room/state/wake.json
dressing-room/state/wake_queue.jsonl
dressing-room/state/bg_queue.jsonl
dressing-room/state/cortex_runtime.json
# the fuelboard (M3) — per-account usage ledger (runtime)
dressing-room/state/tanks.json
# the live examiner (M4) — the staged code drill names his weaknesses
dressing-room/state/examiner_drill.json
# neuromodulation (M5) — derived from the Governor verdict (which is private)
dressing-room/state/tone.json
# the Rest Room (M6) — pre-drafted interventions name his weaknesses
dressing-room/state/dmn_precache.json
# the pre-answer engine (M17) — tonight's predicted doubts + answers (his words)
dressing-room/state/answer_cache.jsonl
# the season re-read (M18) — quotes his own words across the whole corpus
dressing-room/state/season_read.json
# predictive presence (M7) — stall telemetry + would-have-whispered log
dressing-room/state/presence_log.jsonl
# presence self-calibration (M11) — thresholds fitted to HIS baselines
dressing-room/state/presence_thresholds.json
# the living dossier (M8) — intra-day belief state over his learning
dressing-room/state/dossier.json
# the kennel lease (M9) — LOCAL until the Pi arrives; then remove this line so
# the lease travels the git bus (see groundsman.mjs header: TRANSPORT)
dressing-room/state/bus_lease.json

# --- JARVIS bibles (deeply personal — relationship/mood/psych material; repo is PUBLIC) ---
Jarvis/
jarvis/

# pre-season vault (the build-era test data, archived never deleted)
dressing-room/vault_preseason_*/
